# Feature documentation moved

Current system documentation is indexed in [docs/features/README.md](features/README.md). This compatibility pointer retains old document links; it contains no duplicate feature content. Historical links to sections should use the index to locate the corresponding system.
