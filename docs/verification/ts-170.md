# TS-170 — Configs accordions and per-category defaults

## Implemented

Inspected Jira TS-170 and TS-97 (complete descriptions; neither has children or comments), repository routing, workflow/standards/critique, DevTools and Developer Options documentation, and the existing staged configuration implementation. Work remains on `ts-170-jg`.

- Every gameplay, local tire-graphics and available local-network category has an independent accordion and contained Reset to Defaults action. Catalog order remains unchanged; categories begin expanded and retain disclosure state for the panel lifetime.
- Collapse/reopen never reconstructs editors or changes drafts. Search exposes matching rows even in collapsed categories, hides categories without matches, and restores prior disclosure states when cleared. Headers remain readable but cannot hide results while search is active.
- Category resets stage the entire category, including filtered-out rows, preserving exact unrelated editor text. Gameplay uses the existing `GameplayConfiguration.HostedDefaults` and `DeveloperOptionsDraft`; local tire settings use `TireEffectSettings.Defaults`; global and category network resets share `NetworkSimulation` defaults.
- The persistent global reset remains in the footer. Existing Apply, Cancel, validation, dirty-close, authority, synchronization and persistence owners remain in use. No Core/backend/wire/recovery implementation changed; no Stats/Logs redesign.
- Environment dropdown now uses the existing 150-pixel value-column minimum, correcting a five-pixel alignment discrepancy exposed by the responsive native check.
- Feature documentation and verification index updated. Required Story version is `0.1.67`, with export metadata `0.1.67.0`, against fetched main `66f8a82` (`0.1.66`).

## Verification performed

Windows; Godot `4.7.2.stable.mono.official.ed1daf0bf`; OpenGL compatibility on NVIDIA GeForce RTX 4070 Laptop GPU. Commands use `DOTNET_PROCESSOR_COUNT=4` and Godot executable `C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

| Command/check | Actual result |
| --- | --- |
| Fetch main; `tools/sync-story-version.ps1`; `tools/check-version.ps1` | PASS; current main is already an ancestor, correct canonical/export versions. |
| `tools/check-fast.ps1 -Area Client` | PASS; focused Debug production build, zero warnings/errors. Initial sandboxed attempt could not complete restore; installed SDK/cache access succeeded outside the sandbox. |
| `dotnet test code/TransportTests/Trackstorm.Transport.Tests.csproj -c Release --filter FullyQualifiedName~DeveloperOptionsTests` | PASS, 15 tests. Includes every gameplay category, exact unrelated invalid text, idempotence, Cancel, combined authority transaction and stale baseline isolation. |
| `check.ps1` | PASS: 108 version-rule checks, workflow/media regression checks and media validation, restore, Debug production and Release full-solution builds with zero warnings/errors, 844 Core tests and 362 non-native Client/transport tests. |
| `check-developer-options.ps1 -GodotPath <above> -Visual` | PASS on final implementation: 1,191 write assertions and 9 separate-process restart assertions. |
| `check-menu.ps1 -GodotPath <above> -Visual` | PASS on final implementation: 2,402 assertions, including keyboard/controller accordion activation and 640x360, 1280x720, 2560x1080 Configs layout. |

The first added native assertion incorrectly compared displayed float text as a double; it was corrected to use the catalog's actual numeric type, then the full DevTools check passed. The first responsive menu run exposed the dropdown alignment issue above; it passed after correction. These earlier failures are not represented as successful runs.

## Runtime / manual / native evidence

**VERIFIED through rendered production Godot controls and synthetic native input:** every accordion starts open, toggles independently and retains text; multiple categories stay open; filtered Salvo reset resets all Salvo rows, updates blue default coloring and retains staged Machine Gun, Nitro, vehicle, tire and network values. Category/global resets leave runtime revision and persisted files unchanged until Apply. Combined Apply commits one revision, synchronizes to a real local UDP peer and persists. Cancel restores effective gameplay and local network values. Search clears back to prior disclosure states; tab switches preserve collapse state. Existing native checks exercise dirty-close choices, invalid values, authority rejection, persistence failure/retry, restart persistence, and read-only tabs.

**VERIFIED visually:** rendered filtered-reset and collapsed-category captures; menu captures at small, standard and wide viewport sizes. Final local artifacts are `.godot/developer-options-checks/573aa417db4946c38509070772dc8925` and `.godot/menu-checks/52fb25a3718742e3bd5009c520386578`.

Direct desktop mouse/keyboard inspection was attempted with the computer-use skill against an isolated `--phase=inspect` host. The host launched successfully with separate settings, but the Windows tool's app-approval request timed out before desktop interaction. That process was stopped. This is not claimed as manual click-through evidence.

## Assumptions, limitations and unverified areas

- Existing catalog groups define reset boundaries; the catalog is not reorganized (for example, Nitro fields remain in their existing groups).
- Local tire/network categories retain their existing local-only behavior and distinct defaults. No persistent accordion preference is added.
- Native network evidence is same-machine UDP, not authenticated multi-device EOS. Physical controller ergonomics and a newly exported executable remain unverified.
- The 640x360 viewport retains the pre-existing small scrolling area beneath fixed shell controls; bounds, focus scrolling and action accessibility were checked.
- Pre-existing `assets/maps/oval/grass_variation.png.import` modification is excluded from this Story's commit.

## Additional final native results

| Command (same Godot executable) | Actual result |
| --- | --- |
| `check-startup.ps1 -GodotPath <above> -Visual` | PASS, production main scene, phase-aware startup/recovery and mixed input navigation; no diagnostics. |
| `check-reconnect.ps1 -GodotPath <above> -NoBuild` | PASS, real local UDP, three arena resyncs including 125 seconds offline, retained configuration/state and prediction reset. |
| `check-migration.ps1 -GodotPath <above> -NoBuild` | PASS, three native UDP peers, lobby host leave, former-host fresh join, active authority loss and sequential epochs with retained state. Trusted identity seam; not authenticated EOS. |
| `check-statistics.ps1 -GodotPath <above> -NoBuild` | PASS, 212 assertions. |
| `check-event-log.ps1 -GodotPath <above> -NoBuild` | PASS, live replication, bounded/frozen/filtered journal, activity feed and input isolation. |
| Final diff/whitespace inspection | PASS; only Configs presentation/draft, its verification, feature docs and required Story metadata are included. Existing asset-import edit excluded. |

Saved evidence: [full deterministic gate](ts-170/check.log), [native run summary](ts-170/runtime-results.log), [filtered category reset](ts-170/filtered-category-reset.png), [collapsed categories](ts-170/collapsed-categories.png), [640x360 Configs](ts-170/configs-640x360.png). Native command logs are retained beside these files.

No in-scope defect remains identified by final verification. Recovery regressions are directly exercised by existing native harnesses, but the combination of a pending UI category reset and a live authority migration is inferred from unchanged session/epoch draft-refresh semantics rather than a separate physical end-to-end scenario.

## Astra Runtime Critique — Story Round 1

Independent `gpt-6-astra` runtime/experiential review after all final verification:

- **Overall Score: 8.4 / 10**
- **Quality Assessment: PASS (>=8.0)**
- **Category scores:** Runtime Functionality 8.8; Controls / Responsiveness 8.2; Visual Quality 8.3; UI / UX 8.2; Runtime Stability 8.7; Runtime Integration 8.6.

Astra independently reran the rendered production Configs harness (1,191 write and
9 restart assertions) and menu harness (2,402 assertions), both without reported
runtime errors or warnings. It directly viewed fresh collapsed-category,
filtered-Salvo-reset and expanded-category captures, plus 640x360, 1280x720 and
2560x1080 layouts. It found category reset containment, explicit staged feedback,
readable default/override colors and persistent global/footer actions coherent and
dependable. The score reflects rendered behavior and presentation, not test counts
or static engineering quality. No corrective recommendations were made.

**VERIFIED for the critique:** production controls exercised through harness
signals and synthetic native input; independent disclosure, preserved drafts,
search restoration, filtered whole-category resets, unrelated draft preservation,
local resets, global reset, Apply/Cancel, same-machine UDP synchronization,
persistence and responsive navigation/layout.

**INFERRED for the critique:** broader recovery continuity; the parent directly ran
reconnect/migration/startup/Stats/Logs and Astra corroborated their results log but
did not rerun those checks. **UNVERIFIED:** physical click-through/controller
ergonomics, authenticated multi-device EOS, an exported executable, sustained
performance and subjective latency. No separate performance/latency score was
assigned. At 640x360, the roughly two-row content area remains constrained despite
passing focus scrolling and fixed-action bounds.

Fresh independent evidence: [Astra Configs run](ts-170/astra-configs.log) and
[Astra menu run](ts-170/astra-menu.log). Fresh viewed captures remain under
`.godot/developer-options-checks/6788f49b298d428694ea8feb326fca52/` and
`.godot/menu-checks/296aa054c9b3449087dc770cb152bcc3/`.

No further implementation or critique round was performed after this review.
