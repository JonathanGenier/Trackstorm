# TS-139 verification evidence

## Round 2 — approved visual target and TS-149 through TS-154

Current requirements were read from Jira TS-139 and all six subtasks, including comments, plus the user-approved pasted implementation request and `ChatGPT Image Sep 23, 2026, 05_52_30 PM.png`. The current Jira scope agrees with these approved additions. The earlier Round 1 evidence below is historical and is not substituted for current verification.

### Implemented

- Fixed orthographic showcase composition for eight live presentation-only Wasteland vehicle meshes, with names, HOST and colored READY/NOT READY states. The approved reference was edited with built-in imagegen into a vehicle/UI-free environmental matte; native lighting and contact shadows accompany the actual 3D vehicles. The tent, scaffolding, carnival bulbs, wet platform, atmosphere and clown entrance are baked environmental art. No gameplay, collision, weapons, AI or match scene runs inside the lobby viewport. This is a hybrid fixed-camera scene, not fully modeled 3D scenery.
- TS-149: shared Current Map panel with native captures of Old Map/New Map; host-only Map Select through existing authority. TS-150: Core Start requires every connected non-host Ready, with no host readiness requirement; join/removal/reconnect preserve unrelated readiness.
- TS-151: migration restores survivor lobby readiness, map and existing configuration from the accepted checkpoint. Host markers and controls follow current authority; surviving vehicle roots and slots persist. Existing checkpoint cadence, agreement, retirement fences and rollback semantics remain unchanged.
- TS-152: authoritative Arena phase immediately projects Match Loader and locks conflicting actions. Recoverable initial resource/sync failure uses existing host Return. An admitted client's bounded reliable failure notification uses the existing session/epoch/generation envelope; host recovery is denied after entry release. Cancelled resource requests are drained, preventing an observed native load-token leak. Terminal session failures retain existing Leave cleanup.
- TS-153: vacant presentation slots are allocated per stable ID; survivors are not repacked, fresh reconnects do not duplicate models, and replaceable art retains the same showcase root.
- TS-154: selecting another player's nameplate exposes contextual Kick; a separate named/ID confirmation is required. Cancel does not mutate membership. Confirmation revalidates authority, session, epoch and target. Native mouse and logical keyboard/controller flows are exercised.
- Host-only Lobby Settings edits only existing match mode and kill target through Core validation. Personal Settings and Quit retain existing owners and cleanup. Approved Main Menu plates are reused; no large branding panel or persistent Kick button is present.

### Verification evidence

Environment: Windows, Godot 4.7.2 .NET, RTX 4070 Laptop GPU / OpenGL compatibility renderer. `GodotPath` below is `C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

- Fetched `origin/main`, merged current `51f8df8` into the existing Story branch; merge commit `4c5c214`. Mechanical version/export/index conflicts retained current main plus the TS-139 index entry. Re-fetch before final verification found the same main.
- `./tools/sync-story-version.ps1` and `./tools/check-version.ps1`: PASS, canonical `0.1.42`, Windows file/product `0.1.42.0`.
- Targeted `tools/check-fast.ps1 -Area Client`, `-Area Core`, `-Area Transport` were used while iterating. Early native layout checks exposed initial camera projection/non-finite bounds and overlapping labels; corrected before final verification. The first updated transport run exposed four stale tests expecting migration to clear Ready; their assertions now check the approved preservation rule.
- `./check.ps1`: PASS. Debug production and Release full-solution builds, zero warnings/errors; **594 Core + 354 non-native Client/transport tests**. Log: `.godot/ts-139-round2-check.log`.
- `./check-lobby.ps1 -GodotPath <above> -Visual -NoBuild`: PASS, no warning/error markers. Current artifacts: `.godot/lobby-checks/e6ad7fb060154928a556303fc582983f/`. Eight production session UIs over real local UDP; ready toggles, host/client authority, map UI, Lobby Settings, Kick cancel/confirm, join/departure, unexpected disconnect/fresh rejoin, stable survivor slots, Quit cleanup, repeated Start/conflicting actions, injected host and client loader failures followed by authoritative recovery/retry, both actual maps, Match Loader/Sync, Podium/Return, active admission/interruption and retained capacity. Nameplate bounds/non-overlap checked at 640×360, 1280×720 and 1600×900. Earlier recovery runs completed behavioral checks but failed strict shutdown due one native load token; those are not counted as clean passes.
- `./check-menu.ps1 -GodotPath <above> -Visual`: PASS, **1,204 assertions**, no warning/error markers. Log `.godot/ts-139-round2-menu.log`; artifacts `.godot/menu-checks/48409dbc8c424be59e6fbec70dd2cb7c/`. Production Settings/Back reuse, overlay suppression, visual replacement, native mouse selection, keyboard confirmation opening, controller cancellation and confirmation, repeated use, arena/menu integration, leave and production Quit. Inputs are synthetic Godot native events, not a physical gamepad.
- `./check-transport.ps1 -GodotPath <above>`: PASS, **365 tests including native tests**, then three Godot transport creation/poll/message/cleanup cycles. Log `.godot/ts-139-round2-transport.log`.
- `./check-reconnect.ps1 -GodotPath <above> -NoBuild`: PASS, three arena resyncs including **125 seconds offline**, lobby fresh admission, native body reuse, prediction reset, items/spawn/match continuity and generation reset over real UDP. Log `.godot/ts-139-round2-reconnect.log`.
- `./check-migration.ps1 -GodotPath <above> -Players 2 -NoBuild` and `-Players 3`: PASS. Ready/map/configuration preserved at the accepted checkpoint, surviving showcase roots/slots retained, host controls transferred, former host rejoins as client; existing active-match/sequential migration assertions also pass. Logs `.godot/ts-139-round2-migration2.log` and `migration3.log`. An initial iteration requested departure before the new state reached the existing checkpoint cadence; the fixture now explicitly waits for the accepted boundary and does not change migration policy.
- `./check-online-lobby.ps1 -GodotPath <above> -NoBuild`: PASS, fake-provider frontend/admission/retained-session idempotency, not authenticated EOS. Log `.godot/ts-139-round2-online.log`.
- `./check-startup.ps1 -GodotPath <above> -Visual -SkipBuild`: PASS, frontend media hashes plus production startup/navigation/Settings/browser/media lifecycle, no native diagnostics. Log `.godot/ts-139-round2-startup.log`.
- `git diff --check`, affected current-document link checks, and the three-file lobby asset SHA-256 manifest check: PASS.
- Native previews were rendered from the production map scenes with the retained `assets/frontend/lobby/render-map-previews.gd`; imagegen provenance, prompt summary and hashes are in that directory's manifest. No external asset or font download was introduced.

### Assumptions, limitations and explicitly unverified areas

The existing Wasteland model is used for every participant because no authoritative vehicle-selection field exists. Only its existing identification material varies cosmetically. The reference's different detailed vehicle models are not supplied as production assets. Scenery is a baked environmental matte; no dynamic fog, flame or light animation is claimed. The UI and live 3D vehicles remain independently replaceable.

Local UDP and trusted migration identity/retirement seams do not prove real authenticated EOS, WAN, multi-machine or physical controller behavior. Exported executable packaging, audible output, sustained performance/low-end GPU profiling, exhaustive Unicode names and non-16:9 aspect ratios remain unverified. Migration retains the existing accepted-checkpoint rollback window; state changes newer than that boundary may roll back as before. Resource-load failure is injected at the production loader factory, not by corrupting shipped assets. The existing deterministic initial-sync timeout test covers that timeout; no native 30-second missing-ack timeout has been separately injected in this round.

Final verification is complete. Independent engineering review and formal Round 2 critique are pending for this revision; their evidence will be added before handoff.

---

## Historical Round 1 evidence (superseded implementation)

### Original implementation record

## Implemented

- Dedicated fixed-camera 3D joined Lobby, eight static visual-only vehicle showcases, connected-authority identity/name/HOST/READY projection, no fake empty-slot players.
- Host Start/Settings/Quit/Kick and non-host Ready/Not Ready/Settings/Quit. Existing host map control retained. Start consents through existing Ready authority, then requests the unchanged Start/Loader/Sync transition.
- Host-only lobby Kick uses existing Core removal, normal roster publications and reliable rejection/delayed transport close. No new membership, ban, admission or vehicle-selection authority.
- Replaceable visual factory and per-player model refresh; current authoritative lobby has no selection field, so existing Wasteland art is the fallback.
- Approved Main Menu crest/plate textures, existing vehicle/arena assets and original stage geometry. No new external assets or gameplay systems.

## Verified commands and results

Environment: Windows, Godot 4.7.2 .NET, NVIDIA RTX 4070 Laptop GPU / OpenGL compatibility renderer for visual runs. Godot executable: `C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

- `git fetch origin main`, `git merge origin/main`, `./tools/sync-story-version.ps1`: already synchronized; canonical version 0.1.34, Windows preset 0.1.34.0. Jira's target release does not authorize a release-version bump.
- `./tools/check-fast.ps1 -Area Client`: repeated focused Debug builds passed with zero warnings/errors. Explicit area was used while changes were uncommitted because automatic routing compares committed branch history.
- `./check.ps1`: PASS. Debug production and Release full-solution builds with warnings as errors; 540 Core tests and 353 non-native Client/transport tests passed. Log: `.godot/ts-139-check.log`.
- `./check-lobby.ps1 -GodotPath <above> -Visual -NoBuild`: PASS on final implementation. Artifacts: `.godot/lobby-checks/07e155e5c1684ef9b16a3ccc7dba2d7d/`. Eight real local UDP sessions and native Godot worlds. Host/client permissions, self-kick rejection, selection/Kick removal on all remaining peers, kicked-client cleanup, fresh rejoin, unexpected lobby disconnect/rejoin, Quit/Main Menu, ready/unready, unauthorized client Start, repeated host Start, both maps, Match Loader/Sync, Podium/Return, fresh active admission, interrupted admission and retained-slot capacity exercised. Nameplate viewport bounds/non-overlap asserted at 640x360, 1280x720 and 1600x900.
- `./check-menu.ps1 -GodotPath <above> -Visual`: PASS, 1,038 assertions. Final artifacts: `.godot/menu-checks/9298526850ca437092ba5274dd42ec33/`; log `.godot/ts-139-menu.log`. Added two production lobby Settings/Back cycles, suppression of underlying buttons, retained showcase identity, visual replacement retaining exactly one model, and solo host Start without Ready UI. Existing gameplay navigation, input suppression, leave and application Quit also passed.

- `./check-reconnect.ps1 -GodotPath <above> -NoBuild`: PASS. Three arena resynchronizations including 125 seconds offline, lobby fresh admission, checkpoint/native body reuse, held items, match continuity and new-generation reset; local UDP and trusted test identity. Log: `.godot/ts-139-reconnect.log`.
- `./check-online-lobby.ps1 -GodotPath <above> -NoBuild`: PASS. Fake-provider admission, retained validation/reconnect/release idempotency. Log: `.godot/ts-139-online.log`.
- `./check-transport.ps1 -GodotPath <above>`: PASS. 364 Debug transport tests including native GNS and three Godot node/message/cleanup cycles. Log: `.godot/ts-139-transport.log`.

- `./check-startup.ps1 -GodotPath <above> -SkipBuild`: PASS after updating the legacy duplicate-primary-control assertion to exclude the dedicated joined Lobby controls. Recovery, navigation, Settings/browser return and persistent MenuShell media verified; headless, not audible validation. Log: `.godot/ts-139-startup.log`.
- `git diff --check`: PASS. Complete current Story diff inspected; no Shared layer, gameplay dependency change, new downloaded assets or tracked runtime output.

## Runtime evidence and iteration

The initial rendered composition exposed overlapping nameplates and excessive lighting; both were corrected and subsequent captures inspected. Early harness runs failed because they still expected host Ready or rejoined before the existing exit-completion tick; fixture sequencing was corrected and complete subsequent runs passed. The first startup regression rejected the legitimate lobby Settings button as a duplicate Main Menu target; the scoped fixture was corrected and rerun successfully. Initial sandboxed .NET builds exited without diagnostics; the same checks passed with installed dependency access. These failures are not recorded as passes.

Rendered captures were directly inspected at normal and compact sizes. They show eight separate vehicles and readable associated names/state, reused industrial plates/clown crest, red/cream backdrop, fixed camera and minimal controls. Automated input includes emitted UI signals and existing native input fixtures; it is not a physical multiplayer playtest.

## Assumptions, limitations and explicitly unverified areas

- Current roster contains no selected-vehicle identity; all players use the existing approved vehicle mesh. Future selection work must supply its authoritative asset projection to the visual factory/refresh boundary.
- Kick removes the current session binding; it is not a permanent ban. Fresh admission remains governed by existing systems.
- Authenticated multi-device EOS, remote residential networks, exported executable, physical controller ergonomics, audible output and sustained profiling are UNVERIFIED in this Story. Local UDP/fake-provider evidence does not establish those outcomes.
- No driving, physics, combat or vehicle-selection UI is introduced in Lobby. Static scene assets remain reusable while offscreen rendering stops and player showcases are released outside Lobby.
- Long names use truncation and a full-name tooltip; exhaustive Unicode/name-length combinations and all non-16:9 aspect ratios have not been exercised.

## Reviews

Independent engineering review: PASS, no material correctness/scope/authority/lifecycle/test-design issue found. Astra runtime critique follows. Human acceptance is pending; recommendations are not implemented without a new instruction.

## Astra Runtime Critique — Story Round 1

**Overall Score: 7.8 / 10 — Quality Assessment: FAIL (<8.0).** The overall score is holistic, not an arithmetic average; accurate next-action guidance is central to the lobby.

| Category | Score |
| --- | --- |
| Runtime Functionality | 8.6 |
| Multiplayer / Networking Experience | 8.5 |
| Camera | 8.0 |
| Visual Quality | 8.0 |
| UI / UX | 7.0 |
| Runtime Stability | 8.5 |
| Runtime Integration | 8.5 |

The independent Astra reviewer directly inspected the eight-player 640x360, 1280x720 and 1600x900 captures and the production solo-host capture, then independently reran `check-lobby.ps1 -Visual -NoBuild`. The elevated run passed; artifacts: `.godot/lobby-checks/6453e0dd9b5647e397a1b745f2be0ed9/`. This verified eight-peer ready/unready, Start rejection/acceptance, Kick, cleanup/rejoin, unexpected disconnect, Quit, repeated matches/Return and admission boundary cases. Broader Settings/reconnect/transport conclusions were inferred by the reviewer from the separately verified parent evidence, not independently rerun by Astra.

The first independent sandboxed run completed gameplay assertions but failed the wrapper due denied user-log writes and certificate-store access. It is not a clean pass; the subsequent elevated run passed.

### Recommendation and unresolved runtime risk

- **Problem/evidence:** Host footer says “Waiting for everyone to ready up” while seven clients visibly show READY and Start is available. The solo-host capture has the same instruction despite there being no Ready action for the host. The harness successfully starts both conditions.
- **Severity:** Medium. **Impact:** Misleading primary transition guidance can make the host believe another readiness action is required.
- **Suggested improvement:** Show “Ready to start” when all non-hosts are ready (including solo host); otherwise show the unready-player count. Keep Kick guidance secondary.
- **Scope:** In Scope. **Corrective Work Type:** Existing Task.
- **Recommended next round, if authorized:** Correct the guidance, inspect rendered solo/partially-ready/all-ready states, and recheck Start plus ready toggles.

The scene was judged coherent, readable and operationally robust in local multiplayer. Physical controllers, real multi-device EOS, remote networks, audible output, exports, sustained profiling, exhaustive names and non-16:9 lobby layouts remain unverified. No recommendation was implemented after this formal critique. The requested branch delivery is a reviewable handoff, not runtime acceptance or permission to merge.

## Delivery

Implementation committed and pushed on `ts-139-jg` as `0faa4d3`; PR [#85](https://github.com/JonathanGenier/Trackstorm/pull/85) is open, not merged. Post-push independent engineering review: **PASS**, current fetched main ancestry/version checks passed and working tree was clean. GitHub CI run [35850582987](https://github.com/JonathanGenier/Trackstorm/actions/runs/35850582987) reported `action_required` with no jobs started; remote CI is **UNVERIFIED**, despite successful local verification. This report's delivery note is a subsequent documentation-only commit.
