# TS-139 verification evidence

## PR regression correction — restore EOS logout and active lobby rename

The user explicitly authorized restoring these existing capabilities identified in PR #85's review, without changing the approved 3D staging view or implementing the remaining visual-polish recommendations. Jira TS-139 and TS-149–154 descriptions/comments, the approved changes, PR review, routing/standards, EOS identity/lobbies, Play Menu, session and development documentation were inspected. No further formal Astra Story critique was run: all three permitted Story rounds are already complete, and the user explicitly prohibited another.

### Restored behavior

- Personal **Settings → EOS logout (leave online session)** routes through `DevelopmentSession.ReturnToMainMenu`, existing authoritative Leave, transport teardown and coordinator membership cleanup. Only after `LeaveComplete` does the restored bootstrap bridge invoke `EosIdentityNode.Logout`. Repeated pending requests are ignored. Existing cleanup failure/retry ownership remains intact.
- Online hosts have **Lobby Settings → Rename lobby**. The dialog uses `OnlineLobbyCoordinator.Rename`, revalidates coordinator/lobby identity and current session/authority, and displays coordinator progress/results. Clients receive no rename control or authority. Rename does not replace lobby/session identity, access policy, roster, Ready state, map or configuration.
- The idle 3D lobby, vehicle angles, assets, Current Map panel, lighting, bottom controls and Main/Play flow are unchanged. Only the existing host settings modal gains the rename entry, and personal Settings gains logout. `OnlineLobbyPanel` remains removed.

### Actual verification

- `./tools/check-fast.ps1 -Area Client`: PASS. The first post-freeze attempt encountered a corrupt NuGet HTTP vulnerability-cache entry; `dotnet nuget locals http-cache --clear` repaired the disposable cache and subsequent builds passed. No Python dependency was added; source corrections used direct patches.
- `./check-online-lobby.ps1 -GodotPath <Godot 4.7.2 path recorded below> -Visual -NoBuild`: PASS. Added production UI checks for idle, owning-host and joined-client logout, duplicate requests, deferred membership callbacks, transport stop before identity callback, and return to Main Menu. Client cleanup exercises the existing bounded authoritative Leave fallback using a controlled gateway; it does not claim a remote acknowledgement. Host rename through actual dialog controls preserves snapshot/configuration identity and online ID/session/access/credential; non-host UI absence and coordinator rejection are checked. Existing deterministic Public/Locked rename and identity-service tests also ran in the full suite. Initial fixture iteration omitted the client's connected-peer projection and failed; the corrected fixture passed. Final log: `.godot/ts139-eos-restoration-online-final.log`.
- `./check-menu.ps1 -GodotPath <above> -Visual`: PASS, **1,237 assertions** after current-main integration and the caret correction. Existing navigation, Settings return, session leave and production Quit exercised. Log `.godot/ts139-eos-restoration-menu-final.log`; artifacts `.godot/menu-checks/38d8f5e831d943349935d52795460e1f/`.
- `./check-startup.ps1 -GodotPath <above> -Visual -SkipBuild`: PASS, five materialized media files and production Main/Play/Settings/browser/media flow. Log `.godot/ts139-eos-restoration-startup-final.log`.
- `./check.ps1`: PASS after current-main integration, clean Debug production and Release full-solution builds, **607 Core + 354 non-native Client/transport tests**. Log `.godot/ts139-eos-restoration-check-final.log`. Earlier pre-TS-145 runs had 597 Core tests and 1,204 menu assertions; those are not substituted for the final integrated counts.
- Directly inspected rendered `management-lobby-settings.png` and `management-logout-host.png` in `.godot/online-lobby-checks/`: the rename entry fits its expanded modal backing and logout is readable in the existing scrollable Settings layout. Additional rename/idle/client captures are produced by that fixture; physical input interaction is not claimed.
- Main advanced during the work; merged through `ffa4c6d` (TS-145), retaining newer item/network behavior. Preserved concurrent remote Story merge `1acfe4b` in `9aa2296`; that merge changes only one historical verification line relative to its tested implementation. Mechanical version/index conflicts retain all current entries. Canonical Story version **0.1.44**, export **0.1.44.0**; version checks pass. Conflict-marker scan and `git diff --check` pass. The approved staging presentation is unchanged; `JoinedLobby` only adds native text-caret handling for the new rename editor.

### Limits and delivery

Online checks use deterministic provider/gateway seams with native Godot controls; they do not authenticate EOS, connect multiple physical machines, prove service-side metadata notifications, or exercise real WAN/P2P logout. Actual identity SDK invocation is wired through the existing bootstrap/node, with service behavior covered by deterministic tests. Physical controllers, audible output, exports and sustained performance remain unverified. Existing visual-polish findings remain unchanged and outside this correction.

Independent engineering review identified interception of Left/Right in the new name editor. `JoinedLobby` now follows the existing Play Menu handling: native Left/Right/Home/End reach the focused text editor, while horizontal logical navigation does not move focus away. Native key press/release tests assert focus and caret position for all four keys. The online UI suite, comprehensive `check.ps1`, menu and startup checks above all passed after this correction. The rendered rename dialog was also directly inspected.

Correction and current-main integration are pushed through `65b0aa1` to `ts-139-jg`; independent engineering re-review is **PASS**, with no remaining material findings. PR #85 remains unmerged. Final remote CI is not claimed as verified here; the final report is a subsequent documentation-only commit.

## Round 3 — user-requested slight diagonal vehicles

The user authorized only a slight diagonal pose because the cars looked flat. `JoinedLobby` adds a mirrored 22-degree yaw offset to the existing slot-dependent angle, producing approximately 22–35 degrees from head-on. Existing positions, camera, nameplates, lighting, assets, controls and session behavior remain unchanged. This is not authorization for the previous map-panel or lighting recommendations.

- `./tools/check-fast.ps1 -Area Client`: PASS, zero warnings/errors.
- `./check.ps1`: PASS, Debug/Release builds and 594 Core + 354 non-native tests. Log `.godot/ts-139-angle-check.log`.
- `./check-lobby.ps1 -GodotPath <Godot 4.7.2 path below> -Visual -NoBuild`: PASS, eight-peer production lobby lifecycle and layout checks. Log `.godot/ts-139-angle-lobby.log`; artifacts `.godot/lobby-checks/14542b99c614467aa46f51522ef64ea2/`.
- Primary agent directly inspected `lobby-1280.png`: vehicle sides are visible with mirrored diagonal poses. Existing eight-slot layout and associated labels remain present.
- Fetched/merged current main (already up to date), Story version sync/check and `git diff --check`: PASS. Implementation committed/pushed as `0efc274` on `ts-139-jg`.
- Delivery maintenance after TS-140 merged to main: synchronized current main `e4751e7`, preserved both Stories' compatible changes, recalculated canonical Story version from main 0.1.42 to **0.1.43** and Windows file/product version to **0.1.43.0**. New-head CI is required after this merge; prior runtime evidence remains evidence for the TS-139 behavior before this mechanical main sync.
- Earlier broad verification remains historical evidence; unrelated native suites were not rerun for this yaw-only change. Prior environmental-art and real-device/network/performance limitations still apply.

### Astra Runtime Critique — Story Round 3

**Overall Score: 7.8 / 10 — Quality Assessment: FAIL (<8.0).** The authorized diagonal adjustment succeeds at both inspected sizes: fronts and sides are clearly visible in a mirrored arrangement. The integrated presentation remains below the quality gate because existing map-panel treatment and vehicle/background mismatch remain conspicuous.

| Category | Score |
| --- | --- |
| Visual Quality | 7.3 |
| UI / UX | 7.8 |
| Runtime Functionality | 8.4 |
| Runtime Integration | 8.3 |
| Runtime Stability | 8.4 |

**VERIFIED by Astra:** Direct inspection of fresh `lobby-1280.png` and `lobby-640.png` in the artifact directory above, plus reading `runtime.log` and `evidence.txt`. Diagonal orientation, eight separate labels, HOST/READY states and bottom controls are visible. **INFERRED by Astra:** Recorded successful eight-peer readiness, kick/rejoin, authority rejection, load-failure recovery, repeated match return, active admission, retained-slot rejection and host-loss cleanup; the primary agent executed these flows. Functionality/integration/stability scores rely on these records, not independent interactive operation.

Remaining recommendations (neither authorized by this angle-only request):

1. **Problem/evidence:** At both sizes the rectangular map preview appears wider than the decorative frame interior; its sky strip and dark green view look pasted across the ornament. **Severity:** Medium. **Impact:** Selected-map information looks unfinished and is visually weak. **Suggested improvement:** Fit/clip the preview inside the frame opening and improve framing/readability. **Scope:** Out-of-Scope Recommendation for the authorized angle-only correction. **Corrective Work Type:** Existing Task, subject to explicit authorization.
2. **Problem/evidence:** Faceted, simply shaded orange vehicles remain disconnected from richly lit scenery and reflective flooring. The new angle improves silhouette depth but not rendering cohesion. **Severity:** Medium. **Impact:** The showcase remains below polished presentation quality. **Suggested improvement:** Evaluate showcase lighting/material response and contact shading against the background. **Scope:** Out-of-Scope Recommendation for the authorized angle-only correction. **Corrective Work Type:** Existing Task, subject to explicit authorization.

**UNVERIFIED by this reviewer:** Direct interactive play, animation, input feel, audio, sustained frame pacing and real remote/EOS conditions. Still captures and runtime records do not establish those qualities.

**Recommended next round:** None; Story Round 3 exhausts the permitted critique rounds. Await human acceptance/decision on remaining limitations. No recommendations were implemented.

Independent engineering review: **PASS** on pushed `0efc274`, no material findings. Remote head/version/diff and recorded native/full checks confirmed. PR #85 remains unmerged; new-head CI is not claimed as verified here. This record is a subsequent documentation-only change.

## Round 2 — approved visual target and TS-149 through TS-154

Current requirements were read from Jira TS-139 and all six subtasks, including comments, plus the user-approved pasted implementation request and `ChatGPT Image Sep 23, 2026, 05_52_30 PM.png`. The current Jira scope agrees with these approved additions. The earlier Round 1 evidence below is historical and is not substituted for current verification.

### Implemented

- Fixed orthographic showcase composition for eight live presentation-only Wasteland vehicle meshes, with names, HOST and colored READY/NOT READY states. The approved reference was edited with built-in imagegen into a vehicle/UI-free environmental matte; native lighting and contact shadows accompany the actual 3D vehicles. The tent, scaffolding, carnival bulbs, wet platform, atmosphere and clown entrance are baked environmental art. No gameplay, collision, weapons, AI or match scene runs inside the lobby viewport. This is a hybrid fixed-camera scene, not fully modeled 3D scenery.
- TS-149: shared Current Map panel with native captures of Old Map/New Map; host-only Map Select through existing authority. TS-150: Core Start requires every connected non-host Ready, with no host readiness requirement; join/removal/reconnect preserve unrelated readiness.
- TS-151: migration restores survivor lobby readiness, map and existing configuration from the accepted checkpoint. Host markers and controls follow current authority; surviving vehicle roots and slots persist. Existing checkpoint cadence, agreement, retirement fences and rollback semantics remain unchanged.
- TS-152: authoritative Arena phase immediately projects Match Loader and locks conflicting actions. Recoverable initial resource/sync failure uses existing host Return. An admitted client's bounded reliable failure notification uses the existing session/epoch/generation envelope; host recovery is denied after entry release. Cancelled resource requests are drained, preventing an observed native load-token leak. Terminal session failures retain existing Leave cleanup.
- TS-153: vacant presentation slots are allocated per stable ID; survivors are not repacked, fresh reconnects do not duplicate models, and replaceable art retains the same showcase root.
- TS-154: selecting another player's nameplate exposes contextual Kick; a separate named/ID confirmation is required. Cancel does not mutate membership. Confirmation revalidates authority, session, epoch and target. Native mouse and logical keyboard/controller flows are exercised.
- Host-only Lobby Settings edits only existing match mode and kill target through Core validation. Personal Settings and Quit retain existing owners and cleanup. Approved Main Menu plates are reused; no large branding panel or persistent Kick button is present.

### Verification evidence

Environment: Windows, Godot 4.7.2 .NET, RTX 4070 Laptop GPU / OpenGL compatibility renderer. `GodotPath` below is `C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

- Fetched `origin/main`, merged current `51f8df8` into the existing Story branch; merge commit `4c5c214`. Mechanical version/export/index conflicts retained current main plus the TS-139 index entry. Re-fetch before final verification found the same main.
- `./tools/sync-story-version.ps1` and `./tools/check-version.ps1`: PASS, canonical `0.1.42`, Windows file/product `0.1.42.0`.
- Targeted `tools/check-fast.ps1 -Area Client`, `-Area Core`, `-Area Transport` were used while iterating. Early native layout checks exposed initial camera projection/non-finite bounds and overlapping labels; corrected before final verification. Direct wide-screen inspection subsequently exposed background natural-size cropping; correcting TextureRect initialization order restored the intended full backdrop and vehicle/platform alignment. Native lobby/menu reruns passed after that correction. The first updated transport run exposed four stale tests expecting migration to clear Ready; their assertions now check the approved preservation rule.
- `./check.ps1`: PASS. Debug production and Release full-solution builds, zero warnings/errors; **594 Core + 354 non-native Client/transport tests**. Log: `.godot/ts-139-round2-check.log`.
- `./check-lobby.ps1 -GodotPath <above> -Visual -NoBuild`: PASS, no warning/error markers. Current artifacts: `.godot/lobby-checks/693fc20a7e974dbfb7f8778c429b2b96/`. Eight production session UIs over real local UDP; ready toggles, host/client authority, map UI, Lobby Settings, Kick cancel/confirm, join/departure, unexpected disconnect/fresh rejoin, stable survivor slots, Quit cleanup, repeated Start/conflicting actions, injected host and client loader failures followed by authoritative recovery/retry, both actual maps, Match Loader/Sync, Podium/Return, active admission/interruption and retained capacity. The final native run also injects an authenticated current-generation Failed message after an authority freeze has cleared EntryContext and authority has unfrozen, but before readiness is reconstructed: the already-released match and generation remain intact, the packet is rejected, and normal synchronization resumes. A successful retry clears stale failure notices on every peer. Nameplate bounds/non-overlap checked at 640×360, 1280×720 and 1600×900. Earlier recovery runs completed behavioral checks but failed strict shutdown due one native load token; those are not counted as clean passes.
- `./check-menu.ps1 -GodotPath <above> -Visual`: PASS, **1,204 assertions**, no warning/error markers. Log `.godot/ts-139-round2-menu.log`; artifacts `.godot/menu-checks/e88c1773c00743b8839cff7f4fc31a44/`. Production Settings/Back reuse, overlay suppression, visual replacement, native mouse selection, keyboard confirmation opening, controller cancellation and confirmation, repeated use, arena/menu integration, leave and production Quit. Inputs are synthetic Godot native events, not a physical gamepad.
- `./check-transport.ps1 -GodotPath <above>`: PASS, **365 tests including native tests**, then three Godot transport creation/poll/message/cleanup cycles. Log `.godot/ts-139-round2-transport.log`.
- `./check-reconnect.ps1 -GodotPath <above> -NoBuild`: PASS, three arena resyncs including **125 seconds offline**, lobby fresh admission, native body reuse, prediction reset, items/spawn/match continuity and generation reset over real UDP. Log `.godot/ts-139-round2-reconnect.log`.
- `./check-migration.ps1 -GodotPath <above> -Players 2 -NoBuild` and `-Players 3`: PASS. Ready/map/configuration preserved at the accepted checkpoint, surviving showcase roots/slots retained, host controls transferred, former host rejoins as client; personal Settings/Quit remain enabled during an explicit lobby authority freeze; existing active-match/sequential migration assertions also pass. Logs `.godot/ts-139-round2-migration2.log` and `migration3.log`. An initial iteration requested departure before the new state reached the existing checkpoint cadence; the fixture now explicitly waits for the accepted boundary and does not change migration policy.
- `./check-online-lobby.ps1 -GodotPath <above> -NoBuild`: PASS, fake-provider frontend/admission/retained-session idempotency, not authenticated EOS. Log `.godot/ts-139-round2-online.log`.
- `./check-startup.ps1 -GodotPath <above> -Visual -SkipBuild`: PASS, frontend media hashes plus production startup/navigation/Settings/browser/media lifecycle, no native diagnostics. Log `.godot/ts-139-round2-startup.log`.
- `git diff --check`, affected current-document link checks, and the three-file lobby asset SHA-256 manifest check: PASS.
- Native previews were rendered from the production map scenes with the retained `assets/frontend/lobby/render-map-previews.gd`; imagegen provenance, prompt summary and hashes are in that directory's manifest. No external asset or font download was introduced.

### Assumptions, limitations and explicitly unverified areas

The existing Wasteland model is used for every participant because no authoritative vehicle-selection field exists. Only its existing identification material varies cosmetically. The reference's different detailed vehicle models are not supplied as production assets. Scenery is a baked environmental matte; no dynamic fog, flame or light animation is claimed. The UI and live 3D vehicles remain independently replaceable.

Local UDP and trusted migration identity/retirement seams do not prove real authenticated EOS, WAN, multi-machine or physical controller behavior. Exported executable packaging, audible output, sustained performance/low-end GPU profiling, exhaustive Unicode names and aspect ratios beyond the exercised 16:9 sizes and observed 2560×1080 solo/confirmation letterboxing remain unverified. Migration retains the existing accepted-checkpoint rollback window; state changes newer than that boundary may roll back as before. Resource-load failure is injected at the production loader factory, not by corrupting shipped assets. The existing deterministic initial-sync timeout test covers that timeout; no native 30-second missing-ack timeout has been separately injected in this round.

Final verification is complete. Independent engineering review initially identified a P2 recovery fence defect: transient EntryReady could allow an already-released match to be aborted immediately after unfreeze. Recovery now checks the existing monotonic InitialEntryReleased flag, local recovery does not handle established-match resync failures, and the exact native regression above passes. Independent engineering re-review passed pushed correction `022d92d`. A final navigation-preservation correction keeps personal Settings/Quit usable during a lobby migration freeze while mutating controls remain locked; comprehensive checks and the native migration regression pass. Independent engineering review is **PASS** on pushed head `fe83921`, with no material findings; version, ancestry, clean working tree and remote-head match were confirmed. Formal Round 2 critique is complete below.

### Astra Runtime Critique — Story Round 2

**Overall Score: 7.7 / 10 — Quality Assessment: FAIL (<8.0).** This is a holistic score, not an arithmetic average. The integrated lobby operates reliably: readiness guidance is accurate, confirmation identifies its target, and composition follows the approved circus direction. The Current Map panel and vehicle/backdrop integration remain below the highly polished quality bar.

| Category | Score |
| --- | --- |
| Runtime Functionality | 8.7 |
| Controls / Responsiveness | 8.3 |
| Multiplayer / Networking Experience | 8.5 |
| Camera / Composition | 8.0 |
| Visual Quality | 6.8 |
| UI / UX | 7.6 |
| Runtime Stability | 8.7 |
| Runtime Integration | 8.6 |

#### What was exercised

- **VERIFIED:** Astra independently reran `check-lobby.ps1 -Visual -NoBuild` on `fe83921`, with a clean pass. Eight local UDP peers exercised readiness, unauthorized Start, kick/rejoin, disconnect/rejoin, stable presentation, repeated entry/return, host/client resource-failure recovery and rejection of late failure packets after initial release. Fresh artifacts: `.godot/lobby-checks/51723522d9dd4f8ca4672f1e43299a19/`.
- **VERIFIED:** Astra independently reran `check-menu.ps1 -Visual`, passing 1,204 assertions, including synthetic native mouse/keyboard/controller confirmation and cancellation, Settings return, repeated navigation and Quit. Fresh artifacts: `.godot/menu-checks/87cb6f0bd4ff44539c3ccb967da84c34/`.
- **VERIFIED:** Astra inspected fresh 640×360, 1280×720 and 1600×900 lobby captures, fresh confirmation, and supplied 2560×1080 production captures against the approved reference. Names/states are distinguishable, controls are organized correctly, and confirmation has clear focus/hierarchy.
- **INFERRED:** Host migration and prolonged reconnect conclusions use the primary agent's native evidence above; Astra did not independently rerun those suites.

#### Recommendations requiring human authorization

1. **Problem:** Current Map needs a deliberate interior layout. **Evidence:** In each inspected size, CURRENT MAP crosses bright upper frame decoration, the thumbnail extends across the frame interior, and the map name competes with the lower decorative rail. The dark, distant preview gives the track little prominence. **Severity:** Medium. **Impact:** Required primary information looks unfinished and is harder to read compactly. **Suggested improvement:** Reserve opaque padded title/name bands, inset the preview inside the frame, and recapture existing maps with tighter framing and clearer exposure. **Scope:** In Scope. **Corrective Work Type:** Existing Task (TS-149).
2. **Problem:** Live vehicles and the environment matte lack visual cohesion. **Evidence:** Fresh eight-player captures show relatively flat orange/brown vehicles against a detailed, luminous, reflective stage. Layout follows the reference, but lighting/material contrast makes vehicles appear as a separate layer. **Severity:** Medium. **Impact:** The backdrop dominates the vehicles, which should be the focal content. **Suggested improvement:** Tune showcase illumination, contrast and contact grounding alongside matte exposure/saturation to share lighting direction and tonal range. Retain existing models; new detailed assets are not required. **Scope:** In Scope. **Corrective Work Type:** Existing Task (TS-139 presentation).

**Recommended next round, only if authorized:** Address the map panel first, then vehicle/environment cohesion. Inspect both map previews, solo/eight-player composition, compact/wide sizes, and rerun affected UI checks.

**UNVERIFIED:** Physical controller ergonomics, human-paced multiplayer, authenticated EOS/WAN/multiple machines, audible output, exports, and sustained performance/low-end GPU behavior. Synthetic input and screenshot FPS do not establish physical ergonomics or sustained performance. Driving, physics and audio were not scored.

No recommendations were implemented after this formal critique and no corrective Jira work was created. Human acceptance or authorization for another round is pending under `docs/critique.md`.

### Current delivery

Implementation and corrections are committed and pushed on `ts-139-jg` through `fe83921365023cb736849b72d362369444dd75ca`. Independent engineering review is **PASS** on that head. GitHub CI [run 35929848832](https://github.com/JonathanGenier/Trackstorm/actions/runs/35929848832), including its [verify job](https://github.com/JonathanGenier/Trackstorm/actions/runs/35929848832/job/107413541261), completed **SUCCESS** for that implementation head. PR [#85](https://github.com/JonathanGenier/Trackstorm/pull/85) remains open and unmerged. This final critique/delivery record is a subsequent documentation-only change; the runtime quality gate remains **FAIL**, independently of engineering and CI results.

---

## Historical Round 1 evidence (superseded implementation)

#### Original implementation record

### Implemented

- Dedicated fixed-camera 3D joined Lobby, eight static visual-only vehicle showcases, connected-authority identity/name/HOST/READY projection, no fake empty-slot players.
- Host Start/Settings/Quit/Kick and non-host Ready/Not Ready/Settings/Quit. Existing host map control retained. Start consents through existing Ready authority, then requests the unchanged Start/Loader/Sync transition.
- Host-only lobby Kick uses existing Core removal, normal roster publications and reliable rejection/delayed transport close. No new membership, ban, admission or vehicle-selection authority.
- Replaceable visual factory and per-player model refresh; current authoritative lobby has no selection field, so existing Wasteland art is the fallback.
- Approved Main Menu crest/plate textures, existing vehicle/arena assets and original stage geometry. No new external assets or gameplay systems.

### Verified commands and results

Environment: Windows, Godot 4.7.2 .NET, NVIDIA RTX 4070 Laptop GPU / OpenGL compatibility renderer for visual runs. Godot executable: `C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

- `git fetch origin main`, `git merge origin/main`, `./tools/sync-story-version.ps1`: already synchronized; canonical version 0.1.34, Windows preset 0.1.34.0. Jira's target release does not authorize a release-version bump.
- `./tools/check-fast.ps1 -Area Client`: repeated focused Debug builds passed with zero warnings/errors. Explicit area was used while changes were uncommitted because automatic routing compares committed branch history.
- `./check.ps1`: PASS. Debug production and Release full-solution builds with warnings as errors; 540 Core tests and 353 non-native Client/transport tests passed. Log: `.godot/ts-139-check.log`.
- `./check-lobby.ps1 -GodotPath <above> -Visual -NoBuild`: PASS on final implementation. Artifacts: `.godot/lobby-checks/07e155e5c1684ef9b16a3ccc7dba2d7d/`. Eight real local UDP sessions and native Godot worlds. Host/client permissions, self-kick rejection, selection/Kick removal on all remaining peers, kicked-client cleanup, fresh rejoin, unexpected lobby disconnect/rejoin, Quit/Main Menu, ready/unready, unauthorized client Start, repeated host Start, both maps, Match Loader/Sync, Podium/Return, fresh active admission, interrupted admission and retained-slot capacity exercised. Nameplate viewport bounds/non-overlap asserted at 640x360, 1280x720 and 1600x900.
- `./check-menu.ps1 -GodotPath <above> -Visual`: PASS, 1,038 assertions. Final artifacts: `.godot/menu-checks/9298526850ca437092ba5274dd42ec33/`; log `.godot/ts-139-menu.log`. Added two production lobby Settings/Back cycles, suppression of underlying buttons, retained showcase identity, visual replacement retaining exactly one model, and solo host Start without Ready UI. Existing gameplay navigation, input suppression, leave and application Quit also passed.

- `./check-reconnect.ps1 -GodotPath <above> -NoBuild`: PASS. Three arena resynchronizations including 125 seconds offline, lobby fresh admission, checkpoint/native body reuse, held items, match continuity and new-generation reset; local UDP and trusted test identity. Log: `.godot/ts-139-reconnect.log`.
- `./check-online-lobby.ps1 -GodotPath <above> -NoBuild`: PASS. Fake-provider admission, retained validation/reconnect/release idempotency. Log: `.godot/ts-139-online.log`.
- `./check-transport.ps1 -GodotPath <above>`: PASS. 364 Debug transport tests including native GNS and three Godot node/message/cleanup cycles. Log: `.godot/ts-139-transport.log`.

- `./check-startup.ps1 -GodotPath <above> -SkipBuild`: PASS after updating the legacy duplicate-primary-control assertion to exclude the dedicated joined Lobby controls. Recovery, navigation, Settings/browser return and persistent MenuShell media verified; headless, not audible validation. Log: `.godot/ts-139-startup.log`.
- `git diff --check`: PASS. Complete current Story diff inspected; no Shared layer, gameplay dependency change, new downloaded assets or tracked runtime output.

### Runtime evidence and iteration

The initial rendered composition exposed overlapping nameplates and excessive lighting; both were corrected and subsequent captures inspected. Early harness runs failed because they still expected host Ready or rejoined before the existing exit-completion tick; fixture sequencing was corrected and complete subsequent runs passed. The first startup regression rejected the legitimate lobby Settings button as a duplicate Main Menu target; the scoped fixture was corrected and rerun successfully. Initial sandboxed .NET builds exited without diagnostics; the same checks passed with installed dependency access. These failures are not recorded as passes.

Rendered captures were directly inspected at normal and compact sizes. They show eight separate vehicles and readable associated names/state, reused industrial plates/clown crest, red/cream backdrop, fixed camera and minimal controls. Automated input includes emitted UI signals and existing native input fixtures; it is not a physical multiplayer playtest.

### Assumptions, limitations and explicitly unverified areas

- Current roster contains no selected-vehicle identity; all players use the existing approved vehicle mesh. Future selection work must supply its authoritative asset projection to the visual factory/refresh boundary.
- Kick removes the current session binding; it is not a permanent ban. Fresh admission remains governed by existing systems.
- Authenticated multi-device EOS, remote residential networks, exported executable, physical controller ergonomics, audible output and sustained profiling are UNVERIFIED in this Story. Local UDP/fake-provider evidence does not establish those outcomes.
- No driving, physics, combat or vehicle-selection UI is introduced in Lobby. Static scene assets remain reusable while offscreen rendering stops and player showcases are released outside Lobby.
- Long names use truncation and a full-name tooltip; exhaustive Unicode/name-length combinations and all non-16:9 aspect ratios have not been exercised.

### Reviews

Independent engineering review: PASS, no material correctness/scope/authority/lifecycle/test-design issue found. Astra runtime critique follows. Human acceptance is pending; recommendations are not implemented without a new instruction.

### Astra Runtime Critique — Story Round 1

**Overall Score: 7.8 / 10 — Quality Assessment: FAIL (<8.0).** The overall score is holistic, not an arithmetic average; accurate next-action guidance is central to the lobby.

| Category | Score |
| --- | --- |
| Runtime Functionality | 8.6 |
| Multiplayer / Networking Experience | 8.5 |
| Camera | 8.0 |
| Visual Quality | 8.0 |
| UI / UX | 7.0 |
| Runtime Stability | 8.5 |
| Runtime Integration | 8.5 |

The independent Astra reviewer directly inspected the eight-player 640x360, 1280x720 and 1600x900 captures and the production solo-host capture, then independently reran `check-lobby.ps1 -Visual -NoBuild`. The elevated run passed; artifacts: `.godot/lobby-checks/6453e0dd9b5647e397a1b745f2be0ed9/`. This verified eight-peer ready/unready, Start rejection/acceptance, Kick, cleanup/rejoin, unexpected disconnect, Quit, repeated matches/Return and admission boundary cases. Broader Settings/reconnect/transport conclusions were inferred by the reviewer from the separately verified parent evidence, not independently rerun by Astra.

The first independent sandboxed run completed gameplay assertions but failed the wrapper due denied user-log writes and certificate-store access. It is not a clean pass; the subsequent elevated run passed.

#### Recommendation and unresolved runtime risk

- **Problem/evidence:** Host footer says “Waiting for everyone to ready up” while seven clients visibly show READY and Start is available. The solo-host capture has the same instruction despite there being no Ready action for the host. The harness successfully starts both conditions.
- **Severity:** Medium. **Impact:** Misleading primary transition guidance can make the host believe another readiness action is required.
- **Suggested improvement:** Show “Ready to start” when all non-hosts are ready (including solo host); otherwise show the unready-player count. Keep Kick guidance secondary.
- **Scope:** In Scope. **Corrective Work Type:** Existing Task.
- **Recommended next round, if authorized:** Correct the guidance, inspect rendered solo/partially-ready/all-ready states, and recheck Start plus ready toggles.

The scene was judged coherent, readable and operationally robust in local multiplayer. Physical controllers, real multi-device EOS, remote networks, audible output, exports, sustained profiling, exhaustive names and non-16:9 lobby layouts remain unverified. No recommendation was implemented after this formal critique. The requested branch delivery is a reviewable handoff, not runtime acceptance or permission to merge.

### Delivery

Implementation committed and pushed on `ts-139-jg` as `0faa4d3`; PR [#85](https://github.com/JonathanGenier/Trackstorm/pull/85) is open, not merged. Post-push independent engineering review: **PASS**, current fetched main ancestry/version checks passed and working tree was clean. GitHub CI run [35850582987](https://github.com/JonathanGenier/Trackstorm/actions/runs/35850582987) reported `action_required` with no jobs started; remote CI is **UNVERIFIED**, despite successful local verification. This report's delivery note is a subsequent documentation-only commit.
