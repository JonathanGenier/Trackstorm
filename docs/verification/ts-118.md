# TS-118 verification

Date: 2026-09-23. Branch: `ts-118-jg`. Story version: `0.1.33`, derived from current main `0.1.32`; Windows preset `0.1.33.0`.

## Scope and implemented behavior

Read TS-118 in full (no children/comments), repository routing, relevant feature contracts and current item/vehicle/scoring/recovery implementation. The user explicitly selected **score throughout active Nitro duration**. Jira's description now persists that approved condition; movement/throttle are not scoring gates.

- Nitro uses the TS-116 registry, ordinary slot/token, handler and atomic item/vehicle transaction. Consumed grants cannot replay. A second held Nitro remains held while boosted; no stacking or refreshing.
- Defaults: five seconds, 2× forward engine demand, 1.4× forward drive cap. Existing traction/support/absolute physics limits apply. Configurable duration/multipliers are captured at activation; canonical vehicle tuning is unchanged.
- Movement state carries remaining ticks/multipliers through prediction, publication, resume and replacement authority. Expiry, death, new-life reset, Finished and fresh match creation clear the effect.
- Active Circus ticks bank configurable duration points (default 10 base/second) through the current-K/D pipeline. FirstToTarget awards no Circus points. Restoration never replays historical awards.
- Registry-driven activation feedback, reconstructed blue flame trail, local duration label, Stats duration/multipliers and bounded use/end events. Existing assets only.
- Movement codec v5/119 bytes, match codec v6, configuration codec v7/83 options. Feature documentation and verification routing updated.

## Executed verification

Native runs used Windows, Godot .NET 4.7.2 (`ed1daf0bf`), its installed console executable under `C:/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64/`. Rendered runs used OpenGL Compatibility on an RTX 4070 Ti. All results below are observed passes.

| Command / check | Result |
| --- | --- |
| Fetch/merge origin/main; sync-story-version.ps1; check-version.ps1 | Synchronized; canonical/preset checks passed |
| Targeted Core test filter for Nitro and VehicleStateCodec | 14 passed |
| ./check.ps1 | 108 version checks; workflow/media checks; restore; Debug production and Release solution warning-as-error builds; 540 Core and 345 non-native transport tests |
| Debug/Release Client builds after feedback refinement | Zero warnings/errors |
| tools/test-workflow-tools.ps1 after Nitro route coverage | Passed |
| check-nitro.ps1 -NoBuild -Visual -Impaired | Two native UDP peers, 30 ms delay / 5 ms jitter / 2% loss, four uses per vehicle, duplicate remote sends |
| check-reconnect.ps1 -NoBuild | Three reconnects including 125 seconds offline; exact expired and active Nitro restoration |
| check-migration.ps1 -NoBuild -Players 2 and -Players 3 | Both passed; three-player case retained seeded active Nitro and exact Circus checkpoint totals |
| check-migration-processes.ps1 -NoBuild -Players 2 and -Players 3 | Actual host-process termination; independent survivors resumed epoch 2 |
| check-network-vehicles.ps1 -NoBuild -Players 8 -Latency 30 -Jitter 5 -Loss 2 | Eight processes on oval; zero reported large corrections |
| check-transport.ps1 | All 356 tests including native tests; three Godot transport lifecycle cycles |
| check-items.ps1; check-oil.ps1; check-item-spawns.ps1 (all -NoBuild) | Native inventory/missile/repair/Oil and all four pickup identities passed |
| check-match.ps1; check-stunts.ps1 (both -NoBuild -Impaired) | Eight-peer combat totals/frozen result; native stunt banking/lethal cancellation |
| check-death-respawn.ps1 -NoBuild | Four eight-peer native death/respawn cycles |
| check-vehicle.ps1 | 144 assertions at each render rate; movement/surface replay matched at 30 and 144 FPS |
| check-hud.ps1 -NoBuild | Nine resolutions, state changes, units and teardown |
| check-developer-options.ps1 | 529 write assertions and eight persistence-read assertions in a second process |
| check-statistics.ps1; check-event-log.ps1 (both -NoBuild) | 186 Stats assertions; replicated Event Log/activity-feed checks |
| check-startup.ps1 -SkipBuild -SkipMediaCheck | Main startup/navigation/input-gating checks passed |
| Complete diff, scope, dependency direction and git diff --check | Reviewed; Core remains engine-independent; no Shared layer or new assets/dependencies |

The comprehensive gate preceded the final presentation refinement; affected Client builds and rendered/native checks reran afterward. Initial obsolete fixture assumptions about reserved Nitro, protocol sizes, score history and option count were corrected. The first sandboxed native run exercised Nitro but failed on logging/certificate access; elevated reruns passed. Migration exposed a procedural particle-resource cleanup warning; explicit ownership cleanup fixed it and the rerun was clean. An unsupported -NoBuild argument to developer-options was corrected before its successful run.

## Runtime evidence

**VERIFIED:** Normal native speed settled at 44.43 m/s; boosted speed reached 61.96 m/s (about 223 km/h). Both owners consumed slots, completed four boosts, and received replicated duration points. Eight effect-end events followed eight uses. The final local displayed Circus total was 80 for eight boosted seconds at x1; pending Top Speed remained separate.

**VERIFIED:** Inspected rendered `.godot/nitro-checks/active-nitro.png` and `expired-nitro.png`. Blue active trail and duration above speed are visible without obscuring HP, inventory, speed or Circus totals. Expiry removes trail/countdown. The platform and observation camera are test fixtures.

**VERIFIED:** Long-offline resume restored zero Nitro duration. Two short reconnects restored 3599 ticks each, exactly matching their installed checkpoint before prediction. Three-peer authority replacement continued a timer below its seeded 3600 ticks with the same multipliers and score totals.

**VERIFIED:** Deterministic tests cover fractional current K/D/live rate, stationary neutral scoring, repeated duration, failed atomic batches, stale grants, prediction, codec rejection, reset/death/Finished, FirstToTarget exclusion and continued authority after restore. Nitro-specific reset/terminal-match scoring is Core evidence; native lifecycle regressions cover surrounding production paths.

Recovery fixtures seed committed boosts to isolate continuation. Ordinary activation is separately exercised through actual host/client requests. Reconnect uses zero Nitro score rate to preserve its existing fixed-rank assertions; three-player migration uses nonzero duration scoring. Process-kill checks cover general recovery, not Nitro-specific activation.

Raw evidence remains local and ignored under `.godot/`: `ts-118-check.log`, `nitro-checks/`, `reconnect-checks/`, `migration-checks/`, `migration-process-checks/` and per-harness directories. No generated binaries are committed.

## Astra Runtime Critique — Story Round 1

**Overall Score: 8.1 / 10 — PASS (>=8.0).**

| Observed category | Score |
| --- | --- |
| Runtime functionality/repeated use | 8.7 |
| Multiplayer/recovery integration | 8.5 |
| Movement response in flat driving | 8.0 |
| Visual feedback/HUD clarity | 8.0 |
| Runtime stability after correction | 8.5 |

Substantial bounded speed increase, consistent inventory/scoring and readable active/expired feedback were directly exercised. Local impaired networking and recovery were coherent. Missing independent-device Internet and human-feel evidence limit the overall assessment. This is a runtime assessment, not a substitute for independent engineering review. Human critique acceptance/manual merge remain pending.

## Limitations, risks and explicitly unverified

- **UNVERIFIED:** Authenticated multi-PC EOS/Internet, NAT changes, live authority-lease outages, exported-build recovery and long network soak.
- **UNVERIFIED:** Human balance/fun, physical-controller ergonomics, audible quality/listening, sustained boosted cornering/collisions across arbitrary terrain, and a dedicated Nitro-active reset/end native scenario.
- **INFERRED:** Existing authenticated sender/epoch/generation fencing applies through unchanged envelopes. Local UDP/fake-provider tests do not prove production EOS connectivity.
- Expiry removes boost demand/cap while preserving momentum. Holding throttle above the normal cap does not force braking; this follows the existing external-velocity policy.
- Defaults are initial balance values. Traction/safety bounds can reduce nominal multipliers under nondefault tuning or surfaces.
- Normal eight-car snapshots grow by 80 bytes to 1437 bytes. Active Nitro retains existing reliable score publication frequency; no Internet bandwidth optimization is claimed.
- Migration retains normal selected-checkpoint rollback; post-checkpoint outcomes are not guaranteed lossless.
- No outstanding defect was observed in completed scenarios. Independent PR engineering review, remote CI and human acceptance remain separate gates.

## Delivery review synchronization

PR review synchronized the Story branch with current `main` at `4009a5e6` (TS-138) and recalculated the normal Story version to `0.1.33` / `0.1.33.0`. This was mechanical integration/version maintenance only. The verification results above predate that final main synchronization; no post-sync local/runtime command result is claimed here. Fresh CI and applicable post-sync verification remain delivery gates.
