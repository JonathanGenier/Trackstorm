# TS-76 — Infield structures and gameplay geometry
> The approved 2026-09-23 follow-up supersedes Round 1's interpretation that the
> deck was unreachable. Round 1 below is retained as historical evidence; current
> follow-up results are recorded in the final section.

## Scope and inspection

Read Jira TS-76 (no children or comments), TS-74 including its concept attachment
10017 viewed in Jira, TS-75 and its approved terrain-only/acceptance decisions,
repository workflow/standards/critique, map/terrain authoring notes, vehicle
collision contracts and the current implementation before editing.
Fetched origin/main at 1a02028; the existing ts-76-pg branch matched it.
This includes TS-141 damage handling and TS-142 jump retuning. Story version was
calculated as 0.1.38 (0.1.38.0 Windows metadata).

The existing whitespace change in code/Core/Input/InputButtons.cs and the
assets/maps/oval/grass_variation.png.import setting are preserved and excluded.

## Implemented

- Original Blender production set with 62 editable structural pieces, retained
  modifiers/source, GLB export and checksummed authorship manifest.
- Central four-pier concrete structure, buried foundations, capitals, continuous
  soffit, segmented deck, edge beams/parapets, tapered retaining returns and
  open drainage channels. Both existing ground-level crossings remain open.
- Separate Godot imported static collision. The terrain import hook removes
  inherited graybox tunnel nodes/colliders; the original terrain blend, GLB,
  topology and all jump geometry remain unchanged.
- Scene and scene-baker integration, metre-scale identity transforms, obstacle
  crash classification, neutral shading only, no CSG or runtime generation.
- Native fixture coverage for clearance, joins, all structural collision kinds,
  repeated vehicle crossings and repeated deliberate pier impacts.

## Iteration evidence

Blender 5.2.2 LTS authored/exported the set. Godot 4.7.2 .NET imported it.
Windows visual checks used the GTX 1070 Compatibility renderer.
The first Structure run passed 2,598 support probes, 102 clearance rays,
twelve crossings on both axes (three passes in each direction), and four native
15 m/s pier impacts. Traversals retained 1000/1000 HP and zero unsupported frames;
impacts stopped at vehicle-origin Z approximately -13.47 m and registered damage.

Runtime inspection led to neutral shading correction (initial export appeared
overbright). An isolated impact run exposed a fixture issue: the first sample
observed the old pose before Core's queued reset applied. The fixture now waits
three native frames and asserts the setup pose before recording the impact.
The corrected isolated visual impact run passed. No vehicle behavior was changed.
A subsequent targeted check also passed retaining-wing, drain-invert and deck
barrier collision probes with all common geometry checks and native impacts.

Independent engineering review caught an export-fidelity defect: Blender's
default GLB export omitted the retained bevel modifiers. `export_apply=True`
corrected it; the regenerated GLB is 380,256 bytes. Independent reinspection
verified chamfered geometry in all 62 meshes, identity transforms and matching
manifest hashes. The [engineering verdict](ts-76/engineering-review.md) is PASS
after correction. Asset-dependent runtime checks are repeated on that export.

Initial sandbox import/log access errors and a cached NuGet vulnerability-feed
error were resolved using authorized normal-access import and a forced NuGet
restore; subsequent Debug build passed with zero warnings/errors.

## Scope interpretation and limitations

The approved terrain has no reachable elevated crossing and preserves both
ground-level axes. Adding deck approaches would change that topology and jump
recovery space, so the structural deck is not claimed as an elevated route.
Existing jumps remain terrain; there are no replacement manufactured ramps.
The obstacle reservations are unchanged. Material identity, water gameplay and
environment dressing remain outside TS-76.

The existing uphill-start limitation is deferred outside terrain/structure scope.
It must be reported if encountered by the comprehensive infield suite.
Automated native driving establishes physical traversal, not human control feel.
Human keyboard/controller playtesting, dense combat, exported builds, remote
devices, authenticated EOS, audio listening, all off-route impacts and ordinary
chase-camera occlusion throughout the tunnel have not been verified here.

## Comprehensive verification

Commands use the Godot console at
`C:/Users/orsin/OneDrive/Desktop/godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

| Executed check | Actual result |
| --- | --- |
| `tools/check-fast.ps1` | PASS for routed deterministic/version/build checks; runtime checks explicitly pending in this command and executed separately. [Log](ts-76/check-fast.log). |
| `check.ps1` | PASS: zero-warning/error Debug production and Release solution builds, 556 Core and 352 non-native transport tests, workflow/version/media regression and materialization checks. [Log](ts-76/check.log). |
| Blender source reopen audit | PASS: 62 individually editable metre-scale identity meshes, each retaining its bevel modifier. [Log](ts-76/source-audit.log). |
| Godot editor import | PASS after corrected export; no reported errors/warnings. [Log](ts-76/import.log). |
| Main scene `--headless --path . --quit-after 120` | PASS, exit 0, clean startup. [Log](ts-76/startup.log). |
| Current-main synchronization and `tools/check-version.ps1` | PASS: main remains 1a02028, expected canonical 0.1.38 and Windows 0.1.38.0. |
| Source manifests and retained terrain diff | Eight recorded file hashes matched; original terrain blend/GLB, terrain manifest and layout have no Story diff. |
| `check-infield.ps1 -NoBuild -Visual` | **FAIL** only when reaching the known `BasinRecovery-85-28` uphill start. Before that, all 2,598 support probes, structural checks, twelve crossing passes, four impacts, ten original routes, six jump runs and both northern basin crossings passed on the corrected export. [Log](ts-76/infield-final.log), [measurements](ts-76/infield-final.txt). |
| Infield `-Case ConnectedLoopTour` | PASS: uninterrupted loop-to-loop traversal. [Log](ts-76/ConnectedLoopTour.log), [measurements](ts-76/ConnectedLoopTour.txt). |
| Infield `-Case TwoCarTunnel` | PASS: both production cars traverse the tunnel together without damage. [Log](ts-76/TwoCarTunnel.log), [measurements](ts-76/TwoCarTunnel.txt). |
| Infield `-Case BasinRecovery85` | PASS: remaining southeastern basin recovery. [Log](ts-76/BasinRecovery85.log). |
| Infield `-Case TerrainToBank`, then `-Case TerrainToBank1` | **FAIL**: both stationary uphill starts reproduce the existing vehicle limitation. [West log](ts-76/TerrainToBank.log), [east log](ts-76/TerrainToBank1.log). No terrain or vehicle workaround was added. |
| `check-oval.ps1 -NoBuild` | PASS: 3,087 recorded checks, including road rays, terrain rim support, native laps and handling, both adapters' transitions, containment, pickups and practice/reset. [Log](ts-76/oval-final.log), [measurements](ts-76/oval-evidence.txt). |
| `check-network-vehicles.ps1 -NoBuild -Players 2` | PASS: separate-process host/client driving and replication on the production map. [Log](ts-76/network-final.log). This is not remote-device or tunnel-specific network coverage. |

All asset-dependent results above are from the corrected chamfered export.
Final visual observations include [tunnel](ts-76/tunnel.png),
[ground-level clearance](ts-76/tunnel-clearance.png),
[structure/terrain join](ts-76/structure-join.png),
[overview](ts-76/overview.png) and
[vehicle traversal](ts-76/StructureNorthSouth0_1-drive.png).
Structural passes had zero unsupported frames and 1000/1000 HP. Repeated pier
impacts stopped outside the solid at approximately Z=-13.47 m and registered
about 15.6 HP crash damage. Collider classification stayed separate from terrain
landing forgiveness. The known basin stall occurred near (-105.342, 0.627,
28.044) with four compressed wheels, matching the existing supported-start
failure rather than an unsupported seam.

No performance benchmark is claimed. The local network smoke and fixed-step
geometry runs were not an isolated performance measurement. No new structural
runtime defect remains identified by these exercised scenarios; off-route and
human-control limitations above remain explicit.

## Astra runtime critique and delivery

After comprehensive verification, independent Astra actively repeated the
structural and two-car native visual scenarios and inspected the generated
views. [Astra Runtime Critique — Story Round 1](ts-76/astra-critique.md) scored
**8.2/10 — PASS** under the exact 8.0 runtime threshold. Its retained logs,
measurements and images are linked in that report. No production change followed
the critique, and no corrective round was started.

The elevated fixture camera can be obscured by the deck during its approach
capture. This is not evidence of a confirmed production chase-camera defect or
a production-camera pass; ordinary chase-camera occlusion remains unverified.
The original sandbox-only critique run logged environment access errors; the
normal-access rerun passed cleanly and is the authoritative critique run.

Implementation commits `291be2e` and `4d04804` are pushed on `ts-76-pg` to the
single [PR #90](https://github.com/JonathanGenier/Trackstorm/pull/90).
The requested evidence and review reports are delivered on that same branch.
The last main fetch still resolved to `1a02028`; final version/ancestry and diff
checks passed. Local results do not claim GitHub CI completion.

Human runtime acceptance remains pending under the
[mandatory critique gate](../critique.md#mandatory-human-gate).
The user's explicit request authorizes committing/pushing this implementation
and evidence; it does not authorize acceptance, another critique round or merge.
No merge was performed.

## Approved follow-up — Round 2 (2026-09-23)

Re-read TS-76, approved chat corrections, PR 90, current main, repository routing,
map/vehicle documentation and the existing TS-75/TS-76 assets before editing.
Jira now records the approved elevated crossing, open jump-facing ends, retained
kickers/deck height and mirrored dirt side approaches. No children or comments
were added to the Story. The same `ts-76-pg` branch and PR 90 are used.

### Corrected geometry

- Retained the concrete tunnel framework and 6.35 m deck top; removed the ten
  X-end parapet segments facing the jumps. The ten lateral barrier segments remain.
- Retained 53 editable Blender pieces, with 44 imported collision bodies. A
  continuous authored deck collision slab spans the visible panel joints.
  Deck/beam upward contacts use the existing landing classification; piers,
  retaining pieces and barriers remain obstacles. No Core rules or CSG added.
- Replaced isolated dirt recovery zones with full-width, 22.5 m tabletops at the
  deck height. Flat approaches run from |X|=10.75 to 70 m. Rounded side slopes
  share a mirrored hill/approach profile; compact smooth union avoids a cut or
  sharp intersection with the pre-existing southern hills.
- Existing kicker faces at |X|≥83 m, the central north/south underpass, terrain
  outside |Z|=28.25 m, the original rim, basins and topology coordinates remain.
  The east/west connection intentionally now crosses above the tunnel.
- Main side profiles match within the completed approaches; the outer five-metre
  collar and post-kicker longitudinal blend tie into asymmetric existing terrain.
  Symmetry is not claimed for those preserved-terrain tie-ins. The independent
  [source audit](ts-76/round-2/engineering-review.md) quantifies the difference.

### Follow-up iteration evidence

`tools/check-fast.ps1 -Area Client` passed with zero build warnings/errors.
The first route-support probe exposed a steep post-kicker shoulder; a longer
longitudinal blend corrected it without moving the takeoff face. Six native deck
crossings then passed at center and ±7 m offsets, in both directions, with
1000/1000 HP and zero unsupported frames.

Native side-climb diagnostics exposed damaging contacts at the terrain toe.
A hard preservation clamp also broke side symmetry. The final shared mirrored
shoulder and smooth union corrected these issues: all four side climbs passed
at 8 m/s with full health. The fixture starts on existing flat ground and drives
continuously through a turn onto each slope. An earlier stationary start on an
unchanged southern hill reproduced the known vehicle uphill-start limitation;
no Core tuning or mid-run teleport workaround was introduced.

Automated native input is actual production vehicle/physics traversal, not human
keyboard/controller playtesting. The existing deferred terrain uphill-start
limitation remains outside the approved structure corrections.

The first integrated follow-up run also exposed a new stationary-start regression
at the northwest basin approach (-77,-29): a wheel reached the outer toe even
though the vehicle origin was outside it. This was not accepted as an old failure.
The toe now tapers inward only through |X|=70..83 m, leaving the full tabletop and
main climbs unchanged. The original basin setup then passed with 1000/1000 HP
and zero unsupported frames; see [correction evidence](ts-76/round-2/basin-toe-correction.txt).
The earlier [integrated measurements](ts-76/round-2/infield-before-toe.txt) and
[log](ts-76/round-2/infield-before-toe.log) document the issue before correction.

### Final follow-up native results

| Executed check | Actual result |
| --- | --- |
| `check-infield.ps1 -NoBuild` on final tapered-toe export | **FAIL only at the previously recorded `BasinRecovery-85-28` stationary uphill start.** Before that: 2,598 route-support probes, structural/tabletop geometry checks, twelve repeated crossings, four pier impacts, two full slow approaches, six offset deck crossings, four side climbs, ten original routes, six jump runs and both northern basin routes passed. [Log](ts-76/round-2/infield-final.log), [measurements](ts-76/round-2/infield-final.txt). |
| `-Case BasinRecovery85` | PASS for remaining southeastern basin. [Log](ts-76/round-2/BasinRecovery85.log). |
| `-Case TerrainToBank-1` and `-Case TerrainToBank1` | **FAIL**: both reproduce the already documented stationary uphill-start limitation outside changed geometry. [West](ts-76/round-2/TerrainToBank-1.log), [east](ts-76/round-2/TerrainToBank1.log). |
| `-Case ConnectedLoopTour` | PASS: uninterrupted traversal through connected routes. [Log](ts-76/round-2/ConnectedLoopTour.log), [measurements](ts-76/round-2/ConnectedLoopTour.txt). |
| `-Case TwoCarTunnel` | PASS: both production cars traverse the retained lower tunnel without damage. [Log](ts-76/round-2/TwoCarTunnel.log), [measurements](ts-76/round-2/TwoCarTunnel.txt). |
| `-Visual -Case TabletopSide` | PASS on final export: four continuous side approaches/climbs at 8 m/s, full health, final rendered geometry. [Log](ts-76/round-2/side-visual-final.log), [measurements](ts-76/round-2/side-visual-final.txt). |

The six offset deck passes retained continuous ground contact and 1000/1000 HP.
Both 6 m/s approaches crossed the full bridge and exited without trapping or
collision damage; small kicker launches still occur at that speed. Side climbs
had only brief 3–7-frame crest unloads, with grounded completion and full health.
The retained 10.5 cm beam crown did not block the exercised crossings.

Jump launches remained directed toward the center tabletop. At requested
14/16/18 m/s approaches, measured horizontal launch speeds were 10.92/13.14/14.84
m/s west and 11.65/13.20/14.85 m/s east; peak vehicle-origin heights were
8.38/9.65/10.56 m and 8.81/9.65/10.57 m. These are within 0.03 m/s and 0.02 m of
Round 1's recorded launch speed/peak height. Actual airtime is now 1.07–1.70 s
and airborne horizontal travel 11.25–24.68 m because the landing is raised.
All six landed on the tabletop, recovered grounded and retained full health.
Vehicle-origin height is not tire/underside clearance.

Final rendered views: [tabletop](ts-76/round-2/tabletop.png),
[lower passage](ts-76/round-2/tunnel-clearance.png),
[overview](ts-76/round-2/overview.png). Jump images from the preceding integrated
visual run are historical captures before the final outer-toe-only correction;
the final native run reproduced the recorded centerline launch metrics.

Human keyboard/controller feel, all approach angles/speeds, stopped restarts on
all slopes, production chase-camera tunnel occlusion, dense combat, exported
builds, remote devices and authenticated EOS remain unverified. Existing
stationary uphill-start failures are unresolved and are not counted as passes.
No performance benchmark is claimed; automated fixed-step and local networking
checks do not establish target-hardware performance or remote racing balance.
Material identity and dressing remain outside TS-76.

### Final build, asset and integration verification

- `check.ps1`: PASS on final implementation; zero-warning/error Debug production
  and Release solution builds, 556 Core tests and 352 non-native transport tests.
  [Final log](ts-76/round-2/check-final.log).
- Godot 4.7.2 .NET editor import: PASS; [log](ts-76/round-2/import.log).
  Headless main-scene startup also exited cleanly; [log](ts-76/round-2/startup.log).
- Reopened final Blender source audit: PASS, 53 editable structural pieces,
  correct removed/retained barriers, unchanged horizontal terrain topology and
  all 146,145 vertices outside the local fills exactly preserved. Reopened
  float32 mesh coordinates contain 54,969 changed vertices; the generator's
  double-precision array records 54,980 before Blender storage rounding.
  All recorded source/export hashes match. [Audit](ts-76/round-2/source-audit-final.log).
- The final Blender run successfully wrote source/GLB/terrain measurements but
  failed while overwriting `terrain-sources.json` (`OSError: Invalid argument`).
  The stale manifest was regenerated from the actual final files with PowerShell
  SHA-256 hashing, then the complete source audit above passed. No geometry was
  changed by that repair. [Export log](ts-76/round-2/blender-terrain.log).
- `check-oval.ps1 -NoBuild`: PASS, 3,087 checks on the final asset, including native
  driving, rim continuity, both adapters, containment and practice integration.
  [Log](ts-76/round-2/oval-final.log).
- `check-network-vehicles.ps1 -NoBuild -Players 2`: PASS on the final asset for
  separate-process local production-map driving/replication and automated camera
  controls. This is not tunnel-specific remote-device coverage.
  [Log](ts-76/round-2/network-final.log).

### Review and delivery

[Astra Runtime Critique — Story Round 2](ts-76/round-2/astra-critique.md):
**8.1 / 10 — PASS (>=8.0)**. After final verification, Astra independently ran
all tabletop scenarios, a rendered west jump and rendered two-car tunnel
traversal, and inspected the final views. No production changes followed.

[Independent engineering review](ts-76/round-2/engineering-review.md): **PASS**
for implementation commit `c1cb13c`, pushed to `ts-76-pg` / PR 90. Latest main
remained `1a02028`; canonical version 0.1.38 and Windows 0.1.38.0 matched.
GitHub verification was still pending at review; no remote CI pass is claimed.
The user's two unrelated modifications remain excluded. Review records are
committed separately on the same branch. PR 90 remains open and unmerged.

Human runtime verification/acceptance: **PASS (2026-09-23)**. The user accepted
this TS-76 result for the milestone while explicitly noting that the tabletop still
needs further refinement/polish but is good enough for now. That remaining polish
is an accepted limitation, not a claim that the tabletop is final. No further
TS-76 correction round is authorized by this acceptance.
