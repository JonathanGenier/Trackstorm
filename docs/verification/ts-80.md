# TS-80 verification

Date: 2026-09-24. Story branch: `ts-80-pg`.

Current outcome: **Round 2: 7.7 / 10 — FAIL, explicitly accepted by the human for PR delivery**. The user subsequently instructed: "go ahead approve create pr". This accepts the current result without changing the score or authorizing another polish round. The initial sections retain Round 1 history and measurements, superseded where stated by Round 2.

## Scope and authority inspected

- Read [TS-80](https://jonathangenier.atlassian.net/browse/TS-80), its acceptance criteria, comments and issue links. It has no children or comments. TS-74 blocks TS-80 and is Done; TS-80 blocks TS-81.
- Read TS-74 and visually inspected attachment 10017, `Map Concept 0.2.0 Scale.png`, in Jira. Used its rocky dirt-course islands, scrub/grass and restrained perimeter infrastructure as direction, not dimensions.
- Read root AGENTS, workflow, standards, critique, feature index, oval-map documentation, existing oval/infield authoring notes, structural source and third-party registry. No explicit TS-80 requirement changes were found in the supplied instructions or Jira comments. Existing approved TS-76 tabletop changes were preserved.
- Fetched current main before implementation and again before final checks. HEAD and origin/main were both `4c4b24e` (ahead/behind 0/0); no merge was needed. Version synchronization/check passed at canonical `0.1.46`, Windows `0.1.46.0`.

## Implemented locally

- Sixteen independent GLB assets under `assets/environment/models`: BoulderTall, BoulderLow, RockSlab, RockCluster, RockLedge, ScrubLow, ScrubTall, GrassClump, DryGrassClump, FirMature, PineOpen, SpruceYoung, Guardrail4m, Barrier3m, LightPole9m and DrainChannel2m.
- Retained `source/EnvironmentLibrary.blend`: 16 asset-marked collections and 81 individually editable meshes, metre units, identity roots and ground-centred pivots. Conifers derive from the existing original oval source; the original remains unchanged.
- Blender builder, source audit, import configuration, per-asset bounds/triangle/collision/material/provenance manifest and authoring instructions. No new third-party acquisition; existing CC0 Concrete textures are reused.
- Packed UVs, semantic slots, exported vertex variation, shared map material treatment, explicit convex collision-source pieces, native imported LODs and small-vegetation distance limits.
- Standalone native Godot gallery/check script, PowerShell entry point and narrow fast-check routing with regression coverage. Feature/index/provenance documentation updated.
- Production oval scene, terrain, topology, map placement and gameplay code remain unchanged by TS-80. Temporary map-context placements exist only in the verification process and are never saved to the production map.

## Commands and actual results

The local tools were Blender 5.2.2 LTS and Godot 4.7.2 .NET. Rendered checks used Compatibility/OpenGL on NVIDIA GeForce GTX 1070.

| Command / operation | Actual result |
| --- | --- |
| Blender `--background --python-exit-code 1 --python assets/environment/BuildLibrary.py` | Exported all 16 assets and saved the blend. [Log](ts-80/ts80-blender.log). Blender reported a thumbnail-cache OpenImageIO write error; source/export outputs subsequently passed the independent reopen/hash audit. |
| `./assets/environment/ConfigureImports.ps1` | Wrote per-asset import options; retained stable UIDs on rerun. |
| Godot `--headless --path . --editor --import` | Final import exited 0 with no errors/warnings. [Log](ts-80/ts80-import.log). |
| Blender `--background --python-exit-code 1 --python assets/environment/AuditLibrary.py` | PASS: 16 collections, 81 editable meshes, metre pivots, finite packed UVs, material slots, collision counts, GLB structure and hashes. [Log](ts-80/ts80-source-audit.log). |
| `./tools/check-fast.ps1 -Area Client` | PASS: Debug Client build, zero warnings/errors. Explicit area used because Auto reads committed branch changes and implementation was still uncommitted. [Log](ts-80/ts80-check-fast.log). |
| `./check.ps1` | PASS: workflow/version/media regressions, media checksums, restore, Debug production build and Release solution build with zero warnings/errors; 610 Core tests and 355 non-native transport tests passed, none skipped. [Log](ts-80/ts80-check.log). |
| `./check-environment.ps1 -GodotPath <4.7.2 .NET executable> -Visual` | PASS: 296 assertions, 8,246 visual base triangles, 27 surfaces with native LOD buffers, eight native impacts. [Log](ts-80/ts80-runtime.log). |
| Godot `--headless --path . --quit-after 180` | PASS: main-scene startup exited 0, no errors/warnings. [Log](ts-80/ts80-startup.log). |
| `./tools/check-version.ps1` | PASS at 0.1.46 / 0.1.46.0 against freshly fetched main. |
| `git diff --check` | PASS after removing the scaffold's extra blank line at EOF. |

Iteration failures were not treated as passes: initial restricted Godot runs reported user-log/cache/certificate/editor-settings access errors; rerunning with normal application access produced clean final import/runtime logs. The first targeted build failed NU1900 after a restricted NuGet audit; `dotnet restore Trackstorm.sln --force` with network access cleared that cached failure and subsequent builds/check.ps1 passed. The initial reference-car check used local child transforms and failed; using its complete imported transform hierarchy produced the measured 4.81 m length. These were corrected before formal critique.

## Direct native/runtime observations

- VERIFIED: all 16 assets instantiated at unit scale; imported Y-up bounds matched recorded Blender bounds within 2 mm. UV, vertex-color and material channels resolved. Repeated instances shared mesh resources while retaining separate transforms.
- VERIFIED: authored collision part counts, convex shape types and layer 1. Drainage remained open along its channel and solid across its lips. Repeated guardrail/barrier placements retained collision immediately either side of module seams.
- VERIFIED: a 900 kg, CCD-enabled rigid sphere proxy at 12 m/s impacted BoulderTall, Barrier3m, Guardrail4m and LightPole9m at 0 and 90 degrees. All eight runs contacted the obstacle and did not pass through it. This is not a production-vehicle driving test.
- VERIFIED: rendered gallery, close-up materials/vegetation/rock views, repeated modules, 4.81 m reference vehicle comparison, base-versus-generated-LOD distance views and three temporary rock placements on the actual current map were generated and visually inspected.
- VERIFIED: asset base triangle sum 8,246 and GLB file total 904,156 bytes; these are resource measurements, not a frame-time benchmark.
- INFERRED: sparse instances and shared resources should be modest in cost. Final map density, shadows and batching determine actual gameplay performance.

Selected rendered evidence:

- [Complete gallery and reference car](ts-80/gallery.png)
- [Repeated modular infrastructure](ts-80/module-reuse.png)
- [Infrastructure / scale](ts-80/infrastructure.png)
- [Rock close-up](ts-80/rock-detail.png), [ledge close-up](ts-80/ledge-detail.png)
- [Scrub](ts-80/scrub.png), [grass close-up](ts-80/grass-detail.png), [conifers](ts-80/conifers.png)
- [Actual-map context](ts-80/map-context.png)
- [Distance: base mesh](ts-80/distance-base.png), [distance: native LOD](ts-80/distance-lod.png)

## Assumptions, limitations and explicitly unverified areas

- Asset inventory interprets the approved concept/current layout; no invented grandstand/building/sign system or manufactured debris was added. Existing tunnel/deck/piers remain their own authored system. Loose rock forms supply natural rubble.
- Grass and scrub are non-colliding; trunks, rail envelopes and pole mast/base are simplified collision. Lamp heads and canopy collision are intentionally absent. Drain placement must remain outside driving corridors or be integrated into terrain by later placement work.
- Godot Compatibility uses hard grass/scrub visibility cutoffs at 65/120 m. Sampled distance views were inspected, but continuous moving-camera transition quality and final placement masking were not verified.
- The light pole is geometry only; actual light placement, night lighting and light/shadow budgets are not part of this library.
- No manual Blender UI asset-browser drag/drop or manual edited-asset re-export cycle was performed. Source audit used native Blender Python to reopen and inspect the saved file. Procedural rebuild overwrites manual edits; authoring notes explicitly require preserving them.
- No production-car impacts/driving around these new props, exported-build test, multiplayer test, long-duration soak, mass-placement frame-time benchmark or final dressed-map performance/balance test was performed. Native asset proxy impacts do not establish those results.
- No independent post-push engineering review has occurred.
- An unrelated whitespace-only edit appeared in `code/Core/Input/InputButtons.cs` during the session. It was not authored, reverted or included in TS-80's intended change set. Integrated tests ran with that local working tree present.

## Astra Runtime Critique — Story Round 1

**Overall Score: 7.3 / 10**

**Quality Assessment: FAIL (<8.0)**

This is an implementation-agent runtime/experiential assessment of the native results above, not an independent engineering review. The formal assessment was made after comprehensive final verification. No corrective implementation was performed after this assessment.

| Material category | Score | Observed basis |
| --- | --- | --- |
| Runtime Functionality | 8.4 | Clean final imports, correct measured transforms/material channels and reusable instances. |
| Physics | 8.0 | Open channel, modular seam support and eight stable native proxy impacts; production-car behavior unverified. |
| Visual Quality | 6.5 | Functional coherent categories, but close-up rock treatment and sparse radial scrub remain visibly generic and insufficiently refined for the concept direction. |
| Runtime Integration | 7.8 | Forest treatment matches existing map and modules have usable scale; actual-map rocks still read as conspicuously repeated inserted props. |
| Feature-Specific Operational Quality | 7.5 | Repeated modules and source reopen work; manual artist edit/export and asset-browser operation remain unobserved. |

### What was exercised and limitations

VERIFIED: the native rendered gallery, repeated modules, close-up views, actual-map context, sampled LOD comparison, collision queries/impacts and native source reopen described above. At the sampled distant view, generated LODs did not produce an obvious gross silhouette collapse. UNVERIFIED: continuous transition feel, production-car handling, artist GUI workflow and full placement-density performance. These limitations are not replaced by static test counts or an engineering score.

### Findings and recommended next round

1. **Problem:** Rock surfaces have conspicuous similarly oriented brown bands and rounded forms that read closer to generic modeled props than the concept's weathered rocky outcrops. **Evidence:** `rock-detail.png`, `ledge-detail.png` and `map-context.png`; the same treatment repeats across all three map-context stones. **Severity:** Medium. **Impact:** Rocks are a major infield category and repetition is apparent at driving-view distances. **Suggested Improvement:** Refine the Blender fracture planes and introduce restrained original mineral variation/roughness with less dominant striping, validating the same close-up and map-context views. **Scope:** In Scope. **Corrective Work Type:** Existing Task (TS-80).
2. **Problem:** Scrub has a strongly radial bundle of straight stems with thin, sparse planar leaf coverage; the grass close-up exposes a rigid blade fan. **Evidence:** `scrub.png` and `grass-detail.png`. **Severity:** Medium. **Impact:** Repeated vegetation is likely to read as repeated procedural shapes instead of natural shoulder cover. **Suggested Improvement:** Author irregular secondary branches, less uniform leaf orientations and varied curved/tapered blade silhouettes while retaining the current opaque geometry/collision budget. **Scope:** In Scope. **Corrective Work Type:** Existing Task (TS-80).

**Recommended Next Round:** If authorized by the human, address these two art-quality findings without adding asset categories or TS-81 placement, then repeat affected Blender/native checks and the formal critique. Do not start that round automatically.

## Round 1 delivery state (historical)

Implementation and verification evidence are saved locally on `ts-80-pg`. Formal critique is below 8.0, so work stops for human direction as explicitly requested. No commit/push or new polish round was performed after the failing critique; the Story is not represented as complete. No Jira issues/comments/status changes or PR were created.

## Human-authorized Round 2 corrections

The user explicitly authorized a second round to improve rock, shrub and grass realism while preserving the existing library and production placement. TS-80 was re-read in Jira before edits; scope/acceptance criteria remained unchanged. The prior failed critique, uncommitted work and applicable routing/standards/asset docs were inspected. Main was fetched again: HEAD versus origin/main remained 0/0. No restart, new categories, TS-81 dressing or production-map changes were made.

- Replaced rock sphere jitter with deterministic asymmetrical clipping planes, coherent erosion at two scales and denser silhouette geometry. Replaced directional vertex bands with aperiodic variation. Ground contact is corrected after decimation and asserted in the source audit.
- Replaced concrete-derived stone maps with original deterministic 512 x 512 mineral albedo and normal maps, shared through Rock.tres. Packed both maps into the retained Blender source and included them in manifest hashes. Concrete infrastructure retains its existing material.
- Rebuilt shrubs with segmented main stems, secondary twigs and folded alternating leaves with varied lengths/orientations. Adjusted bark/leaf material response after native inspection.
- Rebuilt grass as seven irregular subtufts with varied blade width, height, orientation and curved taper; preserved opaque two-sided rendering, no collision and existing distance policy.
- Regenerated the same sixteen GLBs and editable source. Final source has 217 editable meshes. Added a dedicated shrub close-up to the native fixture and updated authoring/provenance notes.
- Final base triangle sum is **28,550** (previously 8,246); sixteen GLBs total **7,822,176 bytes** (previously 904,156). Increased geometry and embedded maps are real resource costs, not evidence of acceptable mass-placement performance. Godot uses shared stone material overrides; final density and batching remain unverified.

## Round 2 verification evidence

Commands used the same Blender 5.2.2 LTS and Godot 4.7.2 Mono paths as Round 1. Native rendering used Compatibility/OpenGL on NVIDIA GTX 1070. Logs and final captures are in [round-2](ts-80/round-2/).

| Command / exercise | Actual result |
| --- | --- |
| `git fetch origin main`; `git rev-list --left-right --count HEAD...origin/main` | Fetch succeeded, 0/0. |
| `tools/sync-story-version.ps1` | Canonical 0.1.46 / Windows 0.1.46.0 synchronized and checked. |
| Blender `--background --python-exit-code 1 --python assets/environment/BuildLibrary.py` | Final exit 0; sixteen exports and saved blend. `blender-final.log` contains a thumbnail-cache OpenImageIO write error; not a clean thumbnail-generation result. Saved content passed the subsequent audit. |
| `assets/environment/ConfigureImports.ps1`; Godot `--headless --path . --editor --import` | Final exit 0; no ERROR/WARNING entries in `import-final.log`. |
| `tools/check-fast.ps1 -Area Client` | PASS; Debug client build, 0 warnings/errors (`check-fast.log`). Explicit Client selection covers uncommitted work. |
| `check.ps1` | PASS, exit 0; workflow/frontend checks, Debug/Release builds and analyzers, 610 Core + 355 transport tests, 0 failed/skipped; builds 0 warnings/errors (`check.log`). |
| Blender `--background --python-exit-code 1 --python assets/environment/AuditLibrary.py` | Final PASS: 16 asset collections, 217 meshes, units/pivots/UVs/materials/collision sources/GLB structure/hashes, packed stone images and rock ground contact (`source-audit-final.log`). |
| `check-environment.ps1 -GodotPath <Godot console executable> -Visual` | Final PASS, exit 0: 297 assertions, 28,550 base triangles, 27 surfaces with native LODs, eight native proxy impacts. Correct measured scale/orientation, material/UV/color channels, shared-instance resources, module seams and open drain checks (`runtime-final.log`). |
| Godot `--path . --quit-after 180` | Normal native startup exit 0, no logged warning/error (`startup.log`). No production-car driving claim. |
| `git diff --check` | No whitespace errors reported. |

During iteration, the first strengthened ground-contact audit failed on RockSlab: decimation moved its lowest point after an earlier pivot correction. The correction was moved after decimation, assets rebuilt/reimported, source audit and native fixture repeated successfully. The actual failure is retained in `source-audit-ground-failure.log`. Early rendered inspection also exposed thin/dark grass; blade widths and vegetation shading were revised before final verification. These are implementation corrections before the formal critique, not a third critique round.

VERIFIED by direct inspection of final native captures: [rock](ts-80/round-2/rock-detail.png), [ledge](ts-80/round-2/ledge-detail.png), [shrub](ts-80/round-2/scrub-detail.png), [grass](ts-80/round-2/grass-detail.png), [gallery and scale reference](ts-80/round-2/gallery.png), [repeated modules](ts-80/round-2/module-reuse.png), [sampled LOD](ts-80/round-2/distance-lod.png), and [temporary actual-map integration](ts-80/round-2/map-context.png). Captures came from an actual native rendered run, not Blender previews or synthetic mockups.

UNVERIFIED: Blender GUI asset-browser drag/drop, manual source editing/re-export, continuous moving-camera LOD/cutoff quality, production-car impacts/handling, exported build, multiplayer, long-duration soak and mass-placement frame times. Source appearance in the Blender viewport was not visually inspected; native Blender reopening/data audits and Godot rendering were exercised. No independent engineering review occurred. The unrelated InputButtons.cs whitespace edit remains untouched; comprehensive tests ran with it present. The thumbnail-cache error persists. No runtime performance score is inferred from triangle counts or successful tests.

## Astra Runtime Critique — Story Round 2

**Overall Score: 7.7 / 10**

**Quality Assessment: FAIL (<8.0)**

This implementation-agent runtime/experiential assessment was made after the final comprehensive checks, native rerun and capture inspection above. It is not an independent engineering review. The score judges the observed result, not the improvement effort or test count.

| Material category | Score | Observed basis |
| --- | --- | --- |
| Runtime Functionality | 8.4 | Native imports and repeatable shared instances operate correctly; measured orientation/scale/material channels pass. |
| Physics | 8.0 | Eight native proxy contacts, modular seams and open drainage work; production-car behavior remains unverified. |
| Visual Quality | 7.2 | Stone stripes are gone and vegetation curves/coverage improve, but close-up shrub structure and ledge forms remain visibly procedural. |
| Runtime Integration | 7.8 | Original stone tones work better on the actual dirt terrain; retained modules and tree scale remain coherent. Natural assets still need art refinement for the requested believable direction. |
| Feature-Specific Operational Quality | 7.7 | Source reopen and packed textures work; native reuse is demonstrated. GUI authoring and artist export workflow remain unobserved. |

### Findings and recommended next round

1. **Problem:** Shrubs still expose a bundle of long, nearly straight, similarly sized basal stems, with leaf coverage concentrated above them. **Evidence:** final `scrub-detail.png`; improved twigs and folded leaves are visible, but the open angular lower scaffold dominates the silhouette. **Severity:** Medium. **Impact:** Repeated shoulder shrubs will still look assembled rather than grown. **Suggested Improvement:** If a third round is authorized, reshape the existing shrubs around a few irregular curved woody leaders, finer branching at varied heights and asymmetrical canopy masses; inspect near and driving-distance views. **Scope:** In Scope. **Corrective Work Type:** Existing Task (TS-80).
2. **Problem:** The rock ledge remains a stack of softly rounded masses with fairly uniform granular surface response; the new fracture planes are not strongly readable as geological breaks. **Evidence:** final `ledge-detail.png` and `rock-detail.png`. The previous directional banding is resolved, but distinct broken faces/crevices remain weak. **Severity:** Medium. **Impact:** A major natural asset category still falls short of the requested believable rocky outcrop appearance. **Suggested Improvement:** If authorized, emphasize a few large angular fracture faces and localized chipped edges/crevice weathering on the existing source meshes, retaining modular bounds and collision policy. **Scope:** In Scope. **Corrective Work Type:** Existing Task (TS-80).

**Recommended Next Round:** Only with explicit human authorization, prioritize those two remaining shape-quality findings within the existing asset set, then repeat affected native/source verification and the formal critique. No third round is started here.

## Round 2 stop state (historical)

Round 2 changes and actual evidence remain local on `ts-80-pg`. The formal score is below 8.0, so the user's conditional commit/push instruction does not apply. No commit, push, PR, Jira mutation or post-critique polish was performed. TS-80 is not represented as complete.

## Human acceptance and PR delivery

After the Round 2 stop, the user explicitly accepted delivery: "I dont see how a human can test. go ahead approve create pr". This authorizes committing, pushing and creating the Story PR with the known critique findings. The actual critique remains **7.7 / 10, FAIL**; it is not relabeled PASS. No new art changes or third critique round were performed.

Before delivery, main was fetched again (0/0 divergence), Story version synchronization and `tools/check-version.ps1` passed, and `check.ps1` passed again: 610 Core and 355 transport tests, zero failures/skips, zero build warnings/errors (`round-2/check-accepted.log`). The native visual fixture passed again with 297 assertions, 27 LOD surfaces and eight impacts (`round-2/runtime-accepted.log`). Blender's source audit passed again with 16 collections, 217 meshes and packed original textures (`round-2/source-audit-accepted.log`). The human-inspection section in the asset README now explains the automatic screenshot fixture and a separate Godot editor inspection workflow; no manual GUI verification is claimed.

The known visual findings and previously unverified areas remain. Independent engineering review and CI are pending at PR submission. The unrelated InputButtons.cs change and Godot-generated extracted texture copies are excluded from the Story commit. The human approval is acceptance for PR delivery, not a GitHub reviewer approval or authorization to merge.
