# TS-116 verification

## Implemented and approved scope

- Read Jira TS-116 in full (no children or comments), its linked follow-on Story metadata, applicable repository routing, and current item/spawn/HUD/recovery ownership before implementation.
- User explicitly selected **enable Oil/Nitro spawning now despite unavailable use**. Updated the Jira description with that decision on 2026-09-22. All four default weights are one; Oil/Nitro use is rejected without consumption or effects until their separate Stories implement it.
- Added the shared four-item registry, stable byte identities, metadata and stateless Wrench/Missile use handlers. Existing ItemAuthority, ItemSpawnAuthority, vehicle damage authority and transaction boundaries remain the owners.
- Replaced the fixed two-weight distribution with an immutable registry-keyed map, stable registry-order selection and generated Developer Options weight controls. Zero weights exclude items; empty, negative, missing/unknown and overflowing pools fail validation.
- Extended existing publication validation and checkpoint composition to every registered inventory identity. Item wire version is three; gameplay tuning wire version is five. There is no second inventory or replication protocol.
- HUD names/icons and audio/VFX hooks resolve registry metadata. Added original project-authored Oil/Nitro SVGs with provenance in the HUD manifest. Removed all vehicle-attached held models and their follow/update path; projectile and use presentation remain.
- Updated current feature documentation for items, spawning, HUD, tuning, reconnect and migration.
- Synchronized with origin/main before implementation and again before final verification; main was `97a2057`. Canonical version and export metadata validate as `0.1.29` / `0.1.29.0`.

## Verification actually performed

Commands ran on Windows with .NET SDK 10.0.401 and the repository-local Godot 4.7.2 .NET console executable under `.godot/verification-tools/Godot_v4.7.2-stable_mono_win64/`. Native visual runs used OpenGL compatibility rendering on the RTX 4070 Ti. Listed artifact directories are local ignored evidence, not committed runtime assets.

| Check | Actual result |
| --- | --- |
| `tools/sync-story-version.ps1`, including `check-version.ps1` | PASS, current-main ancestry and both version representations |
| Targeted `dotnet test ... --filter FullyQualifiedName~Items` | PASS, initial 22 item tests |
| Targeted Items/MigrationTests/ResumeCheckpoint test filter | PASS, 49 tests after new fixtures were corrected |
| `tools/check-fast.ps1 -Area Client` | PASS, Debug production build, zero warnings/errors |
| `tools/check-fast.ps1 -Area Core` | PASS, 522 Core tests |
| `check.ps1` on the final code | PASS: 108 version-rule checks, workflow/media checks, restore, Debug production build and Release full solution with warnings as errors; 522 Core and 345 non-native Client/transport tests, zero failures |
| Godot `--headless --editor --path . --import` | PASS; imported the added HUD vectors |
| Godot main scene `--headless --path . --quit-after 300` | Exit 0, no warnings/errors in captured log |
| `check-items.ps1 -NoBuild -Impaired` | PASS, eight native UDP peers, full/damaged Wrench use, duplicate requests, physical input edges, missile launch/impact, damage/falloff/impulses and native obstacle collisions |
| `check-items.ps1 -NoBuild -Visual -Impaired` | PASS after fixing synthetic fixture focus/neutral-edge setup; same eight-peer combat checks plus rendered captures |
| `check-item-spawns.ps1 -NoBuild -Visual -Impaired` | PASS, eight peers, contested claim, all eight authored markers, all four awarded identities, matching tokens/slots, cooldowns, repeated unavailable use retaining Oil/Nitro, occupied-slot protection, and no held presentation nodes |
| `check-hud.ps1 -NoBuild` | PASS, Empty plus all four identities, health/speed material state, units, hiding and nine resolutions |
| `check-reconnect.ps1 -NoBuild` | PASS, three native resumes including 125 seconds offline, Oil retained, original bodies/tags reused, prediction reset, healing/death/respawn and final/new-match lifecycle |
| `check-migration.ps1 -NoBuild -Players 2` and `-Players 3` | Both PASS, sequential epochs, native body retention, Wrench continuation/use and Nitro retained on departed host, configuration/RNG/phase continuation |
| `check-migration-processes.ps1 -NoBuild -Players 2` and `-Players 3` | Both PASS, independent processes, forced host termination and survivor epoch-2 installation |
| `check-lobby.ps1 -NoBuild` | PASS, eight-player lifecycle, repeated match/reset, active admission/bootstrap interruption/retry and retained-slot capacity |
| `check-audio.ps1 -NoBuild` | PASS, 53 native assertions; this is routing/asset verification, not a listening assessment |
| `check-developer-options.ps1` | PASS, write phase 504 assertions and fresh read phase eight assertions, including generated weight controls through normal configuration replication/persistence |
| Complete Story diff and `git diff --check` | Inspected; whitespace check passed. No new external dependency, Shared layer or Core engine/provider reference. |

The first automatic `check-fast.ps1` call reported no committed diff because this branch had not yet been committed; it was not counted as verification. Explicit Area checks above exercised the working tree.

## Iteration failures and corrections

- A sandboxed restore could not read the installed user NuGet configuration. Approved elevated execution used the installed package cache; subsequent builds/tests passed.
- New test fixtures initially used nonexistent/reset or read-only record APIs and omitted one parameter comment. Corrected to the existing authoritative reset/construction APIs; targeted and comprehensive builds passed.
- The first comprehensive Core run exposed obsolete assumptions: a fixed 76-option count, and a seed test that tried to consume every draw. Updated the catalog expectation to 78 and used the existing reset boundary between four-item draws. Final comprehensive results above passed.
- The new native migration assertion initially accessed the already-disposed former-host driver. Saved its stable player ID before departure; both roster sizes subsequently passed without runtime diagnostics.
- Initial visual combat attempts failed the synthetic mouse-use assertion while the headless run passed. The fixture now explicitly enables its adapter and observes the neutral boundary before injecting/observing physical press/release edges. Production focus/input handling is unchanged. The final rendered impaired run passed.

## Runtime artifacts inspected

- Combat: `.godot/item-checks/fcd74cfde94441289f0b5a22f5d4ab25/` (log, evidence text, four impact images). Directly inspected `impact-0.png`.
- Four-item spawning: `.godot/spawn-checks/26a479921d9a4051b853692db22c1a31/` final run. Directly inspected `occupied-slots.png` from earlier passing run `5af0a67f9842438b99b23bb9abdadc21`; later fixture edits only moved repeated-use requests earlier so the unchanged-slot assertion follows network processing.
- HUD: `.godot/hud-checks/030f9aed92784c1d8778a7e426f097b8/`; directly inspected `state-850-Oil.png` and `state-850-Nitro.png`. Labels and distinct silhouettes fit the existing slot.
- Reconnect: `.godot/reconnect-checks/runtime.log`.
- In-process migration: `.godot/migration-checks/2-players/runtime.log` and `3-players/runtime.log`.
- Independent-process migration: `.godot/migration-process-checks/90ab6492e8834e4784cdf50642018907/` (two players), `5543d2228e94409e88908ecbca3caa32/` (three players).
- Lobby: `.godot/lobby-checks/470c2cd01281493fbd1c0df213d21dc7/`.
- Developer Options: `.godot/developer-options-checks/4673ebd851004ad7939da620afe4d42c/`.
- Final comprehensive/main-scene logs: `.godot/ts116-final-check.log`, `.godot/ts116-main-smoke.log`.

## Astra Runtime Critique — Story Round 1

**Overall Score: 8.2 / 10**
**Quality Assessment: PASS (>=8.0)**

| Category | Score | Observed basis |
| --- | --- | --- |
| Runtime functionality | 8.5 | Repeated repair/projectile use and four-item grant/distribution work through actual native peers; unavailable-use slots remain intact. |
| Multiplayer / recovery | 8.3 | Eight-peer impaired outcomes agree; three reconnects, sequential migration and independent-process host termination succeed. |
| UI / visual feedback | 8.0 | Inspected Oil/Nitro labels and silhouettes are distinct and readable; occupied vehicles have no inventory model; committed missile impact remains visible. |
| Controls / responsiveness | 8.0 | Native fixture exercises short mouse/controller input edges and duplicate requests through the production adapter/authority path. |
| Runtime stability / integration | 8.2 | Final affected native scenarios complete without captured errors/warnings; normal lobby/reset and tuning persistence remain operational. |

**VERIFIED:** Native outcomes, peer agreement, configured spawning, local HUD state/rendering, no held world nodes, reconnect and migration continuation, current tuning controls.
**INFERRED:** Similar behavior on an ordinary same-build LAN outside these local fixtures.
**UNVERIFIED:** Actual remote EOS/multi-PC/NAT operation, subjective audio quality, sustained gameplay performance and manual driving feel. These are not claimed from local harnesses or screenshots.

The score evaluates the approved transitional framework scope. Oil/Nitro being spawnable without use behavior is the explicit user decision, not implemented Oil/Nitro gameplay. No new critique-driven changes were made. Human acceptance and independent PR engineering review are not claimed by this report.

## Limitations, risks and explicitly unverified areas

- Oil/Nitro currently occupy the single slot until an existing clearing boundary (such as death/reset/removal); pressing use does not free it. This intentional temporary gameplay limitation is approved in Jira and needs TS-117/TS-118 for actual effects.
- The active oval still has zero pickup markers. Spawn verification uses the existing eight-marker combat fixture; no new map pickups were introduced.
- All peers must run the same updated game/schema versions. Old item/configuration envelopes are deliberately rejected; mixed-version or old persisted match continuation is not supported.
- Authoritative inventory still replicates for correctness. Removing its normal world presentation is not cryptographic inventory secrecy against modified clients or developer diagnostics.
- Local UDP impairment and forced local process exit do not establish authenticated remote EOS, separate-PC networking, Internet/NAT behavior or long-session performance. No new exported executable was validated.
- Native audio hooks passed; no subjective listening or physical-controller/manual play session is claimed. Only the explicitly listed images were visually inspected; nine resolutions were verified by the rendering harness, not all manually inspected.
- No unresolved in-scope runtime defect was observed in the final checks. Independent engineering review and CI remain separate delivery gates.
