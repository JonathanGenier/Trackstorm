# TS-122 verification

Date: 2026-09-21. Story branch: `ts-122-jg`. Jira TS-122 was read in full: no children, comments or attachments. The current description and explicit implementation request agree. All ten supplied image references were inspected. Current `main` was fetched before implementation and delivery; branch ancestry and version `0.1.23` / Windows `0.1.23.0` passed.

## Implemented

- Main Menu only: exactly Play, unavailable Garage / Coming Soon, Settings and Quit, delegated to existing browser, Settings and cleanup owners.
- Reusable entry records, independent text/status/icon regions, one blank plate and selected material with identical geometry. Header, repeating chains and fabric are separately composed; the full-menu reference is never used as an underlying image.
- Loader-triggered acceleration/drop, abrupt catch, restrained settle, then subtle sway. Activation is blocked until settled, held activation does not leak, and ambient movement leaves native targets stationary.
- Fabric-only transparent imagegen derivatives with fixed upper attachment regions; original header, plate, chain and icon pixels retained. Manifest records source/runtime hashes and provenance. No new redistributed font or third-party dependency.
- Current-system docs, verification routes and legacy harness entry references updated. MenuShell video/music and other screens retain existing owners and lifecycle.

## Verification

Installed Godot: `4.7.2.stable.mono.official.ed1daf0bf`; Windows OpenGL Compatibility renderer on NVIDIA RTX 4070 Laptop GPU. .NET SDK: `10.0.401`.

| Command | Actual result |
| --- | --- |
| `tools/check-fast.ps1 -Area Client` | PASS, Debug build with no warnings/errors. Auto mode initially reported no committed changes and performed no checks; explicit area was used for uncommitted iteration. |
| `import-godot.ps1 -GodotPath <4.7.2 console>` | PASS, no runtime diagnostics. |
| `check.ps1` | PASS: version/workflow/media regression checks, materialized media, restore, Debug production and Release solution builds with warnings as errors, 500 Core tests and 337 non-native transport tests. Log: `.godot/ts-122-final-check.log`. |
| `check-startup.ps1 -GodotPath <4.7.2 console>` | PASS, headless extended startup harness. |
| `check-startup.ps1 -GodotPath <4.7.2 console> -Visual -SkipBuild` | PASS on final layout; no ERROR/WARNING. Log: `.godot/ts-122-final-startup.log`; screenshots: `.godot/main-menu-checks/`. |
| `check-menu.ps1 -GodotPath <4.7.2 console> -Visual` | PASS, 951 assertions: solo Waiting, native mouse modes, mixed navigation, Settings, real local UDP peer progress, Leave/re-entry and production Quit. Artifacts: `.godot/menu-checks/e073755626e74ea79195e52d86f3d579/`. |
| `check-settings.ps1 -GodotPath <4.7.2 console> -Visual` | PASS: write 8, read 24, visual 16 assertions. Log: `.godot/ts-122-final-settings.log`. |
| `check-online-lobby.ps1 -GodotPath <4.7.2 console> -Visual` | PASS through Play: browser/admission/recovery controls with deterministic provider. Log: `.godot/ts-122-final-browser.log`. No authenticated EOS claim. |
| `tools/check-version.ps1`, `git diff --check` | PASS. |
| PNG alpha inspection | All six runtime PNGs contain transparent pixels; red and checkered generated cloth contain 956,212 and 1,027,427 fully transparent pixels respectively. Source/runtime SHA-256 manifest recorded. |

## Runtime / native evidence

**VERIFIED:** Extended production startup harness injects/retries all three failure phases before revealing the menu. It attempts activation during the drop through signal, keyboard and held synthetic gamepad Accept; waits for settle; confirms no held activation leak; navigates around Garage with keyboard and gamepad; repeats navigation 18 times; clicks Garage; switches to mouse and opens/closes Settings; observes stable targets over 90 idle frames; enters/exits the browser; checks exact Loader video/music player IDs and playing state throughout. Rendered bounds pass at 640×360, 1280×720 and 1600×900.

**VERIFIED:** Native Windows playtesting inspected rendered composition and keyboard selection moving from Play directly to Settings. Iteration corrected initial TextureRect sizing and double texture multiplication in shaders. An early rendered harness failed because another desktop window held focus; the synthetic harness now explicitly enables its input adapter and both headless/rendered checks passed afterward. The 640×360 capture prompted a larger status label before final runtime verification.

**INFERRED:** The unchanged media owners and continuous playing states preserve audio continuity; automated checks do not establish audible quality. Shared plate texture and shader-only color treatment preserve selected/unselected silhouette by construction.

## Runtime critique

The independent [Astra Story Round 1 runtime critique](ts-122-critique.md) scored **7.6/10 — FAIL**. Active native mouse testing independently verified Settings/Back, inert Garage, Play/browser/Back and Main Menu Quit (exit 0, no diagnostics). Remaining in-scope recommendations are less saturated selected metal and a more integrated blank Coming Soon backing. No critique-driven polish is authorized or performed in this round; human direction is required before another round. The requested commit/push records this result without asserting acceptance or merge readiness.

## Limitations and explicitly unverified areas

- Audible output/loop seams and physical controller ergonomics were not directly verified. Controller evidence uses native synthetic Godot events.
- Astra's continuous entrance pacing, catch feel and subtle flutter observation remain unverified beyond sampled frames and harness evidence. Its keyboard tool pulses were inconclusive; the earlier implementation playtest and synthetic harness did verify directional behavior.
- Exported executable, different graphics backends/OSes, high-DPI variants, authenticated multi-PC EOS and remote networking were not verified; this change does not alter those systems.
- Windows Impact typography was observed; Arial Black/Godot fallback on machines lacking Impact was not visually verified. No font binary is distributed.
- Entry count/order/label/action modularity is implemented through `SetEntries`; non-production permutations and unusually long/localized labels were not runtime exercised.
- Existing video content, including its watermarks and unrelated diagnostic HUD, remains unchanged by the Main Menu scope.
- Full gate passed before the final tiny status-font adjustment and obsolete hidden-button cleanup; subsequent Debug/analyzer, rendered startup and browser checks covered those corrections. No authoritative Core or transport behavior changed.
- CI and independent post-push engineering review remain separate delivery gates. No merge is authorized.

## Approved left-side layout adjustment — 2026-09-21

The user authorized only a moderately smaller, left-side assembly to uncover the title on the right. Jira TS-122 was read again and updated with that decision, preserving the original requirements. The earlier selected-material and status-plaque recommendations remain outside this authorized correction.

The layout now applies 75% of its previous viewport-fitting scale and a left margin of 4% of viewport width. Artwork, chain coordinates and stationary hit targets share that layout transform. No entrance, sway, flutter, input, entry/action, media or startup-flow logic changed. The feature documentation describes the new placement.

### Current verification

| Check | Actual result |
| --- | --- |
| `tools/check-fast.ps1 -Area Client` | PASS: Debug build, zero warnings/errors. |
| `check-startup.ps1 -GodotPath <4.7.2 console> -Visual -SkipBuild` | PASS: production reveal/recovery, entrance input blocking, held input, mixed keyboard/controller/mouse, Garage skip/inertness, repeated navigation, stable targets, Settings/browser return and exact persistent MenuShell media state. Log: `.godot/ts-122-left-layout-startup.log`. |
| `check.ps1` | PASS after the final production change: version/workflow/media checks, Debug production and Release solution builds, zero warnings/errors, 500 Core and 337 transport tests. Log: `.godot/ts-122-left-layout-final.log`. |
| Current main ancestry, `tools/sync-story-version.ps1`, `git diff --check` | PASS: current `origin/main` is an ancestor; version remains `0.1.23` / `0.1.23.0`. |

**VERIFIED:** Rendered harness bounds pass at 640×360, 1280×720 and 1600×900. Direct inspection of the 640×360 and 1280×720 captures confirms the complete resting assembly is on-screen, chains remain aligned, and the right-hand title is unobstructed. Primary labels remain readable; the secondary Coming Soon text is small at 640×360. Captures: `.godot/main-menu-checks/layout-640x360.png`, `layout-1280x720.png`, `layout-1600x900.png`.

**UNVERIFIED in this correction:** Physical-controller ergonomics, audible loop seams, exported builds, high-DPI and other aspect-ratio/backend/OS variants. Existing menu/settings/network harness results above are historical and were not rerun for this two-expression layout change; the current rendered startup harness exercises the affected menu integrations. No new artwork or behavior polish was performed.

**Independent native follow-up:** Astra's retry successfully exercised the relocated Settings/Back and Play/browser/Back targets, then Quit exited 0. Its first fresh launch closed before capture; no evidence is claimed from that attempt. [Story Round 2](ts-122-critique.md) confirms the requested layout adjustment, with an integrated **7.7/10 — FAIL** because prior presentation findings remain and the secondary status text is tiny at 640×360. No additional polish is authorized or performed. The requested commit/push records this result; acceptance and any further correction remain a human decision.

## Approved stationary assembly / stronger flutter — 2026-09-21

The user authorized removal of post-settle horizontal sway and more visible anchored fabric flutter only. Jira was read in full (still no children, comments or attachments), then updated with the explicit approval superseding its earlier sway criteria. Earlier layout remains unchanged; selected-material/status polish remains unapproved.

Removed the idle clock and horizontal assembly offset. The original 0.52-second accelerating drop, abrupt catch, restrained 0.6-second settle and interaction gate are unchanged. After settling the assembly's artwork position is exactly zero. The fabric-only shader now combines a broader traveling fold with a smaller faster ripple and stronger vertical sampling displacement. Its upper 24% remains fixed, with deformation increasing toward the loose ends. Header, chains, plates, icons, labels, layout and input logic are unchanged. No texture assets or media owners changed.

Current main `cfa8901` was integrated in merge `ef50b48`; the sole merge conflict was the verification index, resolved by retaining both Story entries. Required version synchronization advances TS-122 to `0.1.24` / Windows `0.1.24.0`. Incoming TS-106 changes are from main, not new work under this animation correction.

### Current checks and observations

| Check | Actual result |
| --- | --- |
| `tools/check-fast.ps1 -Area Client` | PASS: clean Debug build. |
| `check-startup.ps1 -GodotPath <4.7.2 console> -Visual -SkipBuild` | PASS with no ERROR/WARNING: current production recovery/reveal, entrance gating and held activation, keyboard/controller/mouse, disabled Garage, repeated navigation, Settings/browser return and exact persistent media state. Log: `.godot/ts-122-animation-startup.log`. |
| Added startup idle invariant | PASS: all descendant artwork/attachment/native-control global transforms stay identical across six samples separated by 15 frames. Existing stable hit-target and selection checks also pass. |
| `check.ps1` | PASS after final source changes: version/workflow/media checks, Debug and Release builds with zero warnings/errors, 504 Core and 338 transport tests. Log: `.godot/ts-122-animation-final.log`. |
| `git diff --check` | PASS. |

**VERIFIED:** Rendered bounds at 640×360, 1280×720 and 1600×900 pass. Direct comparison of `.godot/main-menu-checks/idle-0.png` and `idle-3.png` shows changed loose flag contours while metal and text remain aligned; the flags stay outside label areas. Six `idle-*.png` samples are retained locally alongside entrance/settled/layout captures. These are sampled observations, not a continuous-motion recording.

**UNVERIFIED:** Audible output/loop seams, physical-controller ergonomics, exported builds, other graphics backends/OSes, high-DPI configurations and continuous entrance/cloth pacing are not established by these checks. Shader isolation and a zero deformation weight at attachment regions support anchoring; sampled runtime views do not prove every animation phase. Older broader menu/settings/network results remain historical and were not rerun for this narrow animation correction. No additional polish was applied.

**Independent native follow-up:** Astra compared all six current idle captures and successive native snapshots, observing fixed rigid artwork, changing lower-fabric silhouettes and aligned attachment regions. Native Settings/Back succeeded. [Story Round 3](ts-122-critique.md) supports the requested animation outcome; integrated score remains **7.7/10 — FAIL** on retained presentation concerns, with no additional polish authorized. This is the third and final Story critique round permitted by the current repository policy. Delivery records the requested work without claiming acceptance or authorizing a merge.
