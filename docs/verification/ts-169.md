# TS-169 verification

## Result and scope

Inspected TS-169, TS-115, TS-145, their comments (none), repository/nested AGENTS routing, workflow, standards, critique and the affected feature/architecture/recovery contracts. TS-169 has no required children. Re-read the unchanged TS-169 acceptance criteria after integrating its Machine Gun dependency.

The existing registry-driven selector and shared configuration catalog already provided category-local proportional item weights, host-only edits and portable recovery. This Story preserves those authoritative owners and makes their semantics explicit in Configs, extends meaningful distribution/live-edit verification, and documents the complete seven-item integration. No balancing-policy redesign, client-local distribution state, item-specific selection branches or individual gameplay changes were introduced.

- Baseline category targets remain Weapon 2, Consumable 1, Droppable 1; Missile, Salvo, Machine Gun, Wrench, Nitro, Oil and Proxy Mine each default to weight 1.
- The selected category's item draw uses the same match RNG and registry pool. Equal and unequal item weights remain independent of category allocation; 2/1/1 Weapons is covered directly by the statistical test.
- Configs now explains proportional weights, zero exclusion, host-wide future-roll application and retained per-player history. All seven item controls and all three category controls use the existing host Apply transaction, persistence and complete configuration replication.
- Added deterministic tests for proportional odds, default/control/codec completeness, rejected client edits, retained inventory/claims/history/RNG and identical next outcomes after two-player rebind/authority restoration.
- Extended the native eight-peer pickup harness: after 16 rounds, tune each category to its last registered item (Machine Gun, Nitro, Proxy Mine), then verify 16 further rounds/player, complete peer agreement and preserved category allocation. Removed a pre-existing invalid assertion that eight random draws must contain the entire roster; registered awards and exact replication remain checked.
- Recovery fixtures now set unequal weights for every item while retaining history and RNG, so existing reconnect/migration checks exercise the complete tuned configuration.

Initially checkpointed six-item work at 3c3b0a8 while TS-167 was unavailable. After the user confirmed its merge and requested continuation, merged origin/main e3fd93b into ts-169-jg at 5562917. Resolved only version/documentation conflicts, retaining Machine Gun and Air Control content. Canonical Story version is now 0.1.63; export versions are 0.1.63.0. A final fetch confirmed main remained an ancestor. The pre-existing unrelated grass_variation.png.import modification is preserved outside the Story commits.

## Verification on the integrated seven-item result

Windows, 2026-09-25; .NET 10; Godot 4.7.2 .NET. `<Godot>` below means:
`C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

| Command | Actual result |
| --- | --- |
| `./tools/check-fast.ps1 -Area Core` | PASS: 789 tests, including equal and doubled item weights with unchanged 20,000/10,000/10,000 category counts over 40,000 draws/category scenario. |
| `./check.ps1` | PASS: version/workflow/media checks, Debug production and Release solution builds with zero warnings/errors, 789 Core tests and 358 non-native Client/transport tests. [Log](ts-169/final-check.log). |
| `./check-developer-options.ps1 -GodotPath <Godot> -Visual` | Final PASS: 987 write assertions and 9 separate-process restart assertions. Every catalog key is edited through production UI over UDP; complete host/client configuration/revision, unauthorized-client rejection and persistence are checked. |
| `./check-item-spawns.ps1 -GodotPath <Godot> -Impaired -Visual -NoBuild` | PASS: eight peers, contention/cooldown, full inventory, 32 further pickup rounds/player and a live pool switch after round 16. Subsequent Weapon rolls are Machine Gun; all histories, grants and RNG survive the edit. 30 ms delay, 5 ms jitter, 2% loss. [Log](ts-169/live-weights.log). |
| `./check-reconnect.ps1 -GodotPath <Godot> -NoBuild` | PASS: three native UDP resumes, including 125 seconds offline; complete unequal-weight tuning/history and retained items/world/match state. Machine Gun retains 299/800 rounds and phase 0.25 with no historical shot replay. [Log](ts-169/reconnect.log). |
| `./check-migration.ps1 -GodotPath <Godot> -Players 3 -NoBuild` | PASS: lobby and active-match replacement, sequential epochs, complete tuned configuration, exact category history and RNG continuation, successor-local presets ignored, partial Machine Gun retained. [Log](ts-169/migration.log). |
| `./check-migration.ps1 -GodotPath <Godot> -Players 2 -NoBuild` | PASS: two-peer configuration/history/RNG continuation, lobby replacement and former-host return. |
| `./check-machine-gun.ps1 -GodotPath <Godot> -Impaired -NoBuild` | PASS: host/remote native firing, damage/range/obstruction, released partial magazine and exhaustion through existing lifecycle. Gameplay unchanged by TS-169. |
| `<Godot> --headless --path . --quit-after 60` | PASS: exit 0, no warnings/errors. |
| `./tools/sync-story-version.ps1`; `./tools/check-version.ps1`; `git diff --check` | PASS: 0.1.63 / 0.1.63.0, main ancestry, clean diff whitespace. |

Earlier six-item iteration evidence: fast check passed 742 tests; focused added tests passed 4; comprehensive gate passed 743 Core/357 Client tests; UI passed 877/9 assertions; eight-peer pickup, reconnect and two-/three-peer migration passed. These earlier results do not replace the integrated runs above.

## Failures investigated

- Initial sandboxed Client restore failed without useful diagnostics. Authorized elevated runs used installed dependencies successfully.
- First pre-merge pickup run exposed the invalid all-items-in-eight-random-draws assertion; corrected as described above.
- First post-merge UI run completed 987 assertions but failed the diagnostic gate because the newly merged MachineGun.svg lacked its Godot import cache. Ran `<Godot> --headless --editor --path . --import`, successfully, with no error/warning matches in its log.
- A subsequent UI run failed the controller-navigation assertion while desktop observation was attempted. The observation returned `foreground window did not report a process id`; refreshed enumeration showed the runtime had exited. A repeat without desktop observation passed all 987/9 checks. Focus interference is plausible, not proven; no production input code was changed.

## Runtime observations and limitations

VERIFIED: inspected the rendered [seven-item distribution page](ts-169/distribution-weights.png) at 1280x720. The searchable item rows, Machine Gun weight, explanatory text and Apply/Cancel/Reset controls are readable. Category controls remain in the same scrolling page. Production UI automation changed all weights and verified host/client agreement and restart persistence.

VERIFIED: native local UDP exercises live pool changes for eight players and recovery with complete unequal weights. Repeated pickup fixtures clear inventory between rounds; they do not simulate each item's normal consumption. Separate native Machine Gun and existing recovery fixtures cover its use and partial resource preservation. Recovery identities use trusted fixtures, not real EOS authentication.

UNVERIFIED: hands-on mouse/controller tuning and manual driving, multi-PC/Internet EOS, changed-public-IP recovery, extended soak and subjective gameplay balance. Attempted Windows observation did not establish manual interaction. No claim of real-device network acceptance or long-session performance is made. Statistical tests use a fixed portable stream with bounded tolerance; they do not prove every possible seed.

The complete diff was reviewed for scope and ownership: production gameplay remains Client → Core with no Shared layer or new dependency. No item gameplay changes originate in TS-169. Existing host presets remain authoritative until explicitly changed/reset; this Story does not overwrite saved user balance choices.

## Astra Runtime Critique — Story Round 1

Performed after comprehensive integrated verification. Re-executed the rendered eight-peer impaired live-distribution scenario on the completed build; it passed again. [Critique run](ts-169/critique-live-weights.log). Evaluated the actual tuning-page capture and UI/persistence, reconnect and migration observations above; this is an operational assessment, not a source-code review.

**Overall Score: 8.2 / 10**

**Quality Assessment: PASS (>=8.0; <8.0 is FAIL).**

| Material category | Score | Observed basis |
| --- | --- | --- |
| Runtime Functionality | 8.5 | Live item-pool changes produce the expected subsequent grants for every player, with independent category allocation and unchanged committed state. |
| UI / UX | 8.0 | All seven weights are readable/searchable, semantics are explicit, Apply has visible feedback and normal validation/persistence behavior. Category fields require scrolling; manual controller ergonomics remain unverified. |
| Multiplayer / Recovery | 8.2 | Impaired eight-peer agreement and exact configuration/history/RNG recovery; Internet and physical multi-device behavior remain unverified. |
| Runtime Integration / Stability | 8.2 | Final native/UI/recovery runs completed without warnings/errors; an earlier focus-navigation failure was not reproduced in the isolated rerun and its cause remains uncertain. |

No in-scope operational defect was reproduced in the final runs. The score applies to the bounded tuning feature exercised here, not weapon balance or untested Internet behavior. Human acceptance and independent PR engineering review remain outstanding. Per the user's delivery request, the completed evidence and changes are committed/pushed on the existing Story branch; no PR merge is authorized or performed.
