# TS-169 verification — implementation pending dependency decision

## Scope and current changes

Inspected TS-169, parent TS-115, TS-145 and their comments (none), repository/nested AGENTS routing, workflow, standards, critique and affected distribution, items, Developer Options, simulation, networking and recovery documentation. TS-169 has no children.

Fetched origin/main; ts-169-jg started exactly at main 939f7e2. The current registry has six items (Wrench, Missile, Oil, Nitro, Proxy Mine, Salvo). Machine Gun exists on origin/ts-167-jg (inspected tip 5d13324), but is absent from main and this Story. TS-169 requires its weight while excluding adding items. Asked the user whether to integrate the existing dependency or explicitly limit this work to the six current items. No decision received at this checkpoint; no dependency merge performed.

The existing registry-driven selector, category balancing, shared configuration catalog, persistence and recovery already implement relative weights for registered items. Preserved those owners, baseline 2:1:1 categories, equal item defaults, per-player credits and the match RNG. Added Configs explanatory text for relative weights and server-wide future-roll semantics. Added deterministic proportional/default/control/codec coverage and a two-player live-edit/rebind/authority-restoration regression. Extended native eight-peer pickup coverage to change the pool midway through repeated rounds and recovery fixtures to retain unequal item weights. Corrected a pre-existing native harness assertion that incorrectly required every item to appear in just eight random pickups; valid identities and exact replication remain checked, with proportional reachability tested separately.

Updated affected feature documentation. Version synchronization/check passed at 0.1.61 / 0.1.61.0. The unrelated pre-existing grass_variation.png.import modification is preserved and excluded from this work.

## Commands and observed results

Windows, 2026-09-25. .NET 10; Godot executable abbreviated `<Godot>` below:
`C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

| Command | Actual result |
| --- | --- |
| `./tools/check-fast.ps1 -Area Core` | PASS, 742 tests at first iteration. |
| `dotnet test code/Tests/Trackstorm.Core.Tests.csproj -c Release --filter 'FullyQualifiedName~ItemWeightTests\|FullyQualifiedName~LiveItemWeights'` | PASS, 4 focused tests after live-edit regression. |
| `./check.ps1` | PASS: workflow/version/media checks, Debug production and Release solution builds with zero warnings/errors, 743 Core tests and 357 non-native Client/transport tests. |
| `./check-developer-options.ps1 -GodotPath <Godot> -Visual` | PASS: initial 876 write/9 restart assertions; final capture run 877 write/9 restart assertions. Actual controls for every registered item/category changed over UDP, client mutation rejected, full configuration and persistence checked. |
| `./check-item-spawns.ps1 -GodotPath <Godot> -Impaired -Visual` | First run exposed the invalid all-items-in-eight-rolls assertion. Corrected run PASS: eight peers, contention, cooldown, held grants, 32 further rounds/player, live pool change after round 16, exact replicated history and 2:1:1 convergence. Impairment: 30 ms delay, 5 ms jitter, 2% loss. |
| `./check-reconnect.ps1 -GodotPath <Godot> -NoBuild` | PASS: three native UDP resumes including 125 seconds offline, full configuration with unequal weights, exact category history, held items, world state and match lifecycle. |
| `./check-migration.ps1 -GodotPath <Godot> -Players 3 -NoBuild` | PASS: lobby and active-match authority replacement, sequential epochs, complete unequal-weight configuration, exact history and RNG continuation, successor-local preset ignored. |
| `./check-migration.ps1 -GodotPath <Godot> -Players 2 -NoBuild` | PASS: two-peer continuation including configuration/history/RNG recovery and former-host return. |
| `<Godot> --headless --path . --quit-after 60` | PASS, exit 0, no warnings/errors. |
| `./tools/sync-story-version.ps1`; `./tools/check-version.ps1`; `git diff --check` | PASS. |

Initial sandboxed Client builds exited unsuccessfully during restore with no reported warnings/errors. Authorized elevated runs used the installed tools successfully; no source correction was needed for that environment restriction.

## Runtime evidence and limits

VERIFIED: rendered production Configs search for `weight` was captured and visually inspected at `.godot/developer-options-checks/50ef4c18ea384fe0917307f3cf620bc8/distribution-weights.png`. Six item rows, explanatory text, category section and staged Apply actions are readable at 1280x720; lower category controls are reached by scrolling. Programmatic production UI operation, rather than manual mouse/keyboard play, supplied interaction evidence.

VERIFIED: pickup run `.godot/spawn-checks/44245f18e2f34fc480b55c5c13c7e556` exercises live pool changes for all eight players. The repeated-round fixture clears inventory between rounds; it does not exercise each item's use behavior. Recovery outputs remain under `.godot/reconnect-checks` and `.godot/migration-checks/{2,3}-players`. Recovery identity is a trusted test seam, not real EOS authentication.

UNVERIFIED: Machine Gun's required seventh control and 2/1/1 three-weapon distribution on this branch; multi-PC/Internet EOS, changed public IP, extended soak, manual gameplay balance and controller ergonomics. Generic registry coverage will include Machine Gun once its implementation is present, but that is an inference, not executed evidence.

## Critique and delivery status

Formal Astra Story critique/score is pending the dependency decision and completion of the assigned seven-item scope. No passing score is claimed for incomplete scope. Comprehensive verification above covers the current six-item result; changes after dependency integration require applicable re-verification before critique.

The user subsequently confirmed that Machine Gun is still being completed and explicitly requested a checkpoint push of the current work. This checkpoint is incomplete Story work, not final acceptance: Machine Gun verification, formal Astra critique and independent engineering review remain outstanding. No merge is authorized. The unrelated grass import modification is excluded from the checkpoint commit.
