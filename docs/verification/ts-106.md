# TS-106 — Circus Game Loop and final results

## Implemented

- Inspected TS-106, parent TS-101, and completed blockers TS-93, TS-102 and TS-103. TS-106 has no children or comments. No requirement changes were introduced.
- Explicit Circus / FirstToTarget mode selection uses the existing host configuration catalog; Circus remains default. Selection may change in Lobby/Waiting, but Countdown/Active/Finished reject a mode change atomically.
- Circus combat/streak/stunt scoring requires Circus mode and authoritative Active phase. Both modes retain the configured kill-target completion rule and existing GameLoopState transition. No parallel lifecycle, new end-condition policy, duplicate ranking/checkpoint owner or scene navigation was added.
- Mode identity travels through existing publications, admission, reconnect and migration. Match codec is version 5; tuning codec is version 4 with 76 keys. Checkpoints reject mismatched configuration/mode and historical award deltas.
- Finished retains its exact match object, result and completion tick during subsequent scoring-tuning changes. Podium shows frozen Circus score using the shared formatter; compact layouts retain readable fractional totals.
- Added deterministic integration coverage and extended native mode-selection, post-match and migration scenarios. Fresh rematches reset points, K/D/streak, pending stunts and consumed-outcome memory through existing Loader/Sync reconstruction.

## Verification performed

Windows; .NET 10; Godot 4.7.2 .NET; NVIDIA RTX 4070 Ti, OpenGL compatibility rendering. Branch `ts-106-jg` synchronized with fetched main `eb9faa8`. Canonical version `0.1.23`; tracked export version `0.1.23.0`.

- Targeted Core: 187/187 passed (`Matches`, `Development`, `ResumeCheckpoint`).
- Targeted Client: 39/39 passed (`CombatHudTests`, `MatchStandingsTests`, `PostMatchContextTests`, `VehicleNetworkDriverTests`).
- `./check.ps1`: PASS. Debug production graph and Release solution built with zero warnings/errors. Core 504/504; non-native Client/transport 338/338. Also passed 108 version checks, workflow helper regressions, frontend verifier regressions and five materialized-media checks.
- `tools/sync-story-version.ps1` and `tools/check-version.ps1`: PASS.
- Main scene: Godot `--headless --path <repo> --quit-after 180 -- --local-practice`: PASS without errors/warnings; `.godot/ts-106-main-smoke.log`.
- Complete source diff reviewed for ownership, restoration propagation, scope, dependencies and generated output. `git diff --check` passed. Core remains engine-independent; no new dependencies/assets or Shared layer.

## Native runtime evidence

All scripts used the installed Godot 4.7.2 console executable. `-NoBuild` reused validated Debug assemblies where supported.

| Command | Directly observed result |
| --- | --- |
| `check-match.ps1 -NoBuild -Impaired -Visual` | PASS: eight UDP peers; nonlethal ram points; alternating real missile/ram deaths through configured completion; one Finished notification; sixth death/respawn leaves results frozen. |
| `check-stunts.ps1 -NoBuild -Impaired` | PASS: native drift escalation, speed accrual/banking, three jump distances, concurrent categories, airborne death cancellation, retained banked points across two peers. |
| `check-post-match.ps1 -NoBuild -Visual` | PASS: 138 assertions on the final score-column layout. Seeded prior Circus state completes through an authoritative lethal event; pending stunts cancel; two immediate rematches clear all Circus state; navigation, offline retention, eight/ten-row layouts and paging exercised. |
| `check-reconnect.ps1 -NoBuild -Visual` | PASS: three real UDP arena resyncs, 125 seconds offline, unchanged Circus totals/streak/multiplier inputs/watermarks, native vehicle reuse, prediction reset, offline standings, Finished and next-generation reset. |
| `check-migration.ps1 -NoBuild -Players 2` | PASS: Countdown/deadline continuation, sequential epochs, retained native vehicles/configuration and item state. |
| `check-migration.ps1 -NoBuild -Players 3` | PASS: exact selected revision restores seeded Circus score/K/D/streak and native-progressed pending flight, without historical award replay. |
| `check-migration-processes.ps1 -NoBuild -Players 2` | PASS: host process terminated; independent survivor resumed epoch 2. |
| `check-migration-processes.ps1 -NoBuild -Players 3` | PASS after harness teardown correction: killed host; both independent survivors resumed epoch 2; clean native shutdown. |
| `check-developer-options.ps1` | PASS: 494 write-phase and 8 separate-process read assertions; Lobby mode selection, Active mode-change rejection, persistence and reliable configuration. |
| `check-standings.ps1 -NoBuild -Visual` | PASS: eight UDP sessions, keyboard/controller hold/release, host ping, changing ranks/HUD, frozen results, five rendered sizes, Return cleanup. |
| `check-lobby.ps1 -NoBuild` | PASS: eight sessions, two Finished/Return cycles, active fresh admission, interrupted bootstrap cleanup, retained-slot capacity rejection and lifecycle teardown. |

Local evidence artifacts (ignored, not committed generated files):

- Combat: `.godot/match-checks/d7908525422740458206593437e1c4d5/`
- Stunts: `.godot/stunt-checks/c3e342c7c96f4bf6a4b19a9836e3a575/`
- Final rendered Podium: `.godot/post-match-checks/2003ed057a7e44cb9700fb0d9b06dd60/`
- Developer Options: `.godot/developer-options-checks/f60aaae22b014a258898c1c638cf624f/`
- Lobby: `.godot/lobby-checks/b3c5f53333c8486fa2a83c032faadfb1/`
- Standings/reconnect: `.godot/standings-checks/`, `.godot/reconnect-checks/`
- In-process migration: `.godot/migration-checks/2-players/runtime.log`, `.godot/migration-checks/3-players/runtime.log`
- Process migration: `.godot/migration-process-checks/9dfc3152967a4879a95def543635ae04/` (two), `.godot/migration-process-checks/f57ef5a461364fd6ac079e3647b31488/` (three).

## Iteration findings and corrections

- Sandbox access initially prevented Godot SDK/NuGet and native user-data/log/certificate access. Checks reran with installed-runtime access; blocked attempts are not passing evidence.
- Stricter recovery validation exposed historical awards in a test fixture; it now constructs the event-free boundary used by production.
- The first post-match run emitted a pickup clock-rewind error from its partial-restore fixture. It now restores the final match boundary without rewinding unrelated world owners; subsequent rendered runs passed.
- Screenshot inspection caught truncated large scores and a heading at 640×360. Wider compact columns were re-rendered and directly inspected: `1234567.89` and `DEATHS` fit.
- The Developer Options command was once called with unsupported `-NoBuild`; the corrected command passed.
- Three-process migration twice completed its migration assertions but emitted native audio resource-leak diagnostics at shutdown. The harness now frees/releases its arena and waits one second on the native clock instead of six render frames before quitting. The corrected scenario passed without warnings/errors. Production audio behavior was not changed. A second corrected three-process run and a corrected two-process run also passed; Release Client rebuilt with zero warnings/errors. Repeat evidence: `.godot/migration-process-checks/f90658a562de4fd59d16ec09ae36d284/` (three) and `.godot/migration-process-checks/e5ca61387b9c48e9bb56f3869b871e95/` (two).

## Assumptions and explicitly unverified areas

- Existing configured kill target remains the completion policy. The existing ranking contract places its recorded winner first, then uses Core Circus-score/kills/deaths/identity order. No timed or score-target rule was invented.
- Local UDP and process-loss harnesses use trusted identity/retirement seams. Real authenticated multi-PC EOS reconnect/migration, service ownership/lease timing, changed-network restart and Internet/long-session bandwidth remain UNVERIFIED.
- Post-match seeds prior statistics/pending state then applies authoritative Core lethal damage. Solo Quit and eight/ten-row layout cases remain explicit fixtures. Separate combat/stunt harnesses exercise native projectiles, collisions and stunt observations.
- Synthetic controller events do not verify physical-controller ergonomics. Audio quality, human balance/fun and arbitrary-terrain stunt behavior were not evaluated.

## Astra Runtime Critique — Story Round 1

Independent Astra agent; no files edited by the reviewer.

**Overall Score: 8.3 / 10**

**Quality Assessment: PASS (>=8.0)**

| Category | Score |
| --- | --- |
| Runtime Functionality | 8.6 |
| Runtime Integration | 8.5 |
| Multiplayer / Networking Experience | 8.2 |
| UI / UX | 8.3 |
| Visual Quality — results presentation | 8.1 |
| Runtime Stability | 8.5 |

### What was exercised

- **VERIFIED:** Independently reran rendered native post-match flow: 138 assertions passed without warnings/errors. Both peers received matching frozen Circus results; repeated rematches reconstructed fresh state; lobby/menu return and controlled exit completed. Native harness exercised synthetic keyboard/controller navigation, overlays, focus restoration, paging and offline retained results.
- **VERIFIED:** Independently reran eight-peer impaired native combat. Nonlethal ram points and alternating missile/collision deaths produced consistent score, streak, lifecycle and winner state, including a further death/respawn after completion. No runtime diagnostics.
- **VERIFIED:** Inspected Podium captures at 640×360 and 1280×720, including large fractional scores, long names, offline rows and paging. `1234567.89` and column headings remained readable; winner hierarchy and focused actions were clear, without overlapping controls.
- **INFERRED from recorded verification:** Longer reconnect retention, stunt continuation, mode selection and independent-process migration support the broader assessment. Reviewer inspected epoch-2 evidence for two/three-process survivors but did not independently rerun those scenarios.

The exercised result gives Circus a coherent completion and replay flow. Frozen results remain understandable at small sizes, repeated transitions do not carry old scores forward, and impaired local peers agree on scoring and completion. No material runtime defect was exposed by the critique scenarios.

### Runtime / operational limitations

Post-match uses seeded prior scores/pending state before real authoritative lethal completion and synthetic navigation; it was not a manually driven multiplayer play session. Authenticated multi-PC EOS, Internet conditions, physical-controller ergonomics, audio quality, competitive balance/fun, arbitrary-terrain stunts and sustained performance remain **UNVERIFIED** and were not scored.

Independent artifacts: `.godot/post-match-checks/2122d1f8e35b4e19b48ffc6bbce11bdc/`, `.godot/match-checks/80572e0ab3094b079a581448c01c423d/`.

No corrective round was undertaken. Human acceptance and independent engineering/PR review remain separate from this runtime score.
