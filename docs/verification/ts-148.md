# TS-148 — Arcing Salvo and owner-local impact marker

**Current evidence: Story Round 3 below supersedes automatic volley firing with five individually pressed shots.** Rounds 1 and 2 remain historical evidence; their scheduled-projectile and cadence descriptions are not the current contract.

## Scope and implementation

Inspected Jira TS-148 and parent TS-115, their descriptions/comments (no required child tasks), applicable agent instructions, workflow/standards/critique, and relevant architecture, item, networking, configuration, HUD and recovery documentation. The Story branch is `ts-148-jg`. Refreshed and merged current `main` at `927520c`, including TS-147 Proxy Mine and swept pickup acquisition. Story version is `0.1.56`.

- Registered Salvo as identity 6 through the existing inventory, category allocation, HUD and tuning paths. Proxy Mine retains identity 5. Added an original SVG with provenance; existing rocket, particle and audio assets are reused.
- Default use atomically reserves five unique rounds. Launches are three metres above the current vehicle, six ticks apart, toward the host-selected point 65 metres forward at the original firing boundary. No manual target-selection packet exists.
- The parabola rises twelve metres above its chord, with approximate mean speed 85 m/s. Host segment sweeps resolve impacts. Each blast has seven-metre radius, 65 maximum HP damage, distance falloff and at most 3,500 N s impulse.
- Only the local owner gets the terrain-sampled circular guide. It previews selected Salvo and retains committed flight targets until their rounds end. Marker identity, geometry and visibility are never serialized.
- Runtime controls cover count, cadence, range, launch height, arc, speed, blast radius, damage/falloff, impulse and marker scale/width/lift. The combined catalog has 131 keys; configuration protocol is 16 and item protocol is 9. Exact game-version admission remains required.
- Existing authoritative health transactions produce distinct attributed damage outcomes for each target/round. Existing lethal-hit attribution recognizes Salvo. No TS-148-specific Circus damage scoring was added; TS-155 owns that shared path.
- Existing checkpoints retain scheduled and flying rounds, target, origin, progress, owner life and IDs. Reset/death/departure and match completion clear applicable rounds. Straight Missile keeps its existing motion and damage settings.

## Targeted verification and corrections

`./tools/check-fast.ps1 -Area Core` passed **688 tests** after current-main integration. New tests cover five timed launches/impacts, fixed aim despite steering, moving launch origins, default-speed authority replacement through final outcomes, unique tokens on reuse, capacity/missing-ground rejection, atomic invalid collision handling, multiple targets/falloff, reset/departure and tuning validation/round-trip.

Earlier iteration failures were corrected and retested: token allocation, an outdated distribution expectation, a test-only Proxy Mine enum typo, missing import of the newly merged Proxy Mine icon, and native fixture shutdown resource leaks. The PC interruption corrupted a generated reference assembly; a clean single-worker rebuild passed. Interactive fixture focus initially stayed inactive despite the window having focus; sampling actual focus in play mode enabled confirmed mouse-driven use. These failed attempts are not counted as successful verification. The full gate was rerun after the final fixture correction.

## Final verification

Environment: Windows, Godot **4.7.2 .NET**, .NET **10**, native GameNetworkingSockets; rendered checks used the Compatibility renderer on an RTX 4070 Laptop GPU. Commands ran from the repository root. `$godot` below denotes the local `Godot_v4.7.2-stable_mono_win64_console.exe`. Runtime scripts reject errors/warnings and missing completion sentinels.

| Command | Actual result |
| --- | --- |
| `./check.ps1` | PASS: Debug production and Release solution builds, zero warnings/errors; **688 Core + 355 non-native transport/Client tests**. Version rules (108), workflow helpers and frontend media checks passed. [Final log](ts-148/ts148-final-check.log). |
| `./tools/check-version.ps1`; `git diff --check` | PASS: current-main ancestry, canonical `0.1.56`, both export fields `0.1.56.0`; no whitespace errors. |
| `./check-salvo.ps1 -GodotPath $godot -NoBuild -Visual` | PASS: three native UDP peers, three salvos, 15 matching ordered launches/impacts, two damage targets, second-slot preservation, switch-away/back and marker privacy/cleanup. Both host and remote firing. [Rendered-run log](ts-148/ts148-final-salvo-visual.log). |
| `./check-salvo.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: same scenarios at 30 ms outbound delay, 5 ms jitter, 2% loss; clean shutdown. [Impaired log](ts-148/ts148-final-salvo-impaired.log). |
| `./check-items.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: eight-peer Wrench/Straight Missile path; vehicle, prop, container and boundary impacts. |
| `./check-mine.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: flat/banked/uneven seating, attraction, moving/stationary targets, drive-over, late join and repeated use. |
| `./check-oil.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: native placement, entry/re-entry, replication and configured bound. |
| `./check-nitro.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: rocket-force scenarios, partial release/reuse, overspeed recovery/scoring and exhaustion. |
| `./check-reconnect.ps1 -GodotPath $godot -NoBuild` | PASS: three resyncs including 125 seconds offline; exact five-round state and empty historical events at ticks 7958, 7989 and 8049. Existing Mine/Nitro/inventory/category/match and new-generation checks passed. [Log](ts-148/ts148-final-reconnect.log). |
| `./check-migration.ps1 -GodotPath $godot -NoBuild -Players 2` | PASS: exact five-round continuation at tick 129; native authority replacement and complete-state checks. [Log](ts-148/ts148-final-migration-2.log). |
| `./check-migration.ps1 -GodotPath $godot -NoBuild -Players 3` | PASS: exact five-round continuation at tick 307; sequential epochs and Mine/Nitro/category continuation. [Log](ts-148/ts148-final-migration-3.log). |
| `./check-developer-options.ps1 -GodotPath $godot` | PASS: write/apply/replication phase **809 assertions**; separate-process persistence read **9 assertions**, covering the current catalog. |
| `./check-hud.ps1 -GodotPath $godot -NoBuild` | PASS: item identities including Salvo/Proxy Mine, resources/slots, units, nine resolutions and teardown visibility. |
| `./check-pickup-drive.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: original inventory regression and **180/180** native motion crossings on the production oval. |
| `$godot --headless --path . res://scenes/verification/pickup_drive_checks.tscn -- --pickup-impaired` | PASS after expanding the fixture to the current registry: both production application peers acquired **all six items**, including Salvo; owner marker visible, other peer marker absent; two slots, full rejection and use/retry. [Log](ts-148/ts148-final-salvo-pickup.log). |
| `./check-transport.ps1 -GodotPath $godot` | PASS: **366 tests**, including native cases; three Godot create/poll/message/cleanup cycles. |
| `$godot --headless --path . --quit-after 120` | PASS: main scene startup, exit 0, no runtime diagnostics. |

Other raw logs remain locally under `.godot/ts148-final-*.log`. The archived logs and images are historical evidence, excluded from Godot importing by the verification directory's `.gdignore`.

## Runtime and active playtest

The rendered three-peer fixture uses a controlled bank in the prototype arena with production vehicle bodies, chase camera, HUD and networking. Both ring edges follow the slope; assertions measured more than one metre of vertical variation. Every peer received identical ordered launch/impact events, while only the owner's viewport had a guide. The production-oval acquisition fixture independently verified owner-only marker visibility after actual driving pickups.

Viewed final [owner aiming](ts-148/owner-aim.png), [in-flight salvo](ts-148/owner-arc.png) and [remote viewport](ts-148/remote-no-marker.png) captures. The gold ring is readable at the distant point from the normal 1280x720 chase view. Launch height, rising rockets/trails, descent and repeated impacts provide distinct firing feedback. The flight's highest portion can pass near/behind the existing top HUD; target and impact area remain visible.

Launched `$godot --path . --resolution 1280x720 res://scenes/verification/salvo_checks.tscn -- --salvo-play` and used native computer automation to click the normal LMB fire action twice after the automatic trials. Confirmed two `UseItem` edges in the [interactive log](ts-148/ts148-final-play.log), visible rockets, consumed/automatically regranted fixture inventory, and target displacement following the first volley. The second volley also visibly launched. The initial attempt before the focus correction is not successful manual evidence. Injected E did not establish manual slot switching; automated native switching checks passed. No subjective audio claim is made.

## Astra Runtime Critique — Story Round 1

**Overall Score: 8.0 / 10**
**Quality Assessment: PASS (>=8.0)**

| Category | Score | Observed basis |
| --- | --- | --- |
| Runtime functionality/integration | 8.5 | Repeated host/client salvos, distinct impacts on multiple targets, pickup/slots and cleanup behaved consistently. |
| Multiplayer/networking experience | 8.5 | Native impaired peers agreed; owner marker remained private; exact recovery installed without replay. |
| Camera/visual readability | 8.0 | Bright ground ring, distinct icon and elevated flight read clearly from the chase view; the apex approaches the existing upper HUD. |
| Motion/feedback and physics | 8.0 | Rapid launches, visible trajectories/impacts and bounded target displacement; no instability observed in exercised scenes. |

**What was exercised — VERIFIED:** the final native/rendered scenarios above, production-map pickup privacy, and two confirmed mouse-triggered volleys. The score reflects the scoped observed outcome, with limited competitive play and manual keyboard coverage; it is not an engineering-review score. No Internet/EOS result is inferred from these local checks.

**Runtime/operational limitations — UNVERIFIED:** authenticated EOS and separate machines, exported builds, extended competitive balance, subjective audio, sustained performance profiling, and a long manually driven production-map combat session. Keyboard automation did not confirm E switching or sustained driving. Recovery fixtures deliberately seed slow valid rounds to isolate snapshot installation; Core tests separately continue default-speed flights after replacement. No further critique round or corrective scope is authorized by this score.

## Assumptions, risks and delivery

- The marker denotes the intended fixed target; intervening obstacles may cause earlier impact. Missing target terrain or the shared projectile cap retains the item. Unsupported marker segments are omitted rather than drawn across empty space.
- Count/target/cadence/arc are captured at use; pending launches read current launch height/speed, while impact/marker properties use current tuning. These semantics are documented in the feature guide.
- Shared item-damage Circus scoring remains TS-155 scope; attributed damage outcomes are available to that shared path.
- No new external dependency or asset was acquired. Core remains engine-independent with Client-to-Core dependency direction and no Shared layer.
- The pre-existing `assets/maps/oval/grass_variation.png.import` edit belongs to the user and is excluded from Story commits.
- Implementation commits `c5555e4` and `4342d6d` precede final verification/delivery metadata. Final commit/push and PR status are reported in the handoff; human merge remains separate.

## Round 2 — approved pacing and moving aim

The user requested one missile every half second, a target that moves with the car, and adjustable developer settings. Jira TS-148's description was updated to preserve that approved change. Five rounds now launch at 30-tick intervals (0.5 seconds at 60 Hz; two seconds from first to fifth). Each pending round samples the current host-observed position and heading at its launch. Airborne rounds retain their committed trajectories. The single owner-local terrain guide follows current forward aim throughout the volley. There is still no free/manual target selection or shared marker state.

The existing **F1 → Configs → Salvo** section exposes count, cadence, range, launch height, arc, speed, blast radius, damage/falloff, knockback and marker properties. The cadence label explicitly explains `30 = 0.5 seconds`. Existing saved overrides remain authoritative; the standard local settings file had no saved cadence override when inspected. No protocol/catalog changes or TS-148-specific Circus scoring were introduced.

### Round 2 targeted and final verification

Commands used the same Windows/Godot/native environment described above. All listed results are from this round. Final `main` fetch still resolved to `927520c`; ancestry and version checks passed at `0.1.56` / export `0.1.56.0`.

| Command | Actual result |
| --- | --- |
| `./tools/check-fast.ps1 -Area Core` | PASS **689 tests**; per-launch moving aim, unchanged airborne targets, exact 30-tick cadence, moving-owner authority replacement and missing-ground cancellation. [Log](ts-148/round-2/ts148-r2-core.log). |
| `./check-salvo.ps1 -GodotPath $godot -Visual` | PASS; three peers, three salvos, 15 matching impacts, exact half-second launches, multiple stationary damage targets and a driving/steering remote shooter. One marker followed its owner's current aim and remained absent on other peers. [Log](ts-148/round-2/ts148-r2-salvo-targeted.log). |
| `./check.ps1` | PASS; Debug production/Release solution builds, zero warnings/errors, **689 Core + 355 non-native Client/transport tests**, version/workflow/media gates. [Log](ts-148/round-2/ts148-r2-final-check.log). |
| `./check-salvo.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS; same revised cadence, moving aim and privacy assertions with 30 ms delay, 5 ms jitter and 2% loss. [Log](ts-148/round-2/ts148-r2-salvo-impaired.log). |
| `./check-developer-options.ps1 -GodotPath $godot` | PASS; **809 write/apply/replication assertions**, **9 separate-process persistence assertions**. [Log](ts-148/round-2/ts148-r2-options.log). |
| `./check-reconnect.ps1 -GodotPath $godot -NoBuild` | PASS; three resyncs including 125 seconds offline; exact five-round snapshots without historical events at ticks 7958, 7989 and 8049. [Log](ts-148/round-2/ts148-r2-reconnect.log). |
| `./check-migration.ps1 -GodotPath $godot -NoBuild -Players 2` | PASS; exact Salvo continuation at tick 130 and native authority replacement. [Log](ts-148/round-2/ts148-r2-migration-2.log). |
| `./check-migration.ps1 -GodotPath $godot -NoBuild -Players 3` | PASS; exact Salvo continuation at tick 320 and sequential epochs. [Log](ts-148/round-2/ts148-r2-migration-3.log). |
| `$godot --headless --path . --quit-after 120` | PASS; exit 0, no runtime diagnostics. [Log](ts-148/round-2/ts148-r2-startup.log). |
| `./tools/check-version.ps1`; `git diff --check` | PASS. |

No affected-check failures occurred this round. Round 1's separate Mine/Oil/Nitro/HUD/pickup/native transport suites were not rerun as separate scripts this round; their earlier results remain historical. The comprehensive gate and native scenarios above are the current evidence. Archived log trailing whitespace was trimmed; contents/results were otherwise preserved.

### Round 2 active playtest and critique

After final verification, launched `$godot --path . --resolution 1280x720 res://scenes/verification/salvo_checks.tscn -- --salvo-play`. Its automatic three-peer trials passed again. Used native window automation to fire **two** volleys through normal LMB input: two `Interactive input: UseItem` entries, visible rising projectiles/trails, consumed then fixture-regranted inventory and visible target displacement. Closed only the playtest window; process exited 0. [Interactive log](ts-148/round-2/ts148-r2-play.log).

Viewed the [moving aim capture](ts-148/round-2/moving-aim.png) and [arc capture](ts-148/round-2/owner-arc.png). The moving-car guide followed the changed heading while earlier impacts stayed behind. Driving/steering in that scenario used the normal logical input path under the native fixture; it was not a manually keyboard-driven combat session. Exact cadence is measured by the fixture, not by a manual stopwatch.

#### Astra Runtime Critique — Story Round 2

**Overall Score: 8.2 / 10**

**Quality Assessment: PASS (>=8.0)**

| Category | Score | Observed basis |
| --- | --- | --- |
| Runtime functionality/integration | 8.5 | Repeated mouse-triggered firing, five separated launches, moving launch targets and preserved second slot. |
| Multiplayer/networking experience | 8.5 | Peers agreed on distinct impacts with impairment; marker remained private; reconnect and authority replacement retained pending/flying rounds. |
| Camera/visual readability | 8.0 | One readable guide follows current aim without old locked rings; elevated flight is distinct, although the apex still approaches the upper HUD. |
| Motion/feedback and pacing | 8.2 | Spaced launches communicate a sustained salvo; steering changes subsequent impact locations while airborne arcs remain predictable. |

**VERIFIED:** the current native scenarios, rendered observations and two manual mouse-triggered volleys above. The score judges the observed scoped behavior; test counts are supporting evidence, not the score's basis.

**UNVERIFIED:** authenticated EOS/Internet peers, separate machines, exported builds, subjective audio, long competitive balance sessions, sustained performance profiling and manually keyboard-driven production-map combat. Developer configuration apply/replication/persistence was exercised by the native harness; manual editing of every Salvo field was not performed. Recovery fixtures seed slow valid rounds to inspect snapshot installation; Core tests separately continue default-speed rounds with moving aim after authority replacement.

**Remaining semantics/risks:** if a later launch has no valid terrain at its new forward aim, only that round is canceled rather than firing toward an obsolete point. Count/cadence/arc are captured at use; pending launches read current range/height/speed. The following marker previews current forward aim, not the destination of already airborne rounds; intervening obstacles can still produce an earlier impact. Saved cadence overrides survive the new default. The unrelated grass import edit remains excluded from delivery. No additional critique round is authorized by this score.

## Round 3 — five individually fired shots

The user requested individual control of all five missiles. Jira TS-148 now records one missile per separate press, five shots per pickup by default, retained ammunition/HUD count, and the existing adjustable half-second minimum interval. Holding does not repeat; an early press is discarded without consuming ammunition or queuing a later shot.

Each physical inventory slot now retains its own ammunition and cooldown ready tick. The fifth accepted shot empties that slot. Each shot samples the current vehicle aim and retires its use capability, preventing replay from spending another shot. Capability rotation suppresses a false pickup cue while retaining distinct fire/impact identities. Host authority still owns launch, damage and knockback; no scoring path was added. Item protocol **10** serializes ammunition/cooldowns and flying arcs; the obsolete automatic launch-delay state was removed. Configuration protocol remains **16**. Both peers must use the updated build.

**F1 → Configs → Salvo** now labels the existing keys **Shots per pickup** and **Shot cooldown (ticks; 30 = 0.5 seconds)**. Count changes affect new pickups; held shots are not refilled. Cooldown is captured per accepted shot. Existing trajectory, damage, knockback and marker controls remain available. Current forward aim and the private terrain guide are preserved.

### Round 3 targeted checks

- `./tools/check-fast.ps1 -Area Core`: PASS **691 tests**, including separate presses, held/idle non-repeat, cooldown rejection, retired capabilities, independent partial slots, default-speed authority replacement, codec validation, missing terrain/capacity, atomic failure, exhaustion, damage and tuning. [Log](ts-148/round-3/ts148-r3-core.log).
- `./tools/check-fast.ps1 -Area Client`: PASS Debug build, zero warnings/errors. [Log](ts-148/round-3/ts148-r3-client.log).
- `./tools/check-fast.ps1 -Area Transport`: PASS **357 tests**. Two iteration failures were corrected: a test referenced nonexistent `MissilePickup` instead of `WeaponPickup`, and the existing generic HUD test expected `SALVO` instead of `SALVO 5`. These failed attempts are not counted as passing evidence. [Successful retest](ts-148/round-3/ts148-r3-transport.log).
- `./check-salvo.ps1 -GodotPath $godot -NoBuild -Visual`: PASS three native peers, three magazines, 15 separately requested launches/impacts, held-input non-repeat, replicated ammunition, moving aim, multiple stationary targets and marker privacy. The controlled bank was enlarged to accommodate the longer individually fired driving scenario. [Log](ts-148/round-3/ts148-r3-salvo-targeted.log).

### Round 3 comprehensive final verification

No production changes followed this gate. A verification-only capture condition initially left an old moving-aim image on disk; the capture was corrected, then the full gate and rendered Salvo check were rerun successfully. The archived moving-aim image is from that new run ([rendered rerun log](ts-148/round-3/ts148-r3-salvo-final-visual.log)). Environment remains Windows, Godot 4.7.2 .NET, .NET 10 and native GameNetworkingSockets. Current main remained `927520c`; ancestry, canonical `0.1.56` and export `0.1.56.0` checks passed. Native checks ran sequentially.

| Command | Actual result |
| --- | --- |
| `./check.ps1` | PASS: Debug production/Release solution builds, zero warnings/errors, **691 Core + 357 Client/transport tests**, version/workflow/media gates. [Log](ts-148/round-3/ts148-r3-final-check.log). |
| `./check-salvo.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: individual input edges, 15 matching impacts, minimum cooldown, partial ammunition and private marker under 30 ms delay, 5 ms jitter and 2% loss. [Log](ts-148/round-3/ts148-r3-salvo-impaired.log). |
| `./check-reconnect.ps1 -GodotPath $godot -NoBuild` | PASS: three resyncs including 125 seconds offline; exact three held shots, cooldown and two flying rounds at ticks 7958, 7989 and 8049, without historical effects. [Log](ts-148/round-3/ts148-r3-reconnect.log). |
| `./check-migration.ps1 -GodotPath $godot -NoBuild -Players 2` | PASS: exact partial-ammunition/flight continuation at tick 128; native authority replacement. [Log](ts-148/round-3/ts148-r3-migration-2.log). |
| `./check-migration.ps1 -GodotPath $godot -NoBuild -Players 3` | PASS: exact partial-ammunition/flight continuation at tick 320; sequential epochs and surrounding item/match continuation. [Log](ts-148/round-3/ts148-r3-migration-3.log). |
| `./check-hud.ps1 -GodotPath $godot -NoBuild` | PASS: new Salvo count/depletion and independent-slot labels, existing state/unit/layout checks across nine resolutions and teardown. [Log](ts-148/round-3/ts148-r3-hud.log). |
| `./check-developer-options.ps1 -GodotPath $godot` | PASS: **809 write/apply/replication + 9 separate-process persistence assertions**. [Log](ts-148/round-3/ts148-r3-options.log). |
| `$godot --headless --path . --quit-after 120` | PASS exit 0; no runtime diagnostics. [Log](ts-148/round-3/ts148-r3-startup.log). |
| `./tools/check-version.ps1`; `git diff --check` | PASS. |

Earlier standalone weapon/transport/pickup checks remain historical; they were not all rerun this round. The current complete machine gate and applicable native checks are listed above. Archived log whitespace is normalized; results are unchanged.

### Round 3 active playtest

After final verification, launched `$godot --path . --resolution 1280x720 res://scenes/verification/salvo_checks.tscn -- --salvo-play`. Automatic three-peer trials passed again. Using native computer automation on the normal LMB action, fired **five separate manual clicks**. Observed the HUD change **5 → 4 → 3 → 2 → 1 → EMPTY**, one launch per click, and four shots remain during an idle pause after the first. The Wrench stayed in slot two. Visible rocket motion, impact effects and target displacement followed the shots. The fixture subsequently regranted five shots for continued testing; this is fixture behavior, not automatic weapon reloading. Closed only the playtest window; process exited 0. [Log with five UseItem edges](ts-148/round-3/ts148-r3-play.log).

Viewed the native [four-shots-held capture](ts-148/round-3/four-shots-held.png); count and terrain guide were readable at 1280×720. Also viewed the refreshed native [moving aim](ts-148/round-3/moving-aim.png). Its driving/steering uses normal logical input under fixture control, not manual keyboard driving. The absence of automatic repeat was exercised by held input in that fixture; the manual mouse actions were separate clicks, not a sustained physical hold. Manual screenshots were observed through computer-use output, not saved as repository images.

#### Astra Runtime Critique — Story Round 3

**Overall Score: 8.2 / 10**

**Quality Assessment: PASS (>=8.0)**

| Category | Score | Observed basis |
| --- | --- | --- |
| Runtime functionality/integration | 8.5 | Each manual click spent one shot, idle preserved ammunition, fifth shot emptied its slot, and the second item remained intact. |
| Controls/feedback | 8.3 | Individual shots responded predictably; visible count and separate fire/impact effects made the remaining weapon capacity clear. |
| Multiplayer/networking experience | 8.5 | Native peers agreed on shots and impacts under impairment; partial inventory/cooldown/flight restored through reconnect and migration; marker remained private. |
| Camera/visual readability | 8.0 | Current ground aim and ammunition were readable from the normal chase view; the elevated arc can still approach the upper HUD. |

**VERIFIED:** current checks, rendered native scenarios and five manual clicks described above. This evaluates observed runtime behavior, not code quality or the volume of tests.

**UNVERIFIED:** authenticated EOS/Internet peers, independent machines, exported builds, subjective audio, sustained performance profiling, extended competitive balance and manually keyboard-driven production-map combat. Audio cue selection was checked deterministically; no listening judgment is claimed. Recovery fixtures seed slow flying rounds and partial ammunition to isolate exact checkpoint installation; Core tests separately continue default-speed flights and further individually requested shots after authority replacement. Configuration persistence was exercised by the native harness, not by manually editing every field.

**Assumptions and remaining risks:** the previously approved 0.5-second cadence now acts as a minimum cooldown between accepted presses. Presses during it are intentionally not buffered. A shot with missing target terrain or no projectile capacity spends no ammunition. The guide previews current aim, not already airborne impact destinations; obstacles can intercept the arc. Saved configuration overrides remain effective. Item protocol 10 rejects earlier layouts. Shared Circus item-damage scoring remains TS-155 scope. The user's unrelated grass import edit is excluded from delivery. Round 3 is the final Story critique round permitted by the current critique policy; this score authorizes no further changes.
