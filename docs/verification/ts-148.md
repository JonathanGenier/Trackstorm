# TS-148 — Arcing Salvo and owner-local impact marker

## Scope and implementation

Inspected Jira TS-148 and parent TS-115, their descriptions/comments (no required child tasks), applicable agent instructions, workflow/standards/critique, and relevant architecture, item, networking, configuration, HUD and recovery documentation. The Story branch is `ts-148-jg`. Refreshed and merged current `main` at `927520c`, including TS-147 Proxy Mine and swept pickup acquisition. Story version is `0.1.56`.

- Registered Salvo as identity 6 through the existing inventory, category allocation, HUD and tuning paths. Proxy Mine retains identity 5. Added an original SVG with provenance; existing rocket, particle and audio assets are reused.
- Default use atomically reserves five unique rounds. Launches are three metres above the current vehicle, six ticks apart, toward the host-selected point 65 metres forward at the original firing boundary. No manual target-selection packet exists.
- The parabola rises twelve metres above its chord, with approximate mean speed 85 m/s. Host segment sweeps resolve impacts. Each blast has seven-metre radius, 65 maximum HP damage, distance falloff and at most 3,500 N s impulse.
- Only the local owner gets the terrain-sampled circular guide. It previews selected Salvo and retains committed flight targets until their rounds end. Marker identity, geometry and visibility are never serialized.
- Runtime controls cover count, cadence, range, launch height, arc, speed, blast radius, damage/falloff, impulse and marker scale/width/lift. The combined catalog has 131 keys; configuration protocol is 16 and item protocol is 9. Exact game-version admission remains required.
- Existing authoritative health transactions produce distinct attributed damage outcomes for each target/round. Existing lethal-hit attribution recognizes Salvo. No TS-148-specific Circus damage scoring was added; TS-155 owns that shared path.
- Existing checkpoints retain scheduled and flying rounds, target, origin, progress, owner life and IDs. Reset/death/departure and match completion clear applicable rounds. Straight Missile keeps its existing motion and damage settings.

## Targeted verification and corrections

`./tools/check-fast.ps1 -Area Core` passed **688 tests** after current-main integration. New tests cover five timed launches/impacts, fixed aim despite steering, moving launch origins, default-speed authority replacement through final outcomes, unique tokens on reuse, capacity/missing-ground rejection, atomic invalid collision handling, multiple targets/falloff, reset/departure and tuning validation/round-trip.

Earlier iteration failures were corrected and retested: token allocation, an outdated distribution expectation, a test-only Proxy Mine enum typo, missing import of the newly merged Proxy Mine icon, and native fixture shutdown resource leaks. The PC interruption corrupted a generated reference assembly; a clean single-worker rebuild passed. Interactive fixture focus initially stayed inactive despite the window having focus; sampling actual focus in play mode enabled confirmed mouse-driven use. These failed attempts are not counted as successful verification. The full gate was rerun after the final fixture correction.

## Final verification

Environment: Windows, Godot **4.7.2 .NET**, .NET **10**, native GameNetworkingSockets; rendered checks used the Compatibility renderer on an RTX 4070 Laptop GPU. Commands ran from the repository root. `$godot` below denotes the local `Godot_v4.7.2-stable_mono_win64_console.exe`. Runtime scripts reject errors/warnings and missing completion sentinels.

| Command | Actual result |
| --- | --- |
| `./check.ps1` | PASS: Debug production and Release solution builds, zero warnings/errors; **688 Core + 355 non-native transport/Client tests**. Version rules (108), workflow helpers and frontend media checks passed. [Final log](ts-148/ts148-final-check.log). |
| `./tools/check-version.ps1`; `git diff --check` | PASS: current-main ancestry, canonical `0.1.56`, both export fields `0.1.56.0`; no whitespace errors. |
| `./check-salvo.ps1 -GodotPath $godot -NoBuild -Visual` | PASS: three native UDP peers, three salvos, 15 matching ordered launches/impacts, two damage targets, second-slot preservation, switch-away/back and marker privacy/cleanup. Both host and remote firing. [Rendered-run log](ts-148/ts148-final-salvo-visual.log). |
| `./check-salvo.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: same scenarios at 30 ms outbound delay, 5 ms jitter, 2% loss; clean shutdown. [Impaired log](ts-148/ts148-final-salvo-impaired.log). |
| `./check-items.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: eight-peer Wrench/Straight Missile path; vehicle, prop, container and boundary impacts. |
| `./check-mine.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: flat/banked/uneven seating, attraction, moving/stationary targets, drive-over, late join and repeated use. |
| `./check-oil.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: native placement, entry/re-entry, replication and configured bound. |
| `./check-nitro.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: rocket-force scenarios, partial release/reuse, overspeed recovery/scoring and exhaustion. |
| `./check-reconnect.ps1 -GodotPath $godot -NoBuild` | PASS: three resyncs including 125 seconds offline; exact five-round state and empty historical events at ticks 7958, 7989 and 8049. Existing Mine/Nitro/inventory/category/match and new-generation checks passed. [Log](ts-148/ts148-final-reconnect.log). |
| `./check-migration.ps1 -GodotPath $godot -NoBuild -Players 2` | PASS: exact five-round continuation at tick 129; native authority replacement and complete-state checks. [Log](ts-148/ts148-final-migration-2.log). |
| `./check-migration.ps1 -GodotPath $godot -NoBuild -Players 3` | PASS: exact five-round continuation at tick 307; sequential epochs and Mine/Nitro/category continuation. [Log](ts-148/ts148-final-migration-3.log). |
| `./check-developer-options.ps1 -GodotPath $godot` | PASS: write/apply/replication phase **809 assertions**; separate-process persistence read **9 assertions**, covering the current catalog. |
| `./check-hud.ps1 -GodotPath $godot -NoBuild` | PASS: item identities including Salvo/Proxy Mine, resources/slots, units, nine resolutions and teardown visibility. |
| `./check-pickup-drive.ps1 -GodotPath $godot -NoBuild -Impaired` | PASS: original inventory regression and **180/180** native motion crossings on the production oval. |
| `$godot --headless --path . res://scenes/verification/pickup_drive_checks.tscn -- --pickup-impaired` | PASS after expanding the fixture to the current registry: both production application peers acquired **all six items**, including Salvo; owner marker visible, other peer marker absent; two slots, full rejection and use/retry. [Log](ts-148/ts148-final-salvo-pickup.log). |
| `./check-transport.ps1 -GodotPath $godot` | PASS: **366 tests**, including native cases; three Godot create/poll/message/cleanup cycles. |
| `$godot --headless --path . --quit-after 120` | PASS: main scene startup, exit 0, no runtime diagnostics. |

Other raw logs remain locally under `.godot/ts148-final-*.log`. The archived logs and images are historical evidence, excluded from Godot importing by the verification directory's `.gdignore`.

## Runtime and active playtest

The rendered three-peer fixture uses a controlled bank in the prototype arena with production vehicle bodies, chase camera, HUD and networking. Both ring edges follow the slope; assertions measured more than one metre of vertical variation. Every peer received identical ordered launch/impact events, while only the owner's viewport had a guide. The production-oval acquisition fixture independently verified owner-only marker visibility after actual driving pickups.

Viewed final [owner aiming](ts-148/owner-aim.png), [in-flight salvo](ts-148/owner-arc.png) and [remote viewport](ts-148/remote-no-marker.png) captures. The gold ring is readable at the distant point from the normal 1280x720 chase view. Launch height, rising rockets/trails, descent and repeated impacts provide distinct firing feedback. The flight's highest portion can pass near/behind the existing top HUD; target and impact area remain visible.

Launched `$godot --path . --resolution 1280x720 res://scenes/verification/salvo_checks.tscn -- --salvo-play` and used native computer automation to click the normal LMB fire action twice after the automatic trials. Confirmed two `UseItem` edges in the [interactive log](ts-148/ts148-final-play.log), visible rockets, consumed/automatically regranted fixture inventory, and target displacement following the first volley. The second volley also visibly launched. The initial attempt before the focus correction is not successful manual evidence. Injected E did not establish manual slot switching; automated native switching checks passed. No subjective audio claim is made.

## Astra Runtime Critique — Story Round 1

**Overall Score: 8.0 / 10**
**Quality Assessment: PASS (>=8.0)**

| Category | Score | Observed basis |
| --- | --- | --- |
| Runtime functionality/integration | 8.5 | Repeated host/client salvos, distinct impacts on multiple targets, pickup/slots and cleanup behaved consistently. |
| Multiplayer/networking experience | 8.5 | Native impaired peers agreed; owner marker remained private; exact recovery installed without replay. |
| Camera/visual readability | 8.0 | Bright ground ring, distinct icon and elevated flight read clearly from the chase view; the apex approaches the existing upper HUD. |
| Motion/feedback and physics | 8.0 | Rapid launches, visible trajectories/impacts and bounded target displacement; no instability observed in exercised scenes. |

**What was exercised — VERIFIED:** the final native/rendered scenarios above, production-map pickup privacy, and two confirmed mouse-triggered volleys. The score reflects the scoped observed outcome, with limited competitive play and manual keyboard coverage; it is not an engineering-review score. No Internet/EOS result is inferred from these local checks.

**Runtime/operational limitations — UNVERIFIED:** authenticated EOS and separate machines, exported builds, extended competitive balance, subjective audio, sustained performance profiling, and a long manually driven production-map combat session. Keyboard automation did not confirm E switching or sustained driving. Recovery fixtures deliberately seed slow valid rounds to isolate snapshot installation; Core tests separately continue default-speed flights after replacement. No further critique round or corrective scope is authorized by this score.

## Assumptions, risks and delivery

- The marker denotes the intended fixed target; intervening obstacles may cause earlier impact. Missing target terrain or the shared projectile cap retains the item. Unsupported marker segments are omitted rather than drawn across empty space.
- Count/target/cadence/arc are captured at use; pending launches read current launch height/speed, while impact/marker properties use current tuning. These semantics are documented in the feature guide.
- Shared item-damage Circus scoring remains TS-155 scope; attributed damage outcomes are available to that shared path.
- No new external dependency or asset was acquired. Core remains engine-independent with Client-to-Core dependency direction and no Shared layer.
- The pre-existing `assets/maps/oval/grass_variation.png.import` edit belongs to the user and is excluded from Story commits.
- Implementation commits `c5555e4` and `4342d6d` precede final verification/delivery metadata. Final commit/push and PR status are reported in the handoff; human merge remains separate.
