# TS-89 — Release 0.1.0 baseline verification

## Scope and source

Jira TS-89 was read in full on 2026-09-19: Story, no subtasks, attachments or comments. The assigned branch is `ts-89-jg`. It was fast-forwarded to current `origin/main` at `7ad77caf49f364ccbf5592d69b5b38cf0ebdd1b0`, canonical **0.0.16**, before implementation. A final fetch confirmed the same main.

The approved source was `C:\Users\j_gen\AppData\Roaming\Godot\app_userdata\Trackstorm\developer-settings.jsonl` (Godot `user://developer-settings.jsonl`), schema 1, all 59 allowlisted gameplay keys, SHA-256 `1D295FF60771A11373F8F0F55ADDD9F0AEC2FB3A150BFD998450399ABD2194B8`. The source was read directly, not reconstructed from UI labels or approximated. Its float-to-double numeric expansions are recorded as exact expectations in `ReleaseDefaultsTests`. No developer-local JSONL is a tracked release asset.

All 59 keys map through the existing `GameplayOptions` allowlist to their owning properties; [Developer Options](../features/developer-options.md#editable-control-to-runtime-mapping) records that complete mapping. Fourteen values differed from the previous hosted preset:

| Owning property | Previous hosted value | Release value |
| --- | --- | --- |
| Vehicle.Acceleration | 11 | 12 |
| Vehicle.StopSpeed | 0.05f | 0.1f |
| Vehicle.ReverseAcceleration | 8 | 10 |
| Vehicle.ForwardSpeed | 28 | 30 |
| Vehicle.SteeringAngle | 0.6f | 0.8f |
| Vehicle.SteeringSpeed | 22 | 30 |
| Vehicle.SteeringResponse | 6 | 8 |
| Vehicle.TireFriction | 1.35f | 3.35f |
| Vehicle.CoastDrag | 0.12f | 1 |
| Vehicle.SuspensionDamping | 8 | 12 |
| Damage.CollisionScale | 3 | 5 |
| Items.MissileSpeed | 45 | 70 |
| Items.ExplosionRadius | 8 | 12 |
| Items.MaximumDamage | 55 | 300 |

The remaining 45 approved values already matched the hosted preset, including 1000 HP, surfaces, respawn, spawning and match rules. **No source value was omitted or unpromotable.** Generic Core/practice configuration records retain their independent defaults; the production hosted preset composes those records with explicit release tuning. Core validation, fixed 60 Hz, persistence schemas, wire schemas and dependency direction remain unchanged.

## Implementation and regression coverage

- The changed release declaration authorizes TS-89 `0.0.16 -> 0.1.0`. `./tools/sync-story-version.ps1 -Kind release` established the canonical value and tracked Windows file/product versions **0.1.0.0**; no direct version-rule bypass was used. A repeated run preserved both file hashes.
- `ReleaseDefaultsTests`: 59 exact numeric expectations, complete preset validation, persistence and configuration-wire round trips.
- `DeveloperOptionsTests`: explicit release-value checks for a fresh absent file, partial saved overrides and Reset + Apply/reload; added malformed-record salvage with an independent override/unknown-key preservation, future-schema and oversized-file protection. Existing save-failure/retry, discard isolation and authoritative validation tests remain.
- `GameVersionTests`: exact runtime **0.1.0**, Join/Resume publication, refusal of 0.0.16, 0.1.1, 0.2.0 and 0.1.0.0 without peer allocation, followed by compatible admission. Existing generated metadata, parser and retained-resume compatibility tests remain.
- `ItemIntegrationChecks`: the missile falloff scenario now starts targets at full configured hosted health instead of 100 HP. The approved 300-damage blast killed both old 100-HP targets, making their post-impact health equal. The assertion now requires both targets to survive, the closer target to have less health, and the farther target to have lost health; no damage expectation was weakened.
- `NetworkVehicleChecks`: after deferred arena frees, wait 100 ms for native mixer cleanup, matching the existing vehicle harness. Verbose diagnostics identified pending audio playback/stream references at shutdown; gameplay assertions and production audio behavior are unchanged.
- No production storage, UI, admission, EOS or compatibility policy implementation changed. Production fresh startup and Reset still use `HostedDefaults`; saved host overrides retain normal precedence.
- Feature documentation updated: game versioning, Developer Options, vehicles, held items and HUD. This report is indexed in the historical verification index.

## Commands and results

All commands ran from the repository root on Windows. `$godot` below denotes exactly:

`C:\Godot\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64\Godot_v4.7.2-stable_mono_win64_console.exe`

.NET commands used `$env:DOTNET_PROCESSOR_COUNT='4'`. Git checks used a process-local `safe.directory` exception for this user-owned checkout. Sandbox access to the user's NuGet configuration and Godot user directory required elevated execution; no repository permission workaround or copied configuration was introduced.

| Exact command | Result and evidence |
| --- | --- |
| `./tools/sync-story-version.ps1 -Kind release` | VERIFIED PASS: 0.0.16 -> 0.1.0; file/product 0.1.0.0; repeated run hash-idempotent. |
| `./tools/check-version.ps1` | VERIFIED PASS after final fetch: main ancestry, changed TS-89 authorization, canonical transition and tracked preset. |
| `./check.ps1` | VERIFIED PASS: 56 version-rule cases; restore/format; Debug and Release builds with zero warnings/errors; 441 Core + 311 non-native Client/transport tests in each configuration. Log: `.godot/ts89-check.log`. |
| `dotnet test code/Tests/Trackstorm.Core.Tests.csproj -c Debug --no-build --filter 'FullyQualifiedName~ReleaseDefaultsTests\|FullyQualifiedName~DeveloperConfigurationTests\|FullyQualifiedName~GameVersionTests'` | VERIFIED PASS: 151 focused tests. Log: `.godot/ts89-focused-core.log`. |
| `dotnet test code/TransportTests/Trackstorm.Transport.Tests.csproj -c Debug --no-build --filter 'FullyQualifiedName~DeveloperOptionsTests\|FullyQualifiedName~OnlineLobbyTests'` | VERIFIED PASS: 119 focused tests. Log: `.godot/ts89-focused-client.log`. |
| `./import-godot.ps1 -GodotPath $godot` | VERIFIED PASS, no warnings/errors. Log: `.godot/ts89-import.log`. |
| `./check-build-version.ps1 -GodotPath $godot` | VERIFIED PASS: native main/bootstrap reports 0.1.0. Log: `.godot/ts89-runtime-version.log`. |
| `./check-developer-options.ps1 -GodotPath $godot` | VERIFIED PASS: 238 write-phase + 7 separate-process read assertions. Actual production UI/UDP, fresh hosted preset, complete Reset staging, Apply synchronization/persistence and restart overrides. Log: `.godot/ts89-developer-options.log`. |
| `./check-online-lobby.ps1 -GodotPath $godot -NoBuild` | VERIFIED PASS: production browser, version mismatch and retained-session UI using a fake provider. Log: `.godot/ts89-online-lobby.log`. |
| `./check-lobby.ps1 -GodotPath $godot -NoBuild` | VERIFIED PASS: eight actual UDP peers, admission/start/return, interrupted bootstrap and retained-slot rejection. Log: `.godot/ts89-lobby.log`. |
| `./check-reconnect.ps1 -GodotPath $godot -NoBuild` | VERIFIED PASS on rerun: three actual UDP arena resyncs including 125 seconds offline, retained native bodies, prediction/history reset, item/spawn/match continuity and new-generation reset; clean exit. Log: `.godot/ts89-reconnect.log`. |
| `./check-items.ps1 -GodotPath $godot -NoBuild` | VERIFIED PASS after updating the scenario health: eight peers, Wrench use, remote missiles, nonlethal distance falloff, vehicle/prop impulses and static/container/perimeter impacts. Log: `.godot/ts89-items.log`. |
| `./check-network-vehicles.ps1 -GodotPath $godot -NoBuild` | VERIFIED PASS with clean host/client exits after the mixer wait: 320 snapshots, 646 immediate prediction frames, client p99 correction 0.014715195 m, maximum 0.45116436 m, zero large corrections and zero prop replica error. Log: `.godot/ts89-network-vehicles.log`. |
| `& $godot --headless --path . --export-release 'Windows Desktop' .godot/ts89-release/Trackstorm.exe --quit` | VERIFIED PASS: actual Windows Release export, no export warnings/errors. Preserved the existing editor layout around the export. Log: `.godot/ts89-export.log`. |
| `./check-build-version.ps1 -ExportPath .godot/ts89-release/Trackstorm.exe` | VERIFIED PASS: executable file/product resources **0.1.0.0**, runtime and embedded Godot metadata **0.1.0**, exported=True. Log: `.godot/ts89-export-version.log`. |

Initial verification attempts exposed the sandbox's denied NuGet.Config read and then one SA1118 warning in the new test's multiline argument. The read was rerun with normal user access, the test was corrected, and the complete check suite subsequently passed. Assertions were not weakened.

The first item harness failure and its correction are described above; the complete `check.ps1` suite passed again after that change. The export contains the existing EOS managed/native libraries and third-party notice. No dependencies, assets or licenses were added.

The first reconnect run reached its gameplay success marker but the wrapper correctly failed on shutdown diagnostics (45 ObjectDB instances, four resources). An unchanged rerun passed cleanly; similar transient behavior was recorded in TS-68. Network movement also reached its gameplay success marker but repeatedly reported 12 audio playback/stream instances and one music resource. An ignored diagnostic copy of the wrapper enabled verbose output to identify those resources; its output is `.godot/ts89-network-diagnostic.log`. Neither wrapper's warning/error gate was disabled.

Final integrated `check.ps1` passed after both harness corrections. The source JSONL hash remained unchanged after native verification. Final review found no unrelated changes, player preferences, tracked local configuration, generated assets, binaries or debug output. Only Core owns gameplay tuning; production dependency direction remains Client -> Core with no Shared layer. Relative links in the affected documents resolve, and the existing feature index already routes to every affected system.

## Remaining manual acceptance

UNVERIFIED: different-version and matching-version exports on separate PCs/profiles through real EOS Public and Locked lobbies, Ready/Start and reconnect; clean second-PC execution without Godot; multi-PC/NAT and long-session performance; subjective gameplay balance, physical-controller feel and audible presentation. The native checks here use local UDP or a fake online provider. The release tuning is exactly the approved source; these checks do not establish new human playtest approval.
