# TS-68 retained-match decision verification

Implemented on `ts-68-jg` and synchronized with `origin/main` at `3ea435f`, which includes TS-65 and merged PR #38 / TS-66. The integrated Story uses canonical version `0.0.1.1`. TS-68 was re-read after its player-flow clarification; it has no child issues or comments. Current Jira requires Lobby-first startup and treats a local locator only as a silent routing hint. The earlier approved correction also requires version mismatch to preserve that locator because the authoritative reservation remains intact.

## Implemented flow

Application entry presents the Lobby browser first. The existing same-account resume locator initiates membership/routing recovery and an authenticated, read-only Core reservation query silently; `Checking` is internal progress and does not expose or replace the lobby with the retained-match prompt. Only an authoritative `Available` result overlays **Reconnect / Leave Match** and blocks normal lobby interaction. Reconnect uses the existing generation-checked Resume and full checkpoint path. Leave Match requires the same authenticated subject/session/player/generation and current authority epoch; only its authoritative response permits successful-release presentation and locator removal. Missing reservations clear the stale hint while the browser remains available. Unavailable checks, failed reconnects, unconfirmed releases and game-version mismatches return to the recoverable browser and retain a retryable hint without claiming success. A mismatch displays the TS-66 host/local version diagnostic, binds no gameplay state and permits a later compatible runtime to inspect and reclaim the same player. Repeated choices cannot start simultaneous operations. In-process transient recovery is preserved.

Core separates occupied/reserved slots from released participant display identities in the same immutable lobby snapshot. Release removes authorization and the occupied slot; the existing vehicle driver removes the suspended vehicle/inventory, while MatchState retains statistics and consumed-life memory. Standings ranks occupied and departed identities together, retaining offline dimming and `--` Ping through results. Additional history pages use Page Up / Page Down. Return clears history with the existing lifecycle. Limits remain eight occupied slots and 256 lifetime match participants.

Lobby protocol version seven carries bounded binary roster/history state and existing JSON controls. Binary names avoid JSON escape expansion at maximum Unicode history size inside the existing migration packet bound. EOS compatibility is `trackstorm-lobby-14`; older peers must update. Locked control-only inspection/abandonment does not bypass the fresh-join access-code gate.

## Changed files

- Core sessions: `LobbyAuthority.cs`, `LobbySnapshot.cs`, `LobbyRestoreState.cs`, `LobbyCodec.cs`, `LobbyCommand.cs`, `SessionReconnectPolicy.cs`; new `MatchParticipant.cs` and `ReservationResult.cs`.
- Core standings: `Matches/MatchRanking.cs`.
- Client online: `OnlineLobby.cs`, `OnlineLobbyCoordinator.cs`, `OnlineLobbyPanel.cs`, `OnlineSessionBinding.cs`; new `RetainedSessionDecision.cs`.
- Client networking: `DevelopmentSession.cs`, `LobbyNetworkDriver.cs`.
- Client HUD: `MatchStandings.cs`, `MatchStandingsView.cs`, `StandingsMetal.cs`.
- Tests: new `Tests/Sessions/ReservationDecisionTests.cs` and `TransportTests/OnlineLobbyTests.ReservationDecision.cs`; updated `MatchRankingTests.cs`, `OnlineLobbyTests.cs`, `OnlineLobbyTests.Restart.cs`, `OnlineLobbyTests.Leases.cs`.
- Native harnesses: `OnlineLobbyUiChecks.cs`, `StandingsIntegrationChecks.cs`.
- Integration correction: `OnlineLobbyCoordinator.cs`, `OnlineLobbyTests.cs`, `OnlineLobbyTests.ReservationDecision.cs`, `LobbyIntegrationChecks.cs` and `GameVersionTests.cs`.
- Feature docs: index, game versioning, reconnection, sessions, standings, matches, EOS lobbies/P2P, host migration and death/respawn compatibility reference. Verification index and this report.

## VERIFIED

| Check | Result |
| --- | --- |
| `./tools/check-version.ps1` | Canonical Story version and ancestry passed; **28 version-rule checks passed**. |
| Focused retained-session/startup command | **9 transport tests passed**, covering silent locator validation, authority-confirmed prompt ownership, unavailable-validation browser recovery/manual retry, locator preservation on version mismatch, later compatible inspection/Resume, same PlayerId, and missing/abandonment/idempotency regressions. |
| `./check.ps1` | Restore and formatting verification passed; Debug and Release builds completed with zero warnings/errors; **374 Core + 306 non-native Client/transport tests passed in each configuration**. |
| `check-online-lobby.ps1 -NoBuild` | Production Godot controls passed for Lobby-first launch, no prompt without authoritative confirmation, browser visibility during silent validation, confirmed modal ownership, Public/Locked flows and idempotent release/reconnect using the fake provider. |
| `check-reconnect.ps1 -NoBuild` | Real local UDP passed three arena resyncs including 125 seconds offline, retained bodies, prediction/interpolation reset, gameplay continuity, offline standings, final results and new-generation reset. |

The focused compatible-retry test starts with a disconnected authoritative player and vehicle, injects the TS-66 mismatch response, verifies the locator and exact authority snapshot remain unchanged, then constructs a fresh coordinator from that locator. The compatible retry performs `InspectReservation` and `Resume`, rebinds the original PlayerId with generation advanced once, and observes exactly one matching roster, vehicle and standings entry. Core remains engine-independent, with Client -> Core dependencies and no Shared layer. No packages, assets or service deployment were added.

## Limitations and manual acceptance

**UNVERIFIED:** real two-PC EOS restart/rejoin, NAT/routing, provider callback timing and long Internet soak. Fake authenticated EOS datagrams and trusted native identity seams are not equivalent to those checks. The UI choice harness uses deterministic responses; Core release and vehicle cleanup are separately exercised through the production drivers.

Run the following on two PCs with the same new build, for Public and Locked sessions:

1. Start an active match and record the client's identity, vehicle/item state, Kills/Deaths and leaderboard row.
2. Exit/restart that client under the same EOS identity. Verify launch enters the Lobby list and no reconnect prompt appears while the locator is being validated. After the authority confirms the reservation, verify the modal appears over the Lobby and gameplay remains unbound until a choice.
3. Choose Reconnect, including repeated activation. Confirm one original identity/vehicle/row and current authoritative state, without a password retry for the retained Locked identity.
4. Repeat departure/restart, choose Leave Match and confirm the client waits for release. Verify the slot can be reused by normal admission, the old claim cannot resume, and name/stats/rank remain offline/dimmed with `--` Ping through results. Check later history pages if more than eight participants have played.
5. Interrupt the release reply, recover and retry. Verify no premature success and an already-released claim resolves cleanly. Exercise host Return/closure while choosing and a short in-process interruption.
6. Confirm Return/new generation clears history. With a mismatched build, confirm gameplay remains unbound and the locator survives; after updating to a compatible build under the same account, confirm the prompt returns and the same PlayerId can reconnect.

Assumptions remain those of the existing locator design: no locator means normal browsing, and a crash before saving the newest generation may produce a stale claim. The host remains the source of truth; unavailable authority cannot be treated as confirmed release. Maximum history preserves the existing 256-participant match bound. History paging currently uses keyboard Page Up / Page Down.

No additional Jira implementation scope is proposed. Record real-device acceptance against TS-68; no Jira completion transition or PR creation is included in this implementation checkpoint.
