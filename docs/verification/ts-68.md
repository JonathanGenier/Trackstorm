# TS-68 retained-match decision verification

Implemented on `ts-68-jg`, based on merged TS-65 (`9aeca82`). The branch was checked against `origin/main` during integrated verification: zero commits behind. TS-68 was re-read after implementation; it has no child issues or clarifying comments. No requirement changes were assumed.

## Implemented flow

The existing same-account resume locator initiates membership/routing recovery and an authenticated, read-only Core reservation query. A validated reservation presents **Reconnect / Leave Match** before normal discovery. Reconnect uses the existing generation-checked Resume and full checkpoint path. Leave Match requires the same authenticated subject/session/player/generation and current authority epoch; only its authoritative response permits successful-release presentation and locator removal. Missing reservations resolve to the browser. Unavailable checks, failed reconnects and unconfirmed releases retain a retryable hint without claiming success. Repeated choices cannot start simultaneous operations. In-process transient recovery is preserved.

Core separates occupied/reserved slots from released participant display identities in the same immutable lobby snapshot. Release removes authorization and the occupied slot; the existing vehicle driver removes the suspended vehicle/inventory, while MatchState retains statistics and consumed-life memory. Standings ranks occupied and departed identities together, retaining offline dimming and `--` Ping through results. Additional history pages use Page Up / Page Down. Return clears history with the existing lifecycle. Limits remain eight occupied slots and 256 lifetime match participants.

Lobby protocol version seven carries bounded binary roster/history state and existing JSON controls. Binary names avoid JSON escape expansion at maximum Unicode history size inside the existing migration packet bound. EOS compatibility is `trackstorm-lobby-14`; older peers must update. Locked control-only inspection/abandonment does not bypass the fresh-join access-code gate.

## Changed files

- Core sessions: `LobbyAuthority.cs`, `LobbySnapshot.cs`, `LobbyRestoreState.cs`, `LobbyCodec.cs`, `LobbyCommand.cs`, `SessionReconnectPolicy.cs`; new `MatchParticipant.cs` and `ReservationResult.cs`.
- Core standings: `Matches/MatchRanking.cs`.
- Client online: `OnlineLobby.cs`, `OnlineLobbyCoordinator.cs`, `OnlineLobbyPanel.cs`, `OnlineSessionBinding.cs`; new `RetainedSessionDecision.cs`.
- Client networking: `DevelopmentSession.cs`, `LobbyNetworkDriver.cs`.
- Client HUD: `MatchStandings.cs`, `MatchStandingsView.cs`, `StandingsMetal.cs`.
- Tests: new `Tests/Sessions/ReservationDecisionTests.cs` and `TransportTests/OnlineLobbyTests.ReservationDecision.cs`; updated `MatchRankingTests.cs`, `OnlineLobbyTests.cs`, `OnlineLobbyTests.Restart.cs`, `OnlineLobbyTests.Leases.cs`.
- Native harnesses: `OnlineLobbyUiChecks.cs`, `StandingsIntegrationChecks.cs`.
- Feature docs: index, reconnection, sessions, standings, matches, EOS lobbies/P2P, host migration and death/respawn compatibility reference. Verification index and this report.

The unrelated staged and unstaged `export_presets.cfg` edits were preserved and excluded from this delivery.

## VERIFIED

| Check | Result |
| --- | --- |
| `./check.ps1` | Restore/format verification; Debug and Release builds, zero warnings/errors; **345 Core + 289 non-native Client/transport tests passed in each configuration** |
| Core reservation tests | Exact owner/session/generation, read-only inspection, connected-player rejection, release once, full-slot reuse, old resume rejection, history reset, maximum Unicode history codec/migration continuation and incompatible/truncated/trailing packet rejection |
| Client reservation tests | Public/Locked choice, no assignment during inspection, duplicate replies/actions, confirmed release, lost release reply, delayed membership cleanup, forged/stale response and changed-session rejection/locator cleanup, nonzero authoritative score retention after actual vehicle cleanup, offline final winner/name/rank |
| Existing restart tests | Explicit choice followed by existing same-identity/state resync after host migration, including stale EOS routing and former-host recovery; no fresh Join or duplicate assignment |
| `check-online-lobby.ps1 -NoBuild -Visual` | Production Godot controls: ordinary browser flow, retained prompt, both actions, double activation, pending release and confirmed return; deterministic provider/response seam |
| `check-standings.ps1 -NoBuild -Visual` | Eight UDP sessions, keyboard/controller held visibility, published ping, ranks/final results at five viewport sizes, native Page Down to a ninth offline history row and Return cleanup |
| `check-reconnect.ps1 -NoBuild -Visual` | Three real UDP arena resyncs, first after **125 seconds offline**; native body reuse, prediction/input reset, HP/items/spawns/match state, offline standings, results and new-generation reset |
| `check-lobby.ps1 -NoBuild` | Eight-peer lobby/session lifecycle, full retained-slot rejection and host-loss cleanup |
| `check-match.ps1 -NoBuild -Impaired` | Eight-peer lethal collision/missile scoring, winner agreement and death/respawn under impairment |
| `check-menu.ps1` | **68 assertions**, native scene/menu navigation, settings, leave and production Quit |
| `check-migration.ps1 -NoBuild -Players 2` and `-Players 3` | Native UDP lobby/arena migrations, sequential epochs, retained vehicles and complete gameplay restore |

Actual rendered prompt and second history page were inspected. Evidence is under `.godot/ts68-*.log`, `.godot/online-lobby-checks`, `.godot/standings-checks` and `.godot/reconnect-checks`; generated output is not committed. Core remains engine-independent, with Client -> Core dependencies and no Shared layer. No packages, assets or service deployment were added.

## Limitations and manual acceptance

**UNVERIFIED:** real two-PC EOS restart/rejoin, NAT/routing, provider callback timing and long Internet soak. Fake authenticated EOS datagrams and trusted native identity seams are not equivalent to those checks. The UI choice harness uses deterministic responses; Core release and vehicle cleanup are separately exercised through the production drivers.

Run the following on two PCs with the same new build, for Public and Locked sessions:

1. Start an active match and record the client's identity, vehicle/item state, Kills/Deaths and leaderboard row.
2. Exit/restart that client under the same EOS identity. Verify the prompt appears before the browser and gameplay remains unbound until a choice.
3. Choose Reconnect, including repeated activation. Confirm one original identity/vehicle/row and current authoritative state, without a password retry for the retained Locked identity.
4. Repeat departure/restart, choose Leave Match and confirm the client waits for release. Verify the slot can be reused by normal admission, the old claim cannot resume, and name/stats/rank remain offline/dimmed with `--` Ping through results. Check later history pages if more than eight participants have played.
5. Interrupt the release reply, recover and retry. Verify no premature success and an already-released claim resolves cleanly. Exercise host Return/closure while choosing and a short in-process interruption.
6. Confirm Return/new generation clears history and a mismatched build cannot reconnect.

Assumptions remain those of the existing locator design: no locator means normal browsing, and a crash before saving the newest generation may produce a stale claim. The host remains the source of truth; unavailable authority cannot be treated as confirmed release. Maximum history preserves the existing 256-participant match bound. History paging currently uses keyboard Page Up / Page Down.

No additional Jira implementation scope is proposed. Record real-device acceptance against TS-68; no Jira completion transition or PR creation is included in this implementation checkpoint.
