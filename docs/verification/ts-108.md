# TS-108 verification

## Assignment and implementation

Read Jira TS-108 in full (no children, comments or linked dependencies) and parent TS-107. Read applicable root/nested instructions, workflow, standards, critique, camera/input and relevant vehicle lifecycle/network/reconnect contracts. Worked on `ts-108-jg`, synchronized with `origin/main` before implementation and final verification. Per explicit user instruction, no version or PR prose maintenance and no PR creation command.

- Shared presentation-only orbit for practice/network arenas using their existing displayed vehicle pose.
- RMB-gated screen-relative displacement, held-angle retention, smooth release return, and preservation of drags released between render frames.
- Existing remappable right-stick intent, bounded diagonal speed, and minimum 0.15 camera dead zone even when driving dead zone is lower. Neutral/noisy-neutral input recenters.
- Wrapped yaw, bounded pitch and exponential recentering preserve normal direct heading follow, positional inertia, feedback and orientation fallback.
- Identity/life changes clear orbit and queued mouse motion. Existing network resynchronization explicitly resets the camera even for unchanged identity/life; arena reconstruction starts fresh.
- No Core changes, gameplay-frame/codec changes, network payloads, authoritative camera state, dependencies or assets.
- Extended pure/native camera tests, live arena controls/captures and held-orbit reconnect checks. Added `check-camera.ps1` to fast-check routing and updated camera/input documentation.

Parent obstruction handling, shake preferences and broader camera-feel redesign are outside TS-108 and were not implemented.

## Environment

Windows, .NET 10, Godot .NET `4.7.2.stable.mono.official.ed1daf0bf`. Rendered checks used OpenGL compatibility on NVIDIA RTX 4070 Ti. `$godot` below denotes `C:/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`. Native commands used the required local runtime/filesystem permissions. `.godot` artifacts are ignored local evidence, not shipped assets.

## Targeted verification

- `./tools/check-fast.ps1 -Area Client` — PASS after each correction. Debug production compilation with warnings as errors. Explicit area used because automatic routing compares committed changes and work was uncommitted.
- `dotnet test code/TransportTests/Trackstorm.Transport.Tests.csproj -c Release --filter 'FullyQualifiedName~Camera'` — 10/10 PASS: orbit/recenter at 30/60/144 FPS, hold, bounds/reset and inertia tests.
- `./check-camera.ps1 -NoBuild -GodotPath $godot` — PASS native mouse/stick/steering, quick RMB drag, dead-zone, suppression, gameplay-frame isolation, identity/life/reseed and chase regressions. Final targeted log: `.godot/camera-checks/988f7208fbc340b68e863f4cff35e245/camera.log`.
- Initial test compilation used nonexistent `InputFrame.Throttle`; corrected to `Accelerate` before successful checks. No failed run is counted as passing.

## Comprehensive final verification

- `./check.ps1` — PASS. Version-rule/workflow-helper/media regressions, materialized media, restore, Debug production and Release solution builds with zero warnings/errors. **504 Core + 342 non-native Client/transport tests passed.** Log `.godot/ts-108-final-check.log`. This executes version-rule regression tests, not branch version maintenance.
- `./check-vehicle.ps1 -Visual -GodotPath $godot` — PASS. Headless 30/144 FPS: 126 assertions each; rendered run: 141 assertions. Driving/surface replay matched across rates within existing 0.02 tolerance. Each run also passed live mouse orbit/hold, smooth return, two-axis stick orbit, noisy-neutral return and actual practice life reset while RMB remained held. Artifacts `.godot/vehicle-checks/0ce132031a944ae3990a1c4c5eb74a22/`; log `.godot/ts-108-vehicle.log`.
- `./check-network-vehicles.ps1 -NoBuild -Players 2 -Visual -Latency 30 -Jitter 5 -Loss 0.02 -GodotPath $godot` — PASS. Real UDP host/client arenas and native collision/prediction; both processes passed live camera inputs/return. Client: 321 snapshots, 644 immediate-input frames, 0.00546 m p99 correction, no large corrections. Artifacts `.godot/network-vehicle-checks/510c63d949104606aeadc9ea3761c0b7/`; log `.godot/ts-108-network.log`.
- `./check-reconnect.ps1 -NoBuild -GodotPath $godot` — PASS. Three actual arena resumes including 125 seconds offline, native body reuse and gameplay continuity. Added assertions seed held orbit before disconnect and verify chase baseline on the reused displayed vehicle at resync. Collision death/timed respawn and new generation also executed. Log `.godot/ts-108-reconnect.log`; artifacts `.godot/reconnect-checks/`.
- `./check-death-respawn.ps1 -NoBuild -Impaired -GodotPath $godot` — PASS. Eight UDP peers, four alternating missile/collision death/respawn cycles with native lifecycle/physics/HP/items/VFX checks. Artifacts `.godot/death-checks/fcd7a3cbd1544730be511c2e5b8c6895/`; log `.godot/ts-108-death.log`.
- `./check-input.ps1 -GodotPath $godot` — **wrapper failed**: build/import passed and native input passed **149 assertions**, but its subsequent five-frame main-scene quit emitted one leaked `RefCounted` ObjectDB warning. Reproduced with `--headless --path . --quit-after 5 --verbose`; logs `.godot/ts-108-input.log` and `.godot/ts-108-short-startup.log`. Root cause was not established; not claimed fixed or proven pre-existing.
- Separate `$godot --headless --path . --quit-after 180` and `$godot --headless --path . --quit-after 180 -- --local-practice` — PASS, clean exits without diagnostics. Logs `.godot/ts-108-main.log` and `.godot/ts-108-practice-smoke.log`.
- Diff inspection confirmed Client camera ownership and no Core/serialization/dependency changes. `git diff --check` passed after removing a scaffold EOF blank line.

## Runtime observations and limitations

**VERIFIED:** synthetic native mouse/gamepad events exercised the real input adapter, camera and both arena presentation paths. Rendered captures include chase, mouse orbit, intermediate return, stick orbit, recentered and practice respawn. Implementation review directly inspected network mouse-orbit and practice recentered images: readable vehicle, level horizon and restored rear chase framing. Native tests verify offsets/return timing rather than inferring motion from still images.

Live camera scenarios run after driving sequences with the vehicle largely stationary. The native camera harness separately exercises changing/unstable displayed headings and 30/60/144 FPS. Network evidence is same-machine Direct-IP UDP. Reconnect identity is the existing trusted test seam. Native input reported **zero connected physical gamepads**. Synthetic events establish software behavior, not physical ergonomics or subjective comfort.

## Risks and explicitly unverified

- Five-frame frontend shutdown leaks one `RefCounted`, so the complete input wrapper did not pass. Longer main/practice smoke and gameplay harnesses exited cleanly. Cause remains unverified; no unrelated frontend change was made.
- Human physical mouse/controller playtesting, hardware drift/hotplug ergonomics, long-session comfort, performance soak and simultaneous driving/free-look human feel remain unverified.
- Authenticated EOS, multi-PC/Internet and a separately executed camera/host-migration scenario were not tested. Reconnect directly exercises the shared resynchronization hook; same-life reset is tested natively. Migration equivalence is inferred.
- Existing world-obstruction handling remains absent and outside this Story. Pitch limits do not guarantee clearance from terrain/structures.
- Independent engineering review, CI, version maintenance and human acceptance are separate from this evidence.

## Astra runtime critique

### Astra Runtime Critique — Story Round 1

**Overall Score: 8.2 / 10. Quality Assessment: PASS (>=8.0).** Independently performed by Astra Medium after final verification; no implementation changes followed the review.

The exercised free-look behavior is predictable and integrates coherently with the existing chase camera. Mouse and synthetic controller input produce orbit; releasing or neutralizing input returns behind the displayed vehicle; resets restore the chase baseline. No in-scope runtime defect emerged. The score covers operational behavior and inspected rendered states, not physical-controller ergonomics or continuous subjective comfort.

| Runtime category | Score | Observed basis |
| --- | ---: | --- |
| Runtime Functionality | 8.6 | Orbit, hold/release, noisy-neutral return, suppression, identity/life/reseed execution. |
| Controls / Responsiveness | 8.2 | Predictable displacement/stick response, RMB retention and noise rejection. Physical ergonomics unverified. |
| Camera | 8.0 | Orbit/partial return and baseline recovery in practice/network. Existing obstruction limitation is outside scope. |
| Animation / Motion | 8.0 | Executed transition sampling proves progressive return. Continuous subjective smoothness unverified. |
| Multiplayer / Networking Experience | 8.3 | Rendered impaired two-peer camera scenarios completed with correct local framing. |
| Runtime Integration | 8.5 | Displayed-pose paths, presentation-only input, heading/inertia and native life reset. |
| Runtime Stability | 8.4 | All three independent runs ended without reported Godot errors/warnings. |

**VERIFIED independently:**

- `./check-camera.ps1 -NoBuild -GodotPath $godot`: native input, reset, noise/return and regression checks at supplied 30/60/144 FPS deltas. Log `.godot/camera-checks/b2e95a1485334d3dadb565f7d5e58c91/camera.log`.
- `./check-network-vehicles.ps1 -NoBuild -Players 2 -Visual -Latency 30 -Jitter 5 -Loss 0.02 -GodotPath $godot`: both peers passed. All five client phase PNGs inspected. Client p99 correction about 0.00130 m, maximum 0.05285 m, zero large corrections. Artifacts `.godot/network-vehicle-checks/d5c215f6fc044187a1e9bd414fec0981/`.
- `$godot --path $PWD --fixed-fps 60 res://scenes/verification/vehicle_checks.tscn --quit-after 12000 -- "--vehicle-output=$PWD/.godot/ts-108-astra-round1/practice"`: rendered practice 141 assertions plus live camera controls/native life reset passed. All six phase PNGs inspected. Log/captures `.godot/ts-108-astra-round1/practice*`.
- Practice mouse/stick orbit placed the camera behind the nearby orange arena wall and fully obscured the vehicle. Return restored visibility. This is observed existing obstruction absence, explicitly outside TS-108; no corrective work proposed.

**INFERRED:** reconnect continuity from the inspected implementation reconnect log (three resyncs, 125 seconds offline, native body reuse); not independently repeated during critique.

**UNVERIFIED / limitations:** physical mouse/gamepad feel, hotplug ergonomics, extended manual driving, motion comfort and continuous visual frame pacing. Camera delta tests do not prove sustained 144 FPS hardware performance. Full application reassignment/extended reconnect were not independently repeated. The earlier five-frame startup leak was reported by implementation evidence and not independently reproduced by Astra; all independent critique runs exited cleanly. No static engineering review or delivery actions were part of this critique. No further critique round or recommendations were initiated.
