# TS-146 — Two held items and active-item switching

## Scope and implementation

Implemented on the existing `ts-146-jg` branch. Jira TS-146 and parent Epic TS-115 were read, including their empty comment lists; TS-146 has no subtasks. The explicit user request matches TS-146. TS-157's future Nitro redesign and other Epic siblings are excluded. Current main was fetched before implementation and again before final verification: `ffa4c6d`, including completed TS-145. The Story started exactly at that main commit. Required version synchronization produced `0.1.44` / Windows `0.1.44.0`.

- Extended the existing per-player `ItemSlot` record and `ItemAuthority` with two fixed physical slots, independent grant tokens, selected index and life-scoped selection watermark. No second inventory, checkpoint, transport or lifecycle authority; production dependency direction remains Client → Core.
- Acquisition fills the first empty slot, preserves the other item and selection, and rejects a third item without consuming a pickup, RNG draw or category credit. TS-145's per-player selector algorithm is unchanged.
- E / D-pad Right use the existing remappable input path. Short taps survive between captures; holding does not repeat; gameplay/diagnostic/focus suppression prevents switching on menu close. Saved remaps remain intact.
- Reliable selection intents resolve actual sender ownership through the existing host session. Duplicate/stale commands, invalid life/generation and inactive participation are rejected. Use binds the active grant capability; a later switch cannot retarget an accepted use. One pending use per player per step remains the bound.
- Item protocol version six carries both slots and selection through existing replication and nested resume/migration checkpoints. Checkpoint installation clears pending Client selection bookkeeping. Default death/reset clears both; optional death retention reissues both tokens in the next life; fresh matches start empty with slot zero selected.
- HUD displays two fixed item frames, each with icon/name or EMPTY, and a gold numbered ACTIVE label. Speedometer moves left; no broader redesign or new assets. Remote vehicles still have no held-item model. Pickup audio handles both grant tokens; read-only Stats shows both slots and selection.

## Verification actually executed

Windows x64; .NET SDK 10.0.401; repository-pinned Godot 4.7.2 .NET. `G` below is `C:/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

| Command/check | Actual result |
| --- | --- |
| `git fetch origin main`; ancestry comparison; `tools/sync-story-version.ps1`; `tools/check-version.ps1` | Main synchronized; version checks passed. Git used a command-scoped safe-directory override for the sandbox's different Windows identity. |
| `tools/check-fast.ps1` | Auto mode reported no committed changes; it did not test the working tree. Explicit area checks were used instead. |
| `tools/check-fast.ps1 -Area Core` | Initial sandbox run could not read the user's NuGet config; authorized execution succeeded. First functional run: 601 pass / 4 stale one-slot expectations fail. After focused updates: 607 pass, then 608 pass. |
| `tools/check-fast.ps1 -Area Transport` | 353 non-native tests passed. An earlier direct non-native run passed 352 tests before the new driver test. |
| `check.ps1` | PASS: restore, warning-free Debug production and Release full-solution builds, 608 Core and 353 non-native Client/transport tests, zero skipped. Log: `.godot/ts-146-final-check.log`. |
| `check-input.ps1 -GodotPath G` | Build/import and then-current 156 native assertions passed; wrapper FAILED on its subsequent five-frame main-scene shutdown warning. |
| `G --headless --path . res://scenes/verification/input_checks.tscn --quit-after 600` | Final expanded native input PASS: 164 assertions, no warnings/errors; zero connected physical gamepads. Log: `.godot/ts-146-final-input.log`. |
| `G --headless --path . --verbose --quit-after 5` | Reproduced one zero-reference RefCounted/ObjectDB leak warning at early shutdown. Log: `.godot/ts-146-startup-verbose.log`. |
| `G --headless --path . --quit-after 120` | PASS, exit zero, no warnings/errors. Log: `.godot/ts-146-startup-120.log`. |
| `check-hud.ps1 -GodotPath G -NoBuild` | PASS: native labels/materials, rendered fill, item mappings, two occupied slots/selected second slot, units, nine viewport sizes and teardown hiding. |
| `check-items.ps1 -GodotPath G -Visual` | Initial synthetic switch failed because the fixture had not observed neutral input after enabling its adapter. Corrected fixture; rerun PASS across eight UDP peers. |
| `check-items.ps1 -GodotPath G -Impaired -Visual` | PASS: immediate second-slot use after E/D-pad Right, retained first-slot Missile, duplicate use and existing repair/projectile outcomes. Initial capture showed the preceding render; added a 0.25-second observation interval, rebuilt warning-free and reran PASS. This last change affects harness capture timing only. |
| `check-item-spawns.ps1 -GodotPath G -Impaired` | PASS: eight peers, contention, cooldown, four items, full two-slot rejection, 32 further rounds/player, exact replicated category history and convergence. Developer fixture grants occupy one slot during repeated rounds without counting toward distribution. |
| `check-reconnect.ps1 -GodotPath G` | PASS: two native peers, actual healing/death/respawn and remote tags, three resyncs including 125 seconds offline, both slots and selection retained, body reuse, prediction reset, category history, Finished/results and new-generation reset. |
| `check-migration.ps1 -GodotPath G -Players 3` | PASS: three peers, lobby host leave/fresh join, active authority loss, successive epochs, both slots/selection restored, replacement switching/use, Oil/Nitro and category/match continuation. |
| `check-migration-processes.ps1 -GodotPath G -Players 2 -NoBuild` | PASS: terminated host process; independent survivor resumed epoch 2. General process-loss coverage; this harness does not seed both slots. |
| `check-death-respawn.ps1 -GodotPath G -NoBuild -Impaired` | PASS: eight peers, four alternating missile/collision death/respawn cycles, matching lifecycle and reset HP/physics/items/VFX. |
| Diff, dependency direction, affected documentation and whitespace inspection | Reviewed for scope, ownership, stale one-slot descriptions and generated artifacts. Verification-index scaffold trailing whitespace removed. |

Focused deterministic additions exercise two pickups with independent tokens, full rejection without RNG/history changes, inactive-slot use rejection, sender isolation, switch/use order, duplicate and empty selection, malformed codec fields, second-token high-water validation, restore/reset, 32 repeated lives under both clearing policies, resume/migration and confirmed-only HUD projection. Existing atomicity/outcome tests remain passing.

## Runtime artifacts and direct observation

Local ignored outputs, not shipped assets:

- HUD: `.godot/hud-checks/3a26c1928ef846cb86fde84ed48e4c2b/`. Directly inspected 1280×720, 640×360 and 2560×1080 images. Native harness rendered nine sizes; the other six were not individually visually inspected.
- Final impaired inventory: `.godot/item-checks/4c8b120aad5148268a26b6bf54295172/`, including `runtime.log`, `evidence.txt` and `two-slots-selected-empty.png`. Directly inspected the corrected image: retained Missile in slot one, EMPTY and ACTIVE in slot two.
- Earlier normal item success: `.godot/item-checks/cf63a1d50bc541649c3a2e542af252b1/`.
- Impaired pickups: `.godot/spawn-checks/d15782ba33444b549673b4c42505518c/`.
- Death/respawn: `.godot/death-checks/5575121cac56462dbf11a695102a395b/`.
- Process migration: `.godot/migration-process-checks/d38c1ff4f2d04c72b8c25250420293f9/`.

The rendered HUD retains its metal-frame style and keeps both held icons/names visible beside health/speed. Gold ACTIVE text distinguishes selection independently of occupancy. At 640×360 text is small, but icons and selection remain distinguishable under the existing uniform scaling policy. The native pickup harness also asserts that held inventory creates no world presentation.

## Astra Runtime Critique — Story Round 1

**Overall score: 8.2 / 10 — PASS.** Exact threshold: below 8.0 is FAIL; 8.0 or above is PASS.

Assessed after comprehensive verification and applicable native runs using runtime assertions and rendered captures. Runtime functionality 8.5; controls/responsiveness 8.2; multiplayer/recovery 8.4; UI/visual clarity 8.0; runtime stability/integration 8.0. These are experiential judgments, not code-quality or test-count scores.

The exercised selection/use behavior is predictable: short input taps switch once, immediate use targets slot two, the other item remains held, and an empty selection stays explicit. Impaired local delivery preserves peer agreement without predicted consumption. Reconnect/replacement authority retain the selected inventory. The narrow HUD extension fits its existing presentation. Limits on this score include small minimum-resolution text, synthetic rather than physical-controller input, local rather than Internet multiplayer, and the separate early-startup shutdown warning. No additional improvement round was performed. The user explicitly requested commit/push after verification and critique; human acceptance and independent PR engineering review remain separate from this handoff.

## Assumptions, limitations and remaining risks

- **VERIFIED limitation:** Five-frame startup shutdown reports one ObjectDB leak warning, so the `check-input.ps1` wrapper did not fully pass. The 120-frame startup and final dedicated input harness exited cleanly. Root cause was not established or changed; the warning is not claimed fixed or conclusively pre-existing.
- **UNVERIFIED:** Physical controller ergonomics, manual free-play under combat load, audio listening, authenticated multi-device EOS/Internet/NAT behavior, exported builds and long-session performance/soak. Native input uses synthetic Godot events; local UDP recovery uses trusted identity/retirement seams.
- **UNVERIFIED:** Independent PR engineering review and remote CI. No PR prose or merge performed.
- Remote HUD selection waits for host confirmation. Immediate use can target the ordered requested slot using an existing known token; no predicted ownership/use effect is displayed. Multiple taps within one capture coalesce; one pending use per player per step remains the existing bound.
- Empty selection is allowed; pickup does not auto-select. Fixed positions and explicit switching are intentional choices within scope.
- Protocol version six requires matching builds; no mixed-version play or backward checkpoint migration is promised.
- Disconnected inventory remains subject to normal death/retention policy. The process-kill run is general recovery evidence; explicit two-slot continuation was tested in Core and three-peer native migration.
