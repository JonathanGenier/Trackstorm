# TS-97 Configs redesign verification

Implemented on `ts-97-jg`, synchronized with current `origin/main` commit `24378e13d6eea1d141d4b26e1bee1e1291097440` on 2026-09-19. Main's canonical version is 0.1.8; the derived Story version is 0.1.9 and Windows preset versions are 0.1.9.0. Jira TS-97 has no required children or comments. Its TS-96 dependency and relevant TS-38 requirements/clarifications were inspected. Approved mid-task changes moved Force Start from Configs to the shell top-right, added icon/color treatments for the TS-97 action buttons, and added persistent draft/apply/cancel feedback directly above the right footer actions. Jira was updated after each decision to make the current scope persistent.

## Files and implementation

| Files | Change |
| --- | --- |
| `code/Client/Development/DeveloperOptionsPanel.cs`, `DeveloperOptionsFeedbackState.cs` | Grouped filtered rows, white labels, canonical blue/red values, staged local transport controls, and shell-mounted configuration footer. Projects persistent amber Unsaved feedback from the existing dirty state, green success only from the completed Apply path, and temporary red Cancel feedback without creating another configuration owner. Removes only supported UI item grants and exposes the existing Force Start authority call to the shell. |
| `code/Client/Development/DeveloperOptionsDraft.cs` | Actual typed default/baseline comparisons; equivalent text and float formatting do not create phantom edits. Shortest round-trip float display. |
| `code/Client/Development/DevToolsShell.cs` | Fixed bottom action area, shell-level top-right Force Start, and in-panel Apply / Discard / Stay close decision, including edits retained while Stats/Logs are selected. |
| `code/Client/Development/DevToolsButtonPresentation.cs`, `assets/devtools/*.svg` | Project-owned action icons and consistent normal/hover/pressed/focus styles. Reset is dark with a red border; Apply is green; Cancel is red; Close is blue. |
| `code/Client/Development/DeveloperSettingsStore.cs` | Reports last save success so failed close-and-apply keeps persistence feedback visible. Storage format and atomic replacement are unchanged. |
| `code/TransportTests/DeveloperOptionsTests.cs` | Default classification, changed-back values, invalid values, persisted overrides and float precision regressions. |
| `code/Client/Verification/DeveloperOptionsIntegrationChecks.cs` | Search, colors, pending state, Cancel/Reset, local simulation, all close decisions, validation/save failure, client synchronization and removed-control coverage. Existing item fixture setup now calls the existing authority directly. |
| `code/Client/Verification/MenuIntegrationChecks.cs`, `MenuIntegrationChecks.DevTools.cs` | Updated control location/name; viewport, scrolling and persistent footer/Force Start bounds. Retains remapped keyboard/controller/axis navigation coverage. |
| `docs/features/developer-options.md`, `devtools.md`, `game-menu.md` | Current UI, staging, closing and integration behavior. `docs/features.md` remains the compatibility pointer to the system index. |
| `Directory.Build.props`, `export_presets.cfg` | Required Story version synchronization. |
| `docs/verification/README.md`, this report | Verification evidence and explicit limitations. |

Gameplay staging still calls `DevelopmentSession.ConfigureDeveloperOptions` and the existing Core configuration transaction. No gameplay, wire, revision, persistence schema, reconnect or migration implementation was changed. The normal item systems and grant authority remain intact; only Configs grant buttons were removed. Local GNS impairment is editor state until Apply, remains process-local and unsaved, and uses the existing capability-gated provider path. A local-provider rejection after gameplay acceptance is reported explicitly rather than claiming an atomic cross-system transaction.

## Acceptance criteria

| Criterion | Direct evidence |
| --- | --- |
| Configs is the F1 default; Force Start is shell-level and immediately visible | Native shortcut checks; Force Start is uniquely owned by the shell, is absent from Configs, and remains at the top-right above the scroll at 640x360, 1280x720 and 2560x1080. The normal solo countdown/Active transition still runs. |
| Action icons and required colors | Native checks require non-null icons on Force Start, Reset, Apply, Cancel and Close, and inspect the normal style: Reset dark/red outline, Apply green, Cancel red and Close blue. Rendered captures confirm readable labels/icons. Code defines distinct hover, pressed and focus styles. |
| Search and clear grouping | Case-insensitive, multiword label/category/key search, no-match feedback, hidden draft retained, clearing search restores the same value; runtime and revision unchanged. Existing catalog groups are preserved. |
| Default values blue; modified values red; labels white | Every canonical editor is checked blue after Reset; changed and accepted nondefault values remain red; row labels checked white. Unit tests compare actual canonical types and equivalent decimals independently of edit history. Rendered captures inspected. |
| No Give Missile/Wrench/equivalent buttons | Native enumeration rejects every Configs button starting with Give. Wrench consumption and configured-speed missile use still run through normal authority in the fixture. |
| Editing stages, Apply validates/commits | Existing native typing/Enter isolation, all 59 keys through production UI and real UDP, invalid input rejection, exactly one revision on changed Apply, unchanged persistence retry. |
| Cancel restores effective values | Draft, native editor, file-content and revision checks; an accepted nondefault remains effective after Cancel. |
| Reset stages canonical defaults | All 59 canonical values checked, including hosted 1000 HP; no runtime/revision/file mutation before Apply; Reset+Apply synchronizes and persists. |
| Fixed footer with correct order | Native viewport/enclosure and scroll-bound assertions for Reset, Apply Settings, Cancel and Close at three sizes. Reset is left; the other three are right. Force Start is independently fixed in the shell header. |
| Configuration feedback above footer actions | Native transition checks require amber `Unsaved changes` for gameplay and local-network drafts and Reset, green `Settings applied` after authoritative runtime Apply succeeds (including when host-local persistence subsequently fails), red `Changes discarded` after Cancel, and no green state after validation or authoritative rejection. Persistence failure status remains visible, close remains incomplete for retry, and the retry does not advance the authoritative revision. Three-size bounds checks keep the feedback directly above Apply/Cancel/Close and outside scrolling content. |
| Configuration actions absent on Stats/Logs | Native tab switches verify hidden configuration footer; existing read-only Stats/Logs harnesses pass. |
| No silent close loss | Close from Logs protects a Configs draft; Escape, Stay, Discard, successful Apply and invalid Apply tested. Tab shortcuts cannot bypass the decision. Save failure stays open and retry preserves the revision. |
| TS-38 host/client authority and synchronization preserved | Joined-client controls hidden; direct unauthorized requests rejected; each of 59 edits agrees across real UDP host/client configuration and revision; process-restart persistence; deterministic ordering/recovery/migration tests remain in the full suite. Supplemental native recovery assertions pass, but strict teardown checks fail as detailed below. |

## Checks and results

Godot 4.7.2 .NET, Windows, NVIDIA RTX 4070 Ti OpenGL compatibility; `DOTNET_PROCESSOR_COUNT=4`.

| Check | Result |
| --- | --- |
| `tools/sync-story-version.ps1`, `tools/check-version.ps1` | PASS against synchronized main 0.1.8: Story 0.1.9 / export 0.1.9.0. |
| `check.ps1` | PASS: formatting, frontend-media and version checks, Debug/Release builds with zero warnings/errors; 453 Core and 318 non-native Client/transport tests in each configuration. |
| `check-developer-options.ps1` | PASS after the review correction: 409 write assertions and 8 separate-process restart assertions, including validation/rejection without green feedback, authoritative success with green feedback on both save success and save failure, visible persistence failure, close staying open, and revision-stable retry. |
| `check-menu.ps1` | PASS after the final synchronization: 754 assertions, including native input, shell-level Force Start, no Configs duplicate, icons/styles, staged changes, close decisions, continued simulation and menu return. |
| `check-statistics.ps1 -NoBuild` | PASS: 55 assertions. |
| `check-event-log.ps1 -NoBuild` | PASS: journal, activity feed, UDP, filtering/freeze and input isolation. |
| `check-startup.ps1 -Visual` | PASS: actual main scene and phase-aware startup/recovery composition. |
| Godot import via `check-gdunit.ps1` | PASS import; GdUnit test FAIL described below. |
| `check-reconnect.ps1 -NoBuild` | Behavioral assertions PASS, including 125 seconds offline and three arena resyncs. Script FAIL on shutdown resource diagnostics. |
| `check-migration.ps1 -NoBuild` | Behavioral assertions PASS for three UDP peers and successive authorities. Script FAIL on shutdown resource diagnostics. |
| Complete diff and documentation links | Reviewed against current main; no unrelated production changes, new dependency, Shared layer, generated assets or alternate configuration pipeline. |

The latest post-synchronization native artifacts are under `.godot/developer-options-checks/d17aee89075b4a3eb7df2f57280872f1` and `.godot/menu-checks/0dc3c56e54b8438291c7b315bc1e914e`. Earlier rendered evidence remains under `.godot/developer-options-checks/6b47d46cd0d8468085207f9cd5acb141` and `.godot/menu-checks/4ec4af99c31d4e119ac717a9ce60d295`. Current Stats and Logs evidence is under `.godot/statistic-checks/e05af4bae9c94b609a72bf8ae00eeb8a` and `.godot/event-log-checks/c9aa0b31f856423ca6385b8032eca5e3`.

## Limitations and outside-scope findings

- **VERIFIED:** Supplemental migration and reconnect runs report success for behavioral assertions, then fail their strict warning/error checks at process shutdown. Migration verbose output identifies retained WAV/MP3 playback resources; a repeat normal run still warns. These fixtures do not instantiate DevTools. **INFERRED:** this is existing audio teardown behavior rather than TS-97 UI logic; no baseline binary comparison was performed. Audio teardown fixes are outside TS-97 and were not made.
- **VERIFIED:** The existing GdUnit bootstrap test fails at line 24 because it expects `DevelopmentSession` immediately after adding the main scene. Current main has `StartupEnabled = true` and creates that session through staged startup. The unchanged test and bootstrap source demonstrate the mismatch. The current startup harness passes. Updating this older test is outside TS-97 and was not done.
- Native networking evidence is same-machine UDP with trusted identity seams, not authenticated multi-PC EOS. Physical gamepad ergonomics, OS focus/capture behavior and a new exported Release executable remain manual validation items. Rendered Godot UI and synthetic keyboard/controller/axis input were exercised; Release compilation/tests passed.
- At 640x360, the deliberately persistent shell header, search and footer leave a small scrolling settings area. Native focus-follow and all action bounds passed; developers should assess this tradeoff on their target display.
- Host-only mutation controls remain hidden for clients. The existing session/epoch draft-reset policy is preserved. Force Start and practice actions remain immediate actions, not staged settings.

## Astra Self-Critique — Story Round 1

- **Overall Score: 7.2 / 10**
- **Quality Assessment: PASS**
- **Category Scores:** functionality 8.0; UI/UX 7.2; controls 7.8; architecture/code quality 7.5; networking 7.2; testing/reliability 6.8.
- **What Was Verified:** actual native UI, colors/layout, staging/default comparisons, explicit close choices and failures, complete configuration replication, persistence/retry/restart, remapped input and main-scene integration. Full build/test suite passes.
- **Verification Limitations:** supplemental recovery teardown diagnostics and stale GdUnit assertion prevent an all-green result across every optional harness; real-device, multi-PC EOS and exported-runtime checks remain unverified.

Human acceptance is required before a PR under `docs/critique.md` and `docs/workflow.md`. No PR was created.

## Astra Self-Critique — Story Round 2

- **Overall Score: 8.2 / 10**
- **Quality Assessment: PASS**
- **Category Scores:** functionality 8.7; UI/UX 8.2; controls 8.3; visual quality 8.0; architecture/code quality 8.2; networking 7.8; testing/reliability 8.3; integration 8.4.
- **What Was Verified:** **VERIFIED** Jira's updated criteria; shell-level top-right Force Start at three viewport sizes; no Configs duplicate; readable project-owned icons; exact required normal-state colors plus implemented interaction states; staged edit, Apply, Cancel, Reset and close decisions; all 59 authoritative configuration keys and client revision synchronization over local UDP; persistence/restart; adjacent Stats/Logs behavior; full Debug/Release builds and deterministic tests. **INFERRED** the unchanged TS-38 reconnect and host-migration implementation remains safe from this presentation-only correction, supported by the full deterministic suite and the earlier native recovery evidence recorded above.
- **Verification Limitations:** no physical gamepad ergonomics, exported Release runtime, multi-PC EOS session, or live host-migration/reconnect rerun was performed for this correction. The earlier optional-harness teardown/GdUnit limitations above remain outside TS-97.

## Astra Self-Critique — Story Round 3

- **Overall Score: 8.6 / 10**
- **Quality Assessment: PASS**
- **Category Scores:** functionality 9.0; UI/UX 8.7; controls 8.5; visual quality 8.4; architecture/code quality 8.5; networking 7.9; testing/reliability 8.8; integration 8.7.
- **What Was Verified:** **VERIFIED** every current Jira criterion, including persistent dirty feedback, green feedback after authoritative runtime Apply whether persistence succeeds or fails, validation/rejection exclusion, visible save-failure status and revision-stable persistence retry, Cancel feedback and restoration, Reset staging, right-footer placement at three viewport sizes, persistent shell Close on Stats/Logs, host/client UDP synchronization, persistence/restart, adjacent read-only tabs, and the full Debug/Release suite. The feedback is derived only from `DeveloperOptionsDraft.IsDirty`, existing local-network dirty state, and the existing authoritative `Apply()` result; persistence status remains separately owned and displayed.
- **Verification Limitations:** physical gamepad ergonomics, an exported Release executable, multi-PC EOS, and live native reconnect/host-migration were not rerun for this presentation correction. Existing optional-harness limitations remain outside TS-97 as documented above.
