# TS-60 implementation verification

Recorded 2026-09-17 on the existing `ts-60-pg` branch. TS-60 and TS-59 descriptions, comments and links were read before implementation. TS-60 was re-read after implementation: updated timestamp remained `2026-09-17T11:53:45.114-0400`, with no comments, required children or approved scope changes. Work remained on `ts-60-pg` without creating or switching branches. After PR #35 was opened, `origin/main` was merged into this branch to resolve its integration conflict; both TS-58 and TS-60 entries were retained in the verification index.

## Implementation and changed files

- `code/Client/Hud/ActivityFeed.cs`: five native, input-transparent labels, top-right layout, semantic colors, outlined text, automatic display and teardown.
- `code/Client/Hud/ActivityFeedView.cs`: live allowlist over the shared journal, independent five-second expiry, five-row oldest-first eviction, lifecycle binding without historical replay.
- `code/Client/Hud/ActivityFeedEntry.cs` and `ActivityFeedTone.cs`: presentation-only row and color semantics.
- `code/Client/Bootstrap/SimulationBootstrap.cs`: binds the feed to the same session/practice source as the Event Log and the existing arena lifetime.
- `code/Core/Events/EventStream.cs`: post-acceptance live notification from the existing journal; existing authority, sequence acceptance and replication remain intact.
- `code/Core/Simulation/Simulation.cs`: annotates the existing death event when the same committed match change emits an awarded Kill. No new kill detection or scoring rules.
- `code/TransportTests/ActivityFeedTests.cs`: ten focused cases covering production presence/combat outcomes, filtering, names, expiry, bursts, replay and subscription lifetime.
- `code/TransportTests/VehicleNetworkDriverTests.cs`: adds feed assertions to the production replication/deduplication and connection-generation test.
- `code/Tests/EventStreamTests.cs`: asserts the shared death/kill relationship.
- `code/Client/Verification/EventLogIntegrationChecks.cs`: extends the existing native bootstrap/UDP harness with feed checks and visual captures.
- `docs/features/activity-feed.md`, `docs/features/README.md`, `docs/features/event-log.md`, `docs/features/hud.md`: current behavior, shared contracts and verification routing.
- `docs/verification/ts-60.md` and `docs/verification/README.md`: this evidence and its index entry.

Pre-existing modifications to `code/Core/Input/InputButtons.cs`, `export_presets.cfg` and untracked `assets/audio/sonniss/` were present at entry. The user explicitly approved normalizing only InputButtons indentation after it blocked required validation; tabs were replaced by four spaces without behavior changes, leaving that file identical to HEAD. The export settings and audio assets remain untouched and outside TS-60.

## Acceptance criteria

VERIFIED means directly exercised; INFERRED means source/diff review. These map to all 19 acceptance criteria in current Jira order.

| # | Criterion | Evidence |
| --- | --- | --- |
| 1 | Automatic gameplay feed | VERIFIED: production bootstrap and actual local UDP departure appear without a toggle. Inactive projection clears rows and ignores lobby events. |
| 2 | Same structured pipeline | VERIFIED: actual LobbyAuthority and simulation events drive tests; native feed binds the Event Log's source. |
| 3 | No duplicate authority/detection | INFERRED: diff adds only a live notification, committed death/kill context and a Client projection; no second transport, connection tracker or damage detector. |
| 4 | Approved categories only | VERIFIED: strict category/kind allowlist, including unknown future-category rejection. |
| 5 | Correct presence names | VERIFIED: join, leave, disconnect, resume and grace removal use captured sanitized names, including removal from the roster. |
| 6 | Authoritative killer/victim | VERIFIED: actual missile and collision deaths use committed match Kill outcomes. Self/environmental and pre-active deaths remain generic deaths. |
| 7 | Optional item wording | VERIFIED: Missile suffix comes only from the shared allowlisted cause. |
| 8 | Damage/debug/admin filtering | VERIFIED: all non-allowlisted categories, developer events and local diagnostics are excluded; raw diagnostic fields are not rendered. |
| 9 | Top-right HUD | VERIFIED: native geometry and inspected screenshots at 640×360, 1280×720 and 2560×1080. |
| 10 | Five lines, transparent background | VERIFIED: exactly five native labels, no panel/background, single-line truncation. |
| 11 | Sixth evicts oldest | VERIFIED: deterministic burst test and native six-entry fixture. |
| 12 | Meaningful colors | VERIFIED: green arrival/reconnection, amber departure/disconnection, soft red death/kill; native captures inspected. |
| 13 | Short automatic expiry | VERIFIED: five presentation seconds, native and pure checks. |
| 14 | Independent expiry | VERIFIED: staggered entry timing retains the newer row when the older expires. |
| 15 | No persistent empty panel | VERIFIED: native CanvasLayer hidden with no remaining rows. |
| 16 | Clean bursts | VERIFIED: oldest-first five-row bound, no row overlap, more than 2,000 subsequent debug entries cannot bury an accepted feed event before rendering. |
| 17 | No replicated/reconnect duplicates | VERIFIED: shared stream rejects duplicate identities, driver test rejects repeated/stale packets, split Dead/Kill packets produce one row. |
| 18 | No resume replay | VERIFIED: old accepted events cannot reappear after expiry; rebinding/reconstructing projections never reads history; simulation restore emits no old death. Existing native reconnect regression also passed. |
| 19 | Extensible shared-event filter | INFERRED: future approved types require a projection allowlist extension, without another event authority. |

## Checks and results

- Focused `ActivityFeedTests`: PASS, 10 cases.
- Debug build with `-warnaserror`: PASS, zero warnings/errors.
- Debug Core tests: PASS, 318/318. Post-sync Debug non-native Client/transport tests: PASS, 164/164.
- Release Core tests: PASS, 318/318. Post-sync Release non-native Client/transport tests: PASS, 164/164.
- Pre-sync `./check.ps1`: PASS in full, including restore, repository-wide formatting, Debug/Release builds with warnings as errors, and all four test runs. Evidence: `.godot/ts60-check-final.log` ends with `All checks passed.`
- Post-sync integrated `./check.ps1`: PASS in full with zero build warnings or errors; both configurations passed 318 Core and 164 non-native Client/transport tests. Evidence: `.godot/ts60-merge-check.log` ends with `All checks passed.`
- Earlier attempts exposed three new-file line-ending issues (corrected) and pre-existing InputButtons tabs that failed formatting and Release SA1027 checks. The user approved indentation-only normalization; the final run required no exclusions, analyzer suppressions or project configuration changes.
- Native `check-event-log.ps1 -NoBuild -Visual`: PASS after normal OS/user-data access was allowed. Final artifacts: `.godot/event-log-checks/a30c54e415cb427ab66efb91ffa1a05e/`. The first sandboxed attempt failed on Windows certificate-store access and its existing user-data screenshot path, before feed checks.
- Native feed: PASS for production bootstrap composition, independent expiry, bounded bursts, filtering and actual UDP departure. Captures `.godot/activity-feed-checks/feed-{640x360,1280x720,2560x1080}.png` were inspected. An observed countdown-strip layering issue was corrected and the visual harness rerun successfully.
- Native `check-reconnect.ps1 -NoBuild`: PASS, three arena resyncs, identity continuity, current state and grace expiry over real local UDP with the harness's authenticated identity seam.
- Native `check-match.ps1 -NoBuild`: PASS, six missile/collision death/respawn cycles with eight peers agreeing on scores and winner. Artifacts `.godot/match-checks/7c5b09ca781a4a7497aa3b2ae25d7ca2/`.
- Post-sync native `check-event-log.ps1 -NoBuild`: PASS, including the activity feed and actual local UDP departure; artifacts `.godot/event-log-checks/74118c219fdf40b38fdaf9e2ac5c8e28/`.
- Post-sync native `check-statistics.ps1 -NoBuild`: PASS, 39 Statistic Panel integration assertions; evidence `.godot/statistic-checks/1611a00816d742378d9bbf28a646fb5b/runtime.log`.
- `git diff --check` and affected documentation link checks: PASS. Changed production code maintains Client → Core dependency; no Shared layer, new third-party dependency or asset was introduced.

Logs for builds, formatting and tests are retained under `.godot/ts60-*.log`. No manual play session, physical input validation, exported Release runtime, or remote EOS test is claimed.

## Remaining validation and manual checks

Required automated repository gates passed after synchronization. PR #35 exists on `ts-60-pg` and remains unmerged.

Manual follow-up: use two separate PCs in a real EOS session for interruption/resume and long-running combat; verify the feed during actual driving with FPS/ping, standings and menus; inspect maximum-length Unicode names, high DPI and additional displays. Local native UDP and fixture screenshots do not establish remote EOS behavior or subjective readability in every scene. Long rows deliberately use ellipsis rather than overlap/wrap. Mixed builds lacking the shared scored-death context are not a verified configuration.

No new out-of-scope feature was implemented. Existing host migration remains separate work.

## Astra Self-Critique — Story Round 1

**Overall Score: 7.4 / 10. Quality Assessment: PASS (>=6.0).**

| Material category | Score |
| --- | --- |
| Functionality | 7.5 |
| Networking | 7.5 |
| UI / UX | 7.0 |
| Performance | 7.0 |
| Stability | 7.5 |
| Code Quality | 7.5 |
| Testing / Reliability | 8.0 |
| Architecture | 8.0 |
| Integration | 7.5 |

VERIFIED: full repository checks, real local UDP departure, independent expiry and capacity boundaries, three-size native visual inspection, real local reconnect and eight-peer scoring/lifecycle regressions. INFERRED: the event-authority boundary, safe allowlist and future extension contract from source/diff inspection. UNVERIFIED: separate-PC EOS interruption/soak, physical driving/combat usability, maximum-length names across every display/DPI, and an actual exported Release runtime.

The feed is bounded, consistent with the existing authoritative history, and suitable for the milestone. It handles split kill/death batches without client-side attribution, does not retain/replay old history, and leaves input untouched. The visual result is intentionally restrained; broader usability and remote-network evidence would be needed for a higher confidence/polish score. This review alone did not authorize PR creation or another implementation round; a later explicit human instruction authorized PR #35.
