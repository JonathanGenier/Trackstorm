# TS-172 — Oil handling and distinct-contact lifecycle

## Scope and integration

Read Jira TS-172 and all twelve acceptance criteria (no children/comments), applicable AGENTS routing, workflow, standards, critique and relevant feature contracts. Re-read Jira during final verification. The user explicitly approved necessary bounded-message work after the old publication/transport limits were explained.

Branch: `ts-172-jg`. Initially based on `1a54d87`; fetched and integrated current main at `745796a` (TS-173 synchronized race start). The verification-index overlap was resolved by preserving both entries. Story/export versions are **0.1.69 / 0.1.69.0**. The pre-existing `assets/maps/oval/grass_variation.png.import` change is excluded from this Story.

## Implementation

- Removed the scripted yaw impulse. Supported Oil overlap reduces lateral tire force by default 50%, preserving steering, countersteering, longitudinal force and physical angular momentum. Leaving Oil restores grip linearly over default 1.75 seconds. Re-entry refreshes recovery; multiple patches do not multiply impairment. Movement snapshots retain recovery; retuning preserves its fraction with tick rounding.
- Removed the global deployment gate. Valid supported placement can consume held Oil while other patches exist. Each patch captures its owner, default two distinct-enemy budget and default 60-second absolute simulation deadline.
- Stable enemy identity counts once per patch across continuous/repeated overlap, new lives and reconnect. Owner overlap impairs handling but consumes no contact and scores nothing. Successful new enemies retain TS-156's 50 base points scaled by current owner K/D; lethal/reset candidates do not consume contacts. Core removes spent/expired patches and history. Retained owners remain eligible for credit after departure.
- Four host controls use **F1 → Configs → Oil**, existing staging, persistence, accordion and category reset: grip 0–0.8, recovery 0.1–10 seconds, enemy budget 1–7, cleanup 1–600 seconds. Existing Oil spawn tuning remains available.
- Extended complete item/resume/migration bounds and retained-owner award capacity. Item protocol 12, movement 9, configuration 23 and match 10 reject incompatible prior schemas.
- Client's shared reliable-message adapter compresses large boundaries when beneficial, sends remaining large payloads as paced 60,000-byte chunks and delivers only complete validated messages. Exact size/order, bounded allocation/backlog, malformed data, replay and timeout are covered. Gameplay consumers still validate authority/session/epoch/generation/revision. Unchanged Oil in ordered item updates references the preceding publication; changed Oil and standalone recovery checkpoints carry complete state, avoiding full patch retransmission with every projectile update.
- Core remains engine-independent; Client owns native framing/presentation. No Shared layer, native SDK change or new package/asset dependency.

## Commands and actual results

Godot: `C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe` (4.7.2 .NET).

| Command/check | Result |
| --- | --- |
| `git fetch origin main`; `tools/sync-story-version.ps1` | Integrated `745796a`; version checks passed at 0.1.69. |
| Targeted Core build and Oil/scoring tests | Passed during iteration; focused Oil run before baseline-reference coverage had 29 cases. |
| `tools/check-fast.ps1 -Area Core` | Passed during iteration (853 cases at that point). |
| Focused reliable-transfer/fake-EOS tests | 12 passed after bounded compression; final suites also include these tests. |
| `check.ps1` | Final pass: 108 version checks, workflow/media helpers, restore, Debug production and Release solution builds, zero warnings/errors, **858 Core + 378 non-native Client/transport tests**. [Log](ts-172/final-check.log). |
| `check-transport.ps1 -GodotPath <above>` | Final rerun: **389/389 passed**, then Godot transport integration passed three creation/poll/message/cleanup cycles. [Log](ts-172/final-transport.log). |
| `check-oil.ps1 -GodotPath <above> -Impaired -Visual -NoBuild` | Passed four-peer native scenario; 30 ms outbound delay, 5 ms jitter, 2% loss. [Log](ts-172/oil.log). |
| `check-reconnect.ps1 -GodotPath <above> -NoBuild` | Passed three arena resyncs including 125 seconds offline, native body reuse and 1,500-patch continuation. [Log](ts-172/reconnect.log). |
| `check-migration.ps1 -GodotPath <above> -Players 2/3 -NoBuild` | Both passed with 1,500 patches, ownership/deadlines/history and other item/score continuation. [Two peers](ts-172/migration-two.log), [three peers](ts-172/migration-three.log). |
| `check-migration-processes.ps1 -GodotPath <above> -Players 3 -NoBuild` | Passed: terminated host process; independent native survivors resumed epoch 2. Artifacts `.godot/migration-process-checks/80fa6a9aaff445658670d9f3e905ec7b`. |
| `check-developer-options.ps1 -GodotPath <above> -Visual` | Passed **1,220 write + 14 restart/read assertions**, including visible Oil fields, catalog/reset/persistence and controller focus. [Write](ts-172/configs-write.log), [read](ts-172/configs-read.log), [screenshot](ts-172/oil-configs.png). |
| `check-match-start.ps1 -GodotPath <above> -NoBuild` | All four scenarios passed: two/four participants, departure and reconnect during Countdown; authority waits at tick zero, neutral pre-GO handling and synchronized sequence. Artifacts `.godot/match-start-checks/9c340204097d42f19036e0c11abd918c`. |
| Godot `--headless --path . --quit-after 120` | Main-scene startup exited successfully without reported errors/warnings. |
| `git diff --check`, stale-identifier searches, complete Story diff inspection | Clean whitespace; active forced-spin/global-cap references removed; affected contracts synchronized. |

## Native scenario evidence

The Oil fixture uses production placement/authority with isolated Godot worlds and real local UDP peers. It verifies a 14-degree banked patch, fresh admission, owner contact without forced yaw/score/contact consumption, continuous enemy overlap, exit/re-entry without another score, a second simultaneous patch and removal of only the original after the second distinct enemy. The original three peers converge on 100 Oil points (two awards of 50). Then 1,500 detached patches plus retained enemy history replicate to existing peers and a fourth fresh participant. [Rendered banked patch](ts-172/banked-oil.png).

Reconnect/migration seed detached patches to isolate continuation; these are not 1,500 player-operated deployments. Real deployment is separately exercised by the Oil harness and 64 consecutive authoritative deployments in Core coverage. Recovery preserves exact owner, absolute deadline and partly consumed enemy budget without historical score replay. Other item/score/native-body assertions remain enabled.

## Iteration failures and fixes

- Large migration initially selected a pre-Oil checkpoint: full patch-list retransmission delayed newer checkpoints. Compression alone still exposed native queue exhaustion. References to unchanged Oil resolved this; subsequent two/three-peer migration and reconnect passed. Diagnostic prints were removed.
- Visual Configs initially failed controller navigation because the test window lacked OS focus. The fixture now grabs/verifies focus before injecting controller input. Production input gating is unchanged; rendered checks pass.
- A comprehensive run found changed rejection counters for ordinary packets from unknown peers. Preserved their prior path while validating chunk senders; final comprehensive checks pass.
- First extended native run passed 388/389; the eight-peer 2%-loss case failed its assertion that every peer rejected one injected *unreliable* stale packet. A dropped injection is a plausible explanation, not proven by packet capture. The unchanged rerun passed all 389. [First output](ts-172/native-first-attempt.log).
- One attempted UI rebuild overlapped a native test process and hit a locked test DLL. Reran after exit; final UI build/run passes. Earlier draft version/assertion/placement failures were corrected before final passes.

## Assumptions, risks and unverified areas

- Lifetime uses the authoritative simulation clock; migration restores the original tick deadline. Tuning affects new budgets/deadlines without resetting existing history.
- The allocation ceiling (288,000 patches) derives from eight uses per step and maximum 600-second lifetime. It is not a deployment gate or performance claim. Resource exhaustion fails closed. Extreme tuning, theoretical maximum population and sustained WAN throughput are unverified.
- Native UDP evidence is local Windows evidence. Authenticated EOS, separate physical devices/NAT/Internet, cross-platform physics and production service failures were not exercised. Fake-provider EOS framing is not authenticated EOS acceptance.
- Segmented native driving establishes response/recovery but is not a long human/controller session or multiplayer balance study. No audio-quality claim.
- Independent post-push engineering review and human merge remain separate gates.

## Astra Runtime Critique — Story Round 1

**Overall Score: 8.3 / 10 — PASS (>=8.0).**

Performed after comprehensive final verification and the successful extended-native rerun. This is a runtime/experiential assessment, not the independent post-push engineering review.

| Category | Score | Observed basis |
| --- | ---: | --- |
| Runtime functionality/integration | 8.7 | Native distinct-contact lifecycle, owner exclusion, repeated use, scoring, simultaneous patches and complete recovery behaved consistently. |
| Controls and movement feel | 8.2 | Normal entry produced extra slip and a wider turn; countersteering changed yaw direction and recovery completed. Aggressive driving retained momentum and remained recoverable. |
| Physics/stability | 8.4 | No entry yaw snap, rollover, damage or runtime errors in the final driving segments. High-speed handbrake rotation developed over time. |
| Multiplayer/recovery experience | 8.3 | Local impaired-network consistency, 1,500-patch join/resume/migration and independent-process succession passed. WAN/EOS experience remains unverified. |
| Configs UI/UX | 8.6 | Rendered Oil controls are readable, grouped together and supported by existing Apply/Cancel/category reset. Controller navigation and restart persistence passed. |

### What was exercised

**VERIFIED:** actively drove the rendered native vehicle through bounded input-file segments using `handling_playtest.tscn --oil-playtest --handling-flat`, read each resulting trace and inspected rendered screenshots before selecting the next segment. The fixture uses production movement/native vehicle physics and Core patch containment with initialized poses/speeds; item-authority/multiplayer behavior is exercised separately by the native Oil harness. Flat terrain is visually plain; its physical surface was explicitly set to Asphalt.

| Segment | Inputs | Direct observations |
| --- | --- | --- |
| Oil crossing | Initial 20 m/s, steer +0.6, throttle 0.7, 60 frames | Timer reaches 105 then ends at 54; peak yaw magnitude 0.412 rad/s, peak lateral magnitude 1.351 m/s. Ends at X=2.403 m, speed 26.24 m/s. [Trace](ts-172/entry.json), [image](ts-172/entry.png). |
| Countersteer | Steer -0.6, throttle 0.4, 90 frames | Yaw changes from -0.360 to +0.394 rad/s; timer reaches zero. Vehicle remains supported with 1000 HP. [Trace](ts-172/countersteer.json), [image](ts-172/countersteer.png). |
| Dry comparison | Reset to same initial 20 m/s pose and +0.6/0.7 inputs, no patch, 60 frames | Ends at X=4.014 m; peak yaw magnitude 0.572 rad/s and lateral magnitude 1.070 m/s. Oil therefore visibly/measurably widens the turn and increases slip in this comparison. [Trace](ts-172/dry.json). |
| Aggressive entry | Reset to 45 m/s, steer +1, throttle 0.8, handbrake held, 90 frames | Yaw starts at zero and develops to -1.486 rad/s; lateral speed reaches -30.205 m/s; timer ends at 20. Upright/supported, 1000 HP. [Trace](ts-172/fast-entry.json), [image](ts-172/fast-entry.png). |
| Release/countersteer | Release handbrake, steer -0.8, throttle 0.6, 180 frames | Timer reaches zero; sustained angular momentum continues, lateral speed declines to +1.667 m/s and longitudinal speed becomes -5.346 m/s. No forced instantaneous correction. [Trace](ts-172/fast-countersteer.json), [image](ts-172/fast-countersteer.png). |
| Straighten/recover | Steer 0, throttle 1, 180 frames | Forward speed returns to 23.28 m/s; yaw and lateral speed approach zero. Supported, 1000 HP. [Trace](ts-172/straighten.json), [image](ts-172/straighten.png). |

**INFERRED from trace:** integrated yaw over the aggressive entry plus release/countersteer is approximately 216 degrees over 4.5 seconds, consistent with a physics-driven spin rather than a contact-triggered rotation. Exact heading was not separately logged.

**VERIFIED:** inspected the rendered banked patch and Oil Configs screenshots; reviewed successful native multiplayer/recovery outputs. Runtime error output from the final segmented driving process was empty. The task-owned fixture was stopped after evidence collection.

### Runtime limitations

Input segments were selected interactively from observations but were not continuous human gamepad driving. Multiplayer game-fun/balance, audio, authenticated EOS/WAN latency, separate-device physics and maximum theoretical patch population were not judged. The score covers the exercised local runtime scope; it does not assert those unverified outcomes. No corrective round is recommended for the observed TS-172 scope.
