# TS-87 verification evidence

Recorded 2026-09-19 on `ts-87-jg`. Jira [TS-87](https://jonathangenier.atlassian.net/browse/TS-87), parent TS-84, completed dependencies TS-86/TS-93, linked downstream TS-121, comments, children and remote links were inspected. TS-87 has no children, comments or remote links. The approved 2026-09-19 replacement of generic results with a dedicated Podium is implemented. Final Jira reread retained the same description and update timestamp (`2026-09-19T18:49:37.706-0400`). No additional approved requirement changes were supplied or introduced.

Root/nested agent routing, workflow, standards, critique, feature index, `features.md` compatibility pointer, startup, match entry, Game Loop, sessions, reconnection, standings, Game Menu and EOS setup were inspected before implementation. Current `main` was synchronized into `ts-87-jg` during final PR review after TS-137 advanced the base. The Story version was recalculated from main `0.1.18` to canonical `0.1.19` and Windows preset `0.1.19.0`; this feature Story does not authorize changing the release prefix to 0.2.

## Implementation and acceptance evidence

| Requirement | Implementation and evidence |
| --- | --- |
| Finished → dedicated Podium | `DevelopmentSession` captures a session/generation-fenced `PostMatchContext` after synchronized authoritative results become available. `ApplicationStage.Podium` and `PodiumScene` own presentation. Native two/eight-peer harnesses observe this transition. |
| Authoritative outcome/standings | Podium uses exact `FinalMatchResults` winner and ordered rows, including per-match wins. It never calls ranking/scoring APIs. Pure tests use a mode winner with fewer kills than the runner-up; native checks compare handoff identity and peer results. |
| Retention through Finished | The arena/driver remain alive behind Podium for checkpoints, retained identities, standings and reconnect. Live metadata changes row connectivity without changing final results. Native tests observe a departed participant retained on Podium; existing reconnect harness covers final-result recovery. |
| Rematch through Loader/Sync | Local host restart applies Core Return cleanup and publishes a new generation. Session teardown disposes old driver/prediction/native arena, then reconstructs through the existing resource/checkpoint barrier. Native tests observe Loader without an arena immediately after Rematch, remote Loader, both peers synchronized, fresh scores/winner and a fresh countdown. |
| Repeated cycles | Two consecutive immediate rematches produce three Podium cycles. Further matches exercise other destinations. Core restart tests repeat three generations and verify map, connected identities, readiness reset, removed reservations and replica/codec acceptance. |
| Lobby / End Match | Host-only actions use the existing authoritative Return, leaving all connected players in a valid joined lobby with readiness reset. Native checks exercise both buttons and start another match from that lobby. |
| Main Menu | Any player can request reliable Leave and explicit Leaving state. Online cleanup may be retried and cannot automatically reattach transport during exit. Native checks verify client departure completion, host retained offline results, host teardown and the same MenuShell video owner resuming. |
| Quit | Podium invokes the bootstrap controlled shutdown, observes session cleanup and then lets the production bootstrap perform actual process exit. MenuShell no longer restarts media while Quit is pending. |
| Recoverable authoritative state | A synchronized accepted checkpoint can supersede a previous Podium context. Native host checkpoint fixture restores Active, observes Podium release, then restores the original final checkpoint. It separately verifies that ordinary remote phase packets cannot roll a terminal replica backward. This is not a live EOS migration test. |
| Controls/layout | Shared theme, remapped navigation, entry/overlay held-input fencing, native mouse release, disabled host actions for clients, paging and offline row treatment. Rendered captures at 640×360, 1280×720 and 2560×1080; small and standard captures visually inspected. ESC overlay open/close, unbound Enter rejection and remapped F8 Return are exercised. |
| Unrelated gameplay | No scoring, vehicle, damage, item, countdown, wire-format or transport implementation changes. Full deterministic suites and native combat/menu/standings/lobby/reconnect/migration checks passed. |

**Interpretations:** End Match ends the current shared match through existing host Return and preserves the joined lobby, as does Return to Lobby. It does not destroy the online session. Host Rematch includes currently connected participants without another lobby Ready vote; disconnected reservations end through Return cleanup. Main Menu/Quit retain the existing individual-departure/migration/reservation rules. These interpretations were surfaced during implementation and documented; they are not represented as new user-approved scope changes.

## Commands and observed results

Native executable for these checks: `C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe` (Godot 4.7.2 .NET). Rendered run used OpenGL compatibility on the NVIDIA RTX 4070 Laptop GPU.

| Command | Actual result |
| --- | --- |
| `./tools/sync-story-version.ps1`; `./tools/check-version.ps1` | PASS, current-main ancestry and canonical/preset values match. |
| `./check.ps1` | PASS: materialized frontend media/checksums and verifier regressions, restore, Debug/Release builds with warnings as errors, **469 Core + 336 non-native Client/transport tests per configuration**, no skipped tests or build warnings. Logs `.godot/ts-87-check.log`, `.godot/ts-87-final-check.log`. |
| `./check-post-match.ps1 -GodotPath <exe>` | PASS: final headless native flow, **75 assertions**, no warnings/errors. Includes actual production bootstrap exit after observing cleanup. Log `.godot/ts-87-post-match-final.log`; detailed assertions in the reported artifact directory. |
| `./check-post-match.ps1 -GodotPath <exe> -Visual` | PASS: **79 assertions**, including rendered layouts and native pointer visibility. Captures/evidence `.godot/post-match-checks/7f2880d731f4454dbd79e490be3d7074`. This rendered run observed controlled cleanup and allowed the harness to exit; the later headless run additionally returned process-exit ownership to the production bootstrap. |
| `./check-lobby.ps1 -GodotPath <exe> -NoBuild` | PASS: eight native sessions, both maps, two Finished/Podium handoffs, Return/disposal, ready/reset, active admission interruption/retry and retained-slot rejection. Log `.godot/ts-87-lobby.log`. |
| `./check-match.ps1 -GodotPath <exe> -NoBuild` | PASS: real missile/ram scoring across eight native peers, six death/respawn cycles and frozen final results. Log `.godot/ts-87-match.log`. |
| `./check-standings.ps1 -GodotPath <exe> -NoBuild` | PASS: eight peers, held keyboard/controller standings, ping/HUD ranks, final results, viewport-size assertions and Return cleanup. Headless; the script's “rendered sizes” message does not establish visual observation. Log `.godot/ts-87-standings.log`. |
| `./check-reconnect.ps1 -GodotPath <exe> -NoBuild` | PASS: three native UDP resyncs including 125 seconds offline, retained bodies, reset prediction/interpolation, items/match/standings continuity, offline final standings and fresh generation. Identity and initial scores are trusted fixtures. Log `.godot/ts-87-reconnect.log`. |
| `./check-migration.ps1 -GodotPath <exe> -NoBuild -Players 2` | PASS: lobby and active-match authority loss, sequential epochs, vehicle/item/spawn/match restore. Trusted local identity/fencing seam, not live EOS. Log `.godot/ts-87-migration.log`. |
| `./check-menu.ps1 -GodotPath <exe>` | PASS: **756 assertions**, menu/navigation/input/settings, Leave and controlled Quit. Log `.godot/ts-87-menu.log`. |
| `./check-startup.ps1 -GodotPath <exe>` | Final PASS: actual main scene, phase-aware dependency/frontend/application recovery and MenuShell continuity. Log `.godot/ts-87-startup-final.log`. The script now caps at 60 FPS so teardown frames allow real audio mixer time. |
| `git diff --check`, source/diff and affected documentation inspection | PASS: dependency direction remains Client → Core, no Shared layer or new third-party assets/dependencies. |

## Failed attempts and corrections

- A sandboxed build exited unsuccessfully without compiler diagnostics; the permitted elevated build used installed .NET/Godot caches and completed. Initial compile errors in verification fixture construction were corrected before final successful builds.
- First post-match native run rejected an invalid test fixture (kills exceeded recorded deaths). The fixture now satisfies existing Core snapshot invariants. No gameplay validation was weakened.
- An early native run passed 71 behavior assertions but failed strict shutdown validation with two leaked audio objects. Verbose diagnostics identified MP3 stream/playback objects. Production Quit was corrected to keep MenuShell suspended during cleanup; subsequent post-match headless/rendered runs exited cleanly.
- The initial ESC fixture supplied only `PhysicalKeycode`; it was corrected to supply both physical and logical codes, matching native input and existing menu harness conventions. Podium was also placed below the Game Menu/DevTools layers.
- The first rollback fixture incorrectly expected a normal phase publication to undo remote Finished. Existing terminal-publication fencing correctly rejected it. The final test uses local authoritative checkpoint restore and explicitly verifies remote rejection; no network fencing was changed.
- Two startup-only strict runs passed behavior assertions but reported two native audio-object leaks at uncapped headless shutdown. The same native scenario with `--max-fps 60` exited cleanly. The verification script was paced accordingly; the final strict startup check passed. This changes verification timing only.
- An initial menu invocation used unsupported `-NoBuild` and did not run; rerun with supported parameters passed.

## Limitations and explicitly unverified areas

- No separate-PC EOS match/rematch/reconnect, residential NAT, live lease-service failure, whole-process resume, multi-hour soak or TS-121 impairment validation was performed.
- Online membership cleanup failure/retry is integrated with the existing owner and inspected, but no new live service failure was induced from Podium. Generic provider-independent recovery tests passed in the full suite.
- The new native flow harness injects authoritative Finished state to isolate application navigation. Real combat completion was tested separately. The existing native reconnect harness validates driver/checkpoint results retention, not a complete EOS reconnect into the new Podium UI.
- The explicit rollback is a trusted host checkpoint fixture, not proof of live migration during Finished. Existing native migration regression passed separately.
- Native UI button signals and synthetic keyboard input were exercised. Physical controller ergonomics, manual alt-tab behavior, audible mix/loop quality and human gameplay assessment were not performed. Screenshots were visually inspected; no claim of listening to audio is made.
- The generic GdUnit suite, exported executable, cross-platform build and real EOS smoke were not run for this Story. The required root check and relevant native harnesses listed above were run.
- No Jira completion transition or merge was performed. Branch delivery is distinct from human critique acceptance.

## Historical Astra Self-Critique — Story Round 1

**Overall Score: 7.2 / 10.** This critique was recorded before TS-137 changed the runtime critique gate to 8.0; it does not satisfy the current final-delivery runtime gate. Category scores: functionality 7.5; architecture/code quality 7.5; UI/controls 7.0; stability/integration 7.5; networking confidence 6.5; testing/reliability 7.5.

**VERIFIED:** complete local flow and repeated Loader/Sync cycles, authoritative result presentation, native layouts and input, retained offline results, final Debug/Release checks and listed native regressions. **INFERRED:** unchanged EOS membership/authority ownership composes with Podium as documented. **UNVERIFIED:** live multi-PC EOS post-match transitions, membership-service failure and long-session behavior. These limits keep the score at solid milestone quality rather than a claim of production network polish. No further implementation or merge is authorized by this score; the repository human decision gate follows presentation of the critique.

## Synchronized runtime re-evaluation — 2026-09-20

Evaluated `e874f5f` on `ts-87-jg`, containing main `1ccc234`, canonical version **0.1.19** / Windows **0.1.19.0**. The initially stale local checkout was clean and fast-forwarded to the already-synchronized remote Story branch; no new merge or version change was made. Jira TS-87 was reread with its description, acceptance criteria, approved Podium update, dependencies, links and comments/children (none); its update timestamp remained `2026-09-19T18:49:37.706-0400`. No further approved requirement changes were found. Existing End Match/Return and host-rematch interpretations above remain assumptions, not newly approved scope.

Current root/Client instruction routing, workflow, standards, runtime-only critique rules, feature index and `features.md` pointer were inspected, along with relevant post-match, Game Loop, match entry, sessions, reconnect, standings, startup, Game Menu and EOS documentation and their implementation/harnesses. This re-evaluation does not repeat the independent static engineering review owned by ChatGPT. Historical results above are not substituted for the new checks below.

### New commands and actual results

`<exe>` below is the same Godot **4.7.2 .NET** console executable identified above. Rendered checks used OpenGL compatibility on the NVIDIA RTX 4070 Laptop GPU. Log paths are local, ignored verification artifacts, not committed files.

| Command | New result and evidence |
| --- | --- |
| `./tools/check-version.ps1` | PASS: current-main ancestry, canonical `0.1.19` and preset `0.1.19.0`. |
| `./check.ps1` | PASS, run once under the current gate: version/workflow/media verifier regressions, frontend materialization/checksums, restore, Debug production build and Release solution build with warnings as errors; **469 Core + 336 non-native transport tests once in Release**, zero failures/skips and zero build warnings/errors. `.godot/ts-87-runtime-r1-final.log`. |
| `./check-post-match.ps1 -GodotPath <exe> -NoBuild -Visual` — initial run | **FAIL strict runtime gate**, despite all **79 behavioral assertions** completing. One `ERROR: System.InvalidOperationException: Handle is not initialized.` appeared in `ScriptManagerBridge.SwapGCHandleForType`, reached through `ResourceLoader.LoadThreadedGet` / `MatchResourceLoader.Advance`. No behavioral assertion failure or process crash was observed. `.godot/ts-87-runtime-r1-podium.log`; artifacts `.godot/post-match-checks/39737085d7714f0eaf418744ec79eb2c`. |
| Same rendered post-match command — repeat | PASS: **79 assertions**, no `ERROR:`/`WARNING:`, controlled production exit. No concurrent build during this repeat. `.godot/ts-87-runtime-r1-podium-repeat.log`; artifacts `.godot/post-match-checks/20cc5bef9bce4193af945d4e51a2f059`. This does not erase or explain the initial error. |
| `./.godot/ts-87-runtime-r1-probe.ps1` | PASS: temporary verification-only extension of the existing native post-match fixture; Debug build, rendered controller/focus/overlay probe, then byte-for-byte fixture restoration and Debug rebuild. Runtime reports **89 evidence entries** (88 successful checks plus one descriptive focus observation), no warnings/errors. `.godot/ts-87-runtime-r1-probe.log`; artifacts `.godot/post-match-checks/8105bc116be148948419f191f2993aa3`. Restored fixture SHA-256 matches its pre-probe backup: `BC7560B4D3266A6EF0C6585DB7993A257129032B1C7AD0464A9D51A15F04E0FB`. |
| `./check-lobby.ps1 -GodotPath <exe> -NoBuild` | PASS, headless: eight production UDP sessions, both maps, two Finished/Podium handoffs, authoritative Return, readiness/reset, active admission and retained-slot capacity, host-loss cleanup. No warnings/errors. `.godot/ts-87-runtime-r1-lobby.log`; artifacts `.godot/lobby-checks/726fc914648c43f3b1ccd17c55520b72`. |
| `./check-reconnect.ps1 -GodotPath <exe> -NoBuild` | PASS, headless: 125 seconds offline and three real-UDP arena resyncs; retained identity/body, prediction/interpolation reset, items/match/standings continuity, final results and new-generation reset. Trusted identity and initial-score fixtures remain explicit. No warnings/errors. `.godot/ts-87-runtime-r1-reconnect.log`. |
| `./check-match.ps1 -GodotPath <exe> -NoBuild` | PASS, headless: eight-peer agreement across six real missile/collision death-respawn cycles, authoritative scoring and frozen final results. No warnings/errors. `.godot/ts-87-runtime-r1-match.log`; artifacts `.godot/match-checks/dc2a670df06549599b21ca3adee3deb8`. |
| `./check-menu.ps1 -GodotPath <exe> -Visual` | PASS: **801 assertions**, native keyboard/synthetic gamepad navigation, settings, overlays, pointer/focus checks, leave and production Quit. No warnings/errors. `.godot/ts-87-runtime-r1-menu.log`; artifacts `.godot/menu-checks/b6c4943d0fbe419eaf3029b686eae4e1`. |
| `./check-startup.ps1 -GodotPath <exe> -SkipBuild -SkipMediaCheck` | PASS: actual main scene, phase-aware dependency/frontend/application recovery and persistent MenuShell, controlled exit code 0, no warnings/errors. Build/media checks had already passed above. `.godot/ts-87-runtime-r1-startup.log`. |
| `dotnet build Trackstorm.Client.csproj -c Debug --no-restore -warnaserror -t:Rebuild` | PASS: forced baseline rebuild after fixture restoration, zero warnings/errors. `.godot/ts-87-runtime-r1-restored-build.log`. |

### What was exercised and observed

- **VERIFIED — flow and results:** native production startup and two local UDP sessions; authoritative Finished fixture reaches dedicated Podium on both peers; Podium receives the exact Core results object and peers agree on final ordered standings. Three Finished/Podium cycles with two immediate rematches traverse Loader/Sync on both peers, dispose prior match/prediction/results, advance generation once, reset winner/totals/life history and complete a fresh countdown. Duplicate rematch and client shared-action requests are rejected. Real combat completion was separately exercised by the eight-peer match harness.
- **VERIFIED — destinations:** Return to Lobby and End Match put both peers in the joined lobby and permit another match; readiness and match runtime clear. Client Main Menu completes reliable Leave while the host retains the frozen result and offline participant. Host Main Menu resumes the same MenuShell with no arena/Podium state. Podium Quit completes cleanup and lets the production bootstrap exit the process.
- **VERIFIED — retention:** post-match fixture preserves final results during client departure; a trusted host checkpoint rollback releases stale Podium and restoring the final checkpoint restores it. The reconnect harness separately exercises retained standings and final-result recovery. This is not an end-to-end authenticated EOS reconnect into Podium.
- **VERIFIED — presentation:** actual rendered captures inspected at 1280×720 and 640×360 from the initial run and 2560×1080 from the clean repeat. Winner, placement, local-player marker and table statistics are clear at normal/ultrawide sizes; no clipping was seen for the fixture's short names. At 640×360 the statistics, destination labels and helper text become very small. The two-player layout devotes six striped rows and a third plinth to empty content, while always displaying disabled paging controls.
- **VERIFIED — Podium controls/overlays:** ESC opens/closes Game Menu, an unbound Enter cannot bypass remapped Accept, and F8 returns to Lobby. Temporary probe injected native controller D-pad Left/Right, Start and A: navigation selected End Match/Main Menu, Start opened/closed Game Menu, and A initiated Rematch through Loader. F2 displayed Stats above Podium and Escape closed it. Captures `podium-controller-overlay.png`, `podium-overlay-return-focus.png`, and `podium-stats-overlay.png` were inspected. **Observed focus changed from Main Menu to Rematch after closing Game Menu.** Both overlays were visually above Podium; mouse capture was released. Input was synthetic, not a physical controller playtest.
- **VERIFIED — repeated use/stability limits:** all exercised destination sequences completed, including the initial run that emitted the native exception. Two subsequent rendered runs (baseline repeat and temporary probe) were clean. The initial resource-loading error remains intermittent and unexplained; the clean repeat is not proof of resolution.

### Astra Runtime Critique — Story Round 1

**Overall Score: 7.1 / 10**

**Quality Assessment: FAIL (<8.0)**

The exercised application flow is functionally complete and repeated local multiplayer cycles work. The observed result falls below the current polished-runtime gate because overlay return loses the selected destination, the small-window results presentation is difficult to read, and one complete native run emitted an unresolved resource-loading exception. The overall score is an experiential judgment, not an average of test counts or a static implementation score.

| Runtime category | Score | Observed basis |
| --- | --- | --- |
| Runtime Functionality | 8.5 | All destinations, authoritative results and fresh rematch barriers completed in local native sessions. |
| Controls / Responsiveness | 7.0 | Keyboard/remapped Accept and synthetic controller worked; closing an overlay unexpectedly reset destination focus. |
| Multiplayer / Networking Experience | 7.5 | Two/eight-peer convergence, retention and local reconnect passed; real EOS/remote-network post-match experience remains unverified. |
| Visual Quality | 6.8 | Clear winner hierarchy at standard size; sparse two-player composition and tiny small-window text lack refinement. |
| UI / UX | 6.7 | Actions are accessible and overlays legible, but selection continuity and small-window readability need correction. |
| Runtime Stability / Integration | 6.5 | Repeated lifecycle cleanup and surrounding integrations passed; an intermittent native loader exception prevents a clean stability assessment. |

#### Material findings and recommendations

1. **Problem:** an unexpected native exception occurs while exercising the complete post-match lifecycle. **Evidence:** initial rendered run's `Handle is not initialized` error and resource-loader stack above; 79 behavior assertions still completed and two later rendered runs were clean. **Severity:** Medium. **Impact:** the runtime fails its diagnostics gate; possible loading/lifetime consequences remain unknown even though this run continued. **Suggested Improvement:** reproduce and isolate the native resource/script-handle lifetime failure across repeated loading/teardown cycles, establish its trigger, then correct the responsible integration and demonstrate clean repeated runs. Do not attribute it to TS-87 production logic or concurrent builds without evidence. **Scope:** In Scope for TS-87 lifecycle verification; root cause/implementation ownership is not established. **Corrective Work Type:** New Corrective Task (recommended only; not created).
2. **Problem:** closing Game Menu loses the selected Podium action. **Evidence:** native probe selected Main Menu, opened/closed Game Menu with controller Start, and recorded/captured focus returning to Rematch. **Severity:** Medium. **Impact:** a player must navigate again; a subsequent Accept can target a shared restart instead of the previously selected exit. The changed focus is verified; an accidental human restart was not observed. **Suggested Improvement:** restore the prior enabled Podium control on overlay dismissal, with a safe fallback only if that control became unavailable, retaining held-input protection. **Scope:** In Scope. **Corrective Work Type:** New Corrective Task (recommended only; not created).
3. **Problem:** the small-window Podium fits but sacrifices readability, while the two-player composition reserves substantial space for empty rows, an absent third-place entry and disabled paging. **Evidence:** inspected 640×360 and 1280×720 captures show very small statistics/helper/action text and six blank table bands. **Severity:** Medium. **Impact:** final results and destination guidance require more effort to read, and the completion screen looks sparsely populated. **Suggested Improvement:** retain readable minimum type/control sizes through a compact responsive arrangement; use available space around actual participants and de-emphasize unavailable paging without removing required destinations or changing standings. **Scope:** In Scope presentation refinement; no new art system or gameplay redesign is proposed. **Corrective Work Type:** New Corrective Task (recommended only; not created).

**Recommended Next Round, only if human-authorized:** first isolate the native lifecycle error, then preserve destination focus across overlays and improve the compact results layout; rerun the affected native sequences and critique the observed result again. No corrective work or further critique round was started.

### Runtime limitations and delivery scope

**UNVERIFIED in this re-evaluation:** separate-PC/authenticated EOS post-match/rematch/reconnect, residential NAT/packet impairment, live membership cleanup failure/retry, live authority migration while Podium is open, whole-process resume, long-duration soak, physical controller ergonomics/alt-tab, audible quality, exported executables and other platforms. The standalone migration/standings harnesses were not rerun this round; their older results remain historical. Long-name, draw/no-winner and more-than-eight-result paging were not newly rendered. No frame-time benchmark or manual human gameplay session was performed; screenshots, native assertions and synthetic input establish only the observations stated above. The Finished fixture isolates Application Flow; the separate combat check does not establish one uninterrupted manually played combat-to-Podium session.

**INFERRED only:** provider-independent successful local cleanup composes with existing EOS ownership; this has not been promoted to verified online behavior. The initial strict runtime failure remains an unresolved risk even though subsequent runs passed.

No production code, persistent test code, assets, feature scope, Jira metadata or version changed. The temporary probe restored the tracked fixture exactly and rebuilt it; the only delivery change is this verification/critique report. Its existing verification-index entry remains accurate. Requested documentation commit/push is distinct from runtime acceptance. Per the user's instruction and current `docs/critique.md` human gate, stop for direction after presenting this **FAIL**; do not begin fixes, create corrective issues or start another polish round automatically.
