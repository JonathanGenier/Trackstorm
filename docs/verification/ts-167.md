# TS-167 — Machine Gun verification

## Round 3: authorized impact feedback and experiential follow-up — 2026-09-25

The user authorized reworking the remaining experiential gap and explicitly requested bullet-hit particles. Jira TS-167 was reread and updated with that approved feedback requirement. The existing 800/40/25 m profile, authority, scoring boundary and recovery contracts remain unchanged. No unrelated system was redesigned. Current main 790f08b remains integrated; sync-story-version confirmed 0.1.60 / 0.1.60.0.

### Changes

Confirmed Machine Gun hits now emit six small orange sparks, independent of tracer frequency, through ItemPresentation. Sparks reuse the acquired CC0 spark_01 texture, fade over 0.22 seconds and release their nodes after 0.3 seconds. A 128-burst shared presentation cap bounds catch-up allocation. Each impact scatters back along the incoming ray; this is a cosmetic direction approximation because the authoritative event has a hit point but no surface normal. Both vehicles and scenery receive hit particles. Miss endpoints do not create hit particles; scenery struck by a missed vehicle shot does. No new network fields, gameplay mutation, scoring or dependency was added.

Native verification now checks live bounded impact emitters on both peers. Both normal and impaired runs observed 12 simultaneous spark emitters on each peer during close sustained firing. The particles use accepted publication events; recovery continues to omit historical shots.

### New verification actually performed

All paths below are in [round-3](ts-167/round-3/). G is the same pinned Godot 4.7.2 .NET executable specified in Round 2.

| Command | Actual result |
| --- | --- |
| ./tools/check-fast.ps1 -Area Client | PASS, zero warnings/errors; [client.log](ts-167/round-3/client.log) |
| ./check.ps1 | PASS, 750 Core / 358 non-native transport tests and warning-free builds; [final-check.log](ts-167/round-3/final-check.log) |
| ./check-machine-gun.ps1 -GodotPath G -NoBuild -Visual | PASS, host/remote effects, range/cover/falloff, release/reuse, slot independence and exhaustion; [machine-gun.log](ts-167/round-3/machine-gun.log) |
| ./check-machine-gun.ps1 -GodotPath G -NoBuild -Visual -Impaired | PASS, same feedback checks with 30 ms latency, 5 ms jitter, 2% loss; [impaired.log](ts-167/round-3/impaired.log) |
| ./check-reconnect.ps1 -GodotPath G -NoBuild | PASS, three restores including 125 seconds offline, 299/800 rounds/phase 0.25 and no historical shot; [reconnect.log](ts-167/round-3/reconnect.log) |
| ./check-migration.ps1 -GodotPath G -NoBuild -Players 3 | PASS, sequential authority epochs and retained state; [migration.log](ts-167/round-3/migration.log) |
| ./check-hud.ps1 -GodotPath G -NoBuild | PASS, nine resolutions and state transitions; [hud.log](ts-167/round-3/hud.log) |
| ./check-audio.ps1 -GodotPath G -NoBuild | PASS, 54 assertions; [audio.log](ts-167/round-3/audio.log) |
| ./check-startup.ps1 -GodotPath G -SkipBuild | PASS; [startup.log](ts-167/round-3/startup.log) |
| git fetch origin main; ./tools/sync-story-version.ps1; git diff --check | PASS, version/export unchanged and no whitespace defects |

The initial successful native run preceded the comprehensive gate; the impaired/recovery checks and final interactive observations followed it. No production change followed final verification. Round 2's configuration persistence, two-peer migration and 11 native transport test evidence remains historical evidence for unchanged paths; those particular commands were not rerun in Round 3.

### Interactive runtime evidence

Launched G --path . --resolution 1440x900 res://scenes/verification/machine_gun_checks.tscn -- --machine-gun-playtest. The initial desktop attempt reproduced the foreground-process-id failure and an earlier playtest window exited. After the user confirmed an unlocked/focused desktop, resetting the helper and activating the uniquely selected returned window directly recovered control. The subsequent interaction succeeded; this resolves Round 2's inability to perform new interactive observations. [Successful launch/clean exit log](ts-167/round-3/playtest-retry.log). Exit code 0 with no runtime diagnostics.

The operator used the native fixture's Fire/release, range, target movement, tracking and refill controls. These drive production held-input/authority/presentation; target motion and aim tracking are fixture-controlled, not manual driving/steering. Screenshots were inspected after state changes.

- **VERIFIED:** at 5 m, close hits produced visible sparks and steady HP reduction. Release stopped at **470/800 rounds**, **59% HUD**, target **9257.5 HP**. Changing range and target movement while released retained exactly 470 rounds and that HP. Reuse resumed spending the partial magazine. [Partial resource](ts-167/round-3/partial.png).
- **VERIFIED:** at 15 m, toggled a moving target and compared fixed aim with tracking. Fixed aim visibly sent shots beside the moving vehicle; tracking turned the shooter toward the target and produced readable contact sparks. [Tracked fire](ts-167/round-3/tracking.png).
- **VERIFIED:** a fresh 800-round magazine at 15 m with moving target/tracked aim reduced target HP from **8943.7 to 7894.0**, approximately **1049.7 HP**, then exhausted. The gun slot became Empty while the Wrench remained. [Tracked exhaustion](ts-167/round-3/tracked-exhaustion.png).
- **VERIFIED:** a fresh 800-round magazine at 24 m reduced HP from **7894.0 to 7838.6**, approximately **55.4 HP**. Wide tracer misses and occasional smaller hit bursts were visible. [Far firing](ts-167/round-3/far-firing.png), [far exhaustion](ts-167/round-3/far-exhaustion.png).
- **VERIFIED:** a full magazine at 28 m left the target at **7838.6 HP**, with normal ammunition depletion and empty transition. Released effects cleared. [Beyond-range release](ts-167/round-3/cutoff-released.png).
- **VERIFIED:** native captures show particles on both a vehicle and solid cover; scenery stops damage but still communicates impact. [Vehicle](ts-167/round-3/native-impact.png), [scenery](ts-167/round-3/scenery-impact.png). No target-obscuring large explosion was introduced.

Displayed HP has one decimal place; these damage differences are approximate. Distances are centre separations and native hit distance is to the collider surface. Exact 20-second/800-round timing, cone widths and mathematical falloff are rechecked by the 750-test Core suite; this interactive sequence is not a stopwatch timing experiment.

### Astra Runtime Critique — Story Round 3

- **Overall Score: 8.2 / 10**
- **Quality Assessment: PASS (>=8.0)**. This is a new judgment after final verification and successful interactive observations, not an adjustment of the prior score to satisfy a requested threshold.
- **Category scores:** Runtime Functionality **8.5**; Weapon Feedback **8.4**; Resource Controls/UI **8.3**; Close/Far Gameplay Profile **8.1**; Multiplayer Runtime/Recovery **8.4**; Runtime Stability **8.5**. Overall is a qualitative judgment with the limitations below, not an arithmetic average.
- **What was exercised:** VERIFIED repeated operator-controlled firing/release/refill, partial retention, moving-target fixed/tracked aim, full magazines at 15/24/28 m, contact sparks, HUD exhaustion and retained Wrench; normal/impaired native host/remote use and reconnect/migration. The new particles make landed rounds distinguishable from long-range spray while keeping the target silhouette readable. Resource transitions are clear. Close/mid-range fire builds useful sustained damage; far spray remains visibly unreliable with much lower pressure. The exercised sessions exited cleanly.
- **INFERRED:** the profile should reward close-range tracking in actual driving combat; the fixture's tracking control demonstrates the aim-dependent outcome but does not prove competitive handling balance.
- **UNVERIFIED / limitations:** subjective audio listening/mix; free-body knockback feel; human steering/gamepad skill; real EOS/WAN or separate-device multiplayer; eight simultaneous guns and frame-time profiling. Controlled poses and 10000 target HP isolate weapon behavior. The operator observed sampled native frames rather than recording continuous video; no measured FPS claim is made. Audio integration assertions and numerical impulses do not substitute for hearing/feeling those effects. These limitations bound the passing judgment and are not claimed as successes.

No additional production change is proposed by this critique. The formal Round 3 presentation is the human decision gate; no merge or Jira completion transition is performed.

---

## Round 2: approved balance/profile update — 2026-09-25

This is historical Round 2 evidence; the authorized Round 3 above supersedes its critique and desktop limitation. Jira TS-167, parent TS-115, PR #107 review findings, repository routing/workflow/standards/critique and affected feature contracts were inspected. TS-167 already contained the approved profile; TS-115's stale Machine Gun paragraph was updated to match, preserving other Epic scope. Static review requested no production redesign.

Branch: ts-167-jg, based on f85f2c3. A fresh origin/main fetch confirmed main 790f08b remains integrated. The Story version procedure confirmed 0.1.60 and Windows export 0.1.60.0. Newer main behavior is retained.

### Exact profile and changes

Only seven production defaults changed in ItemConfiguration: capacity **800**, rate **40 rounds/s (2400 RPM)**, maximum ray length **25 m**, falloff start **12 m**, cone half-angle **6 degrees** (12 full), near damage **2.25 HP**, near central impulse **8 N s**. Existing exponent **1.5** and tracer frequency **every 2 rounds** remain. Intended effective range is 5–15 m. Perfect near-range full-magazine damage is 1800 HP (180% of a 1000 HP vehicle), independent of configured vehicle health.

The existing authoritative sampler already chooses each ray inside the cone from the current observed orientation; no homing, drift or steering implementation was added. Existing resource ownership, native closest-hit validation, lifecycle, two-slot state, HUD percentage, configuration and recovery paths remain. Tests now exercise the new defaults, changing orientation every tick, measured spread/accuracy, native near/far hit rates and 299/800 recovery. Native playtest range controls now offer 5/15/24/28 m. Feature docs describe the current defaults.

The existing damage context remains source machine-gun, instigator vehicle/player, cause bullet. Nonlethal damage earns no Circus points; generic kill awards remain unchanged. TS-155 owns future shared item-damage scoring. Existing explicitly saved configuration overrides are retained; these new defaults do not overwrite a user's tuned profile. Capacity is captured on pickup and live edits do not refill held items.

### Commands actually run and results

G below is C:/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe. Logs are in [round-2](ts-167/round-2/).

| Command | Result and evidence |
| --- | --- |
| ./tools/check-fast.ps1 -Area Core | PASS, 750 tests; [core.log](ts-167/round-2/core.log) |
| ./tools/check-fast.ps1 -Area Client | PASS, Debug production build, zero warnings/errors; [client.log](ts-167/round-2/client.log) |
| ./check.ps1 | PASS, full repository checks, Debug/Release builds, 750 Core and 358 non-native transport tests, zero warnings/errors; [final-check.log](ts-167/round-2/final-check.log) |
| dotnet test code/Tests/Trackstorm.Core.Tests.csproj -c Release --no-build --filter FullyQualifiedName~DefaultConeExpands --logger 'console;verbosity=normal' | PASS, 1 measured spread test; [spread.log](ts-167/round-2/spread.log) |
| ./check-machine-gun.ps1 -GodotPath G -NoBuild | PASS, real local UDP host/remote firing; [machine-gun.log](ts-167/round-2/machine-gun.log) |
| ./check-machine-gun.ps1 -GodotPath G -NoBuild -Visual -Impaired | PASS, rendered native check, simulation tuple 30 ms latency / 5 ms jitter / 2% loss; [machine-gun-impaired.log](ts-167/round-2/machine-gun-impaired.log) |
| ./check-reconnect.ps1 -GodotPath G -NoBuild | PASS, three arena resyncs including 125 seconds offline, 299/800 ammo and phase 0.25, no historical shot replay; [reconnect.log](ts-167/round-2/reconnect.log) |
| ./check-migration.ps1 -GodotPath G -NoBuild -Players 3 | PASS, sequential authority epochs and full retained state; [migration-3.log](ts-167/round-2/migration-3.log) |
| ./check-migration.ps1 -GodotPath G -NoBuild -Players 2 | PASS, sole-survivor recovery and retained resources; [migration-2.log](ts-167/round-2/migration-2.log) |
| ./check-hud.ps1 -GodotPath G -NoBuild | PASS, state/units/slots and nine resolutions; [hud.log](ts-167/round-2/hud.log) |
| ./check-developer-options.ps1 -GodotPath G | PASS, write 924 assertions and read 9; [config.log](ts-167/round-2/config.log) |
| ./check-audio.ps1 -GodotPath G -NoBuild | PASS, 54 integration assertions; [audio.log](ts-167/round-2/audio.log) |
| ./check-startup.ps1 -GodotPath G -SkipBuild | PASS; [startup.log](ts-167/round-2/startup.log) |
| dotnet test code/TransportTests/Trackstorm.Transport.Tests.csproj -c Release --no-build --filter 'TestCategory=Native' | PASS, 11 native tests; [native.log](ts-167/round-2/native.log) |
| git fetch origin main; ./tools/sync-story-version.ps1 | PASS, current main integrated; expected/actual 0.1.60 and 0.1.60.0 |
| git diff --check | PASS |

No production defect was exposed by these checks. Incorrect exploratory paths tools/check-machine-gun.ps1 and docs/features/reconnect*.md were corrected to the existing root script and reconnection.md; those failed reads are not verification runs.

### Measured behavior

- **Depletion VERIFIED:** 799 shots at tick 1199; exactly 800 shots and one Exhausted event at tick 1200, with no later firing. At 60 Hz this is **20.000 simulated seconds**. Native partial-magazine reuse exhausts within the fixture's bounded 35-tick remote-delivery allowance of remaining rounds × 60/40. This is simulated timing, not a separately stopwatch-measured wall-clock claim.
- **Range VERIFIED:** every observed authoritative ray is 25 m, including changing yaw/pitch. Damage at an exact 25 m hit is zero. Native target at 28 m centre separation (nearest collider beyond 25 m) receives zero damage; solid cover also receives no target damage.
- **Spread VERIFIED:** 800 actual authoritative directions projected onto perpendicular planes produce the following widths and centred 2 × 1.5 m rectangle hits. This rectangle is an analysis target, not the native vehicle collider. All samples remain inside the configured cone relative to that tick's orientation.

| Plane distance | Measured horizontal width | Hits |
| --- | --- | --- |
| 5 m | 1.0465 m | 800/800 = 100% |
| 15 m | 3.1395 m | 286/800 = 35.75% |
| 24 m | 5.0232 m | 97/800 = 12.125% |

- **Falloff/impulse VERIFIED:** 2.25 HP and 8 N s through 12 m; approximately 1.517985 HP at 15 m, 0.536689 HP at 20 m, zero at 25 m. Impulse follows the same fade. Full near-range potential remains 1800 HP. These are authority assertions, not free-body subjective knockback observations.
- **Native hit reliability/damage VERIFIED:** at 8 m centre separation, 79/80 hits and 177.75 HP in the host two-second burst; at 24 m, 16/80 hits and 4.58 HP. Actual hit distance is to the collider surface. Beyond range and behind cover: 0/80, zero HP. Normal remote burst: 80 rounds/180 HP; impaired remote burst: 76 rounds/171 HP within the observation window, with eventual depletion convergence. Delayed remote input accounts for the shorter observed burst.
- **Lifecycle VERIFIED:** release retains ammunition after in-flight input settles, reuse continues, switching to the independent Wrench preserves the magazine, depletion clears only the gun slot, repeated fresh grants work. Core coverage also exercises two Machine Guns with independent resources, death/reset, stale ownership/capabilities, no adapter/no invented shots, configuration edits without refill, restored deterministic endpoints and TS-155 attribution boundary. Native recovery retains a deliberately seeded partial magazine rather than replaying fire during restoration.

### Runtime observation and limitations

After comprehensive checks, the new rendered native captures were opened and inspected: [near firing](ts-167/round-2/host-firing.png), [far spray](ts-167/round-2/far-spray.png), [exhaustion](ts-167/round-2/exhausted.png). **VERIFIED:** legible Machine Gun name/90% resource, independent Wrench slot, visible thin directional tracer, far miss trajectory, and empty-slot transition with Wrench retained. Static captures do not establish animation smoothness or sustained tracking feel.

An interactive run was actually launched with G --path . --resolution 1440x900 res://scenes/verification/machine_gun_checks.tscn -- --machine-gun-playtest. Two native peers admitted ([launch log](ts-167/round-2/playtest.log)). Window discovery initially returned a stale handle; refreshed selection then failed capture/activation twice with **foreground window did not report a process id**. No successful new interactive inputs or screenshots were obtained. The task's playtest processes were closed after that failure; no interactive pass is claimed.

**UNVERIFIED this round:** hands-on sustained tracking, moving-target feel at 5–15 m, subjective 40 rps sound/visual rhythm, listening/audio mix, free-body knockback feel, physical gamepad use, live EOS/WAN sessions, separate-machine latency, and high-player-count stress. The native fixtures use controlled poses and 10000 HP to isolate weapon behavior; pose resets cannot validate free-body feel. Network identity and lease behavior use documented test seams. Repeated grants and automated slot use do not substitute for manually driving through pickups this round. Old Round 1 playtests do not close these gaps after balance changes.

### Astra Runtime Critique — Story Round 2

- **Overall Score: 7.7 / 10**
- **Quality Assessment: FAIL (<8.0)**. The new profile's operational behavior is coherent, but the material experiential uncertainty does not support the required highly polished runtime judgment. The score is not rounded up from missing evidence.
- **Category scores:** Runtime Functionality 8.5; Multiplayer Runtime/Recovery 8.4; Observed UI/Feedback 8.0; Runtime Stability 8.5. Gameplay/Fun, Controls/Tracking Feel, free-body Physics and subjective Audio are unscored because they were not directly exercised successfully this round. Overall is a judgment including material uncertainty, not an arithmetic average of verified categories.
- **What was exercised:** VERIFIED native near/far/range/cover bursts, host/remote and impaired delivery, release/reuse/depletion, inventory/HUD, recovery and configuration workflows; directly inspected current native images. INFERRED: the measured widening pattern should reduce precision at long range. UNVERIFIED: whether the revised weapon is satisfying to track and fire continuously in actual driving combat.
- **Problem:** the updated 40 rps / 6-degree cone profile lacks successful fresh interactive feel validation. **Evidence:** failed window capture/activation, static captures and controlled-pose fixtures only. **Severity:** Medium. **Impact:** tuning may be mechanically correct while tracking, audio cadence or free-body feedback is unsatisfactory. **Suggested Improvement:** complete a hands-on native playtest of moving targets at 5/15/24 m, release/reuse and an 800-round burst, listen to the 40 rps feedback and observe free-body impulses; then reassess. **Scope:** In Scope. **Corrective Work Type:** No Code Change Required unless playtesting exposes a defect.
- **Recommended Next Round:** after human authorization, recover the desktop playtest path or have a human perform the missing experiential checks, record actual findings, and issue the next critique. No production redesign is recommended by this evidence.

The approved balance change and evidence are ready for review, but the Story's >=8.0 runtime acceptance gate remains unmet. No merge or Jira completion transition was performed.

---

# Historical Round 1 — superseded 500-round profile

Completion verification: 2026-09-25. Story branch: ts-167-jg. Jira TS-167 and parent TS-115 were reread; neither has comments or required child tasks. PR #107's static review requested completion evidence, formal runtime critique and green CI, with no substantive production-code correction. Reviewer maintenance a0c602d is retained. Current main 7f5e35c is an ancestor; canonical version 0.1.57 and export version 0.1.57.0 passed the repository version procedure.

## Implemented and scope boundary

The existing Weapon registry/category pool includes Machine Gun. Each physical slot captures 500 rounds, shown as 100%; held input fires 25 rounds/second. Only emitted native rays spend ammunition; release and switching retain rounds/cadence. The normal slot lifecycle disposes of exhausted magazines. Host-observed aim and closest-hit terrain/vehicle rays enforce a 10 m maximum. Defaults are 3.5 HP/round, 12 N s central impulse, 3-degree spread, full damage through 5 m and exponent-1.5 falloff to zero at 10 m. Perfect near-range ammunition totals 1750 HP against the hosted 1000 HP baseline; this is tunable rather than health-derived.

Existing authority, complete item publications/checkpoints, configuration, HUD and presentation paths carry ammunition, firing and restoration. Nine tuning controls plus generated spawn weight are in the existing 141-key catalog. Item/configuration wire versions are 11/17. Tracers and small muzzle/hit flashes are presentation-only. Per-round sound reuses a licensed existing sample. No new dependency was acquired.

Damage uses the normal vehicle damage transaction and source machine-gun, instigator player and bullet context. Nonlethal bullet damage awards no Circus points. Lethal damage receives the existing generic kill credit/kill award; no machine-gun-specific or temporary damage-scoring path exists. TS-155 remains the owner of shared item-damage scoring.

## Completion changes

No production gameplay redesign was needed. Added explicit regression assertions for lethal attribution versus damage scoring and repeated-round audio/revision deduplication. Corrected the native migration fixture to seed its partial machine gun on the available participant instead of the successor whose two slots deliberately contain Wrench/Oil. Preserved reviewer documentation/provenance and synchronized remaining configuration/audio/network schema references.

## Deterministic verification

- `./check.ps1`: PASS; version/workflow/media regressions and checksums, restore, Debug production build and Release solution build with warnings as errors; 709 Core and 358 non-native transport tests passed. Zero build warnings/errors. [Log](ts-167/final-check.log).
- `./tools/sync-story-version.ps1`: PASS; 0.1.57 / 0.1.57.0 matches current main plus one.
- Earlier focused `./tools/check-fast.ps1 -Area Core`, `-Area Client`, `-Area Transport` passed after relevant changes. The first lethal-score assertion incorrectly expected zero despite the existing 100-point kill award; corrected the test to assert exactly the generic kill award, with nonlethal zero-score cases retained. No scoring production change followed that failed assertion.

Core machine-gun coverage directly exercises 499 rounds at tick 1199, round 500/one Exhausted event at tick 1200, no later fire, release/reuse, two magazines and switching, stale/wrong-owner capabilities, originating-input sequence gating, disconnect neutralization, malformed native-hit rollback, cover/misses/missing adapter, damage/falloff/impulse/attribution, configuration edits without refilling, invalid continuation, death/reset, and restored authority with deterministic subsequent shot endpoints. Audio tests exercise unique rounds sharing a token, duplicate revision suppression, seed silence and no explosion cue.

## Evidence interpretation

All native commands below use the installed pinned Godot 4.7.2 .NET executable at C:/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe (abbreviated G below). Local UDP is real GameNetworkingSockets transport, with identity/lease seams where documented. Native machine-gun fixtures reset controlled poses each frame and use 10000 target HP to avoid death interrupting resource measurements. Distances in its scenario names are centre separations; actual damage range is measured to the first collider surface.

Earlier implementation attempts exposed missing Godot imports (MachineGun.svg, then newly integrated Salvo.svg); a pinned headless editor import fixed the local cache. The migration fixture initially tried to grant a third item and failed; the available-participant correction is included and passed the final native run. An earlier startup invocation used unsupported -NoBuild and did not execute startup; the final command omits that flag. Those failed attempts are not counted as passes.

## Final native/runtime commands and results

| Command | Actual result | Evidence |
| --- | --- | --- |
| `./check-machine-gun.ps1 -GodotPath G -NoBuild` | PASS; host and remote two-second bursts both spend 50 rounds and apply 175 HP at 6 m centre spacing; partial retention/reuse, second-slot Wrench and full depletion converge | [Normal UDP](ts-167/machine-gun.log) |
| `./check-machine-gun.ps1 -GodotPath G -NoBuild -Visual -Impaired` | PASS; 30 ms outbound latency, 5 ms jitter, 2% loss; host 50 rounds/175 HP, remote 47 rounds/164.5 HP before delayed release settles; exact remaining ammo later converges and depletes on schedule | [Impaired UDP](ts-167/machine-gun-impaired.log) |
| `./check-hud.ps1 -GodotPath G -NoBuild` | PASS; state changes, partial machine-gun percentages and nine resolutions | [HUD](ts-167/hud.log), [1280×720](ts-167/hud-1280x720.png) |
| `./check-audio.ps1 -GodotPath G -NoBuild` | PASS; 54 native audio assertions | [Audio](ts-167/audio.log) |
| `./check-developer-options.ps1 -GodotPath G` | PASS; 859 write/apply/replication assertions and 9 fresh-process persistence assertions | [Configuration](ts-167/options.log) |
| `./check-reconnect.ps1 -GodotPath G -NoBuild` | PASS; three resyncs, including 125 seconds offline; every boundary retains 187/500 rounds and phase 0.25 without historical shots; body reuse, match/item state and new-generation reset | [Reconnect](ts-167/reconnect.log) |
| `./check-migration.ps1 -GodotPath G -NoBuild -Players 3` | PASS; lobby host leave, former-host fresh admission, active-match authority loss and sequential epochs; exact partial machine-gun resource restored | [Three peers](ts-167/migration-3.log) |
| `./check-migration.ps1 -GodotPath G -NoBuild -Players 2` | PASS; two-peer/countdown recovery regression, retained vehicles and item/world restore; partial machine-gun fixture is specific to the three-peer run | [Two peers](ts-167/migration-2.log) |
| `dotnet test code/TransportTests/Trackstorm.Transport.Tests.csproj -c Release --no-build --filter 'TestCategory=Native'` | PASS; 11 native tests | [Native](ts-167/native.log) |
| `./check-pickup-drive.ps1 -GodotPath G -NoBuild -Impaired` | PASS; both application peers drive into real markers and acquire every registered item, explicitly including MachineGun; sequential slots, full rejection/use/retry; 180/180 moving pickup attempts | [Pickup](ts-167/pickup.log) |
| `./check-startup.ps1 -GodotPath G` | PASS; native headless application startup/recovery/navigation | [Startup](ts-167/startup.log) |

The machine-gun range scenarios applied 54.71 HP at 10 m centre separation (near collider face is inside 10 m), zero at 14 m and zero through a solid wall. The Core ray-bound test checks 10 m endpoints and exactly zero damage at the 10 m hit distance. Native exhaustion duration is checked against remaining rounds × 60/25 with bounded remote delivery allowance; exact full-magazine timing is independently asserted at 1200 Core ticks. Native fixture pose resets prevent this test from proving free-body knockback feel.

Earlier on the same combined production implementation, `check-salvo.ps1 -NoBuild -Impaired` passed three peers/15 impacts and `check-items.ps1 -NoBuild` passed eight-peer two-slot/Wrench/Missile regressions. These earlier checks were observed directly in this task; their logs are not presented as fresh completion-run artifacts.

## Interactive observations

Launched `G --path . res://scenes/verification/machine_gun_checks.tscn -- --machine-gun-playtest` and actively clicked the fixture controls through Computer Use. This uses production drivers, rays, damage and HUD with controlled poses; moving target and tracking are scripted fixture choices, not a human driving duel.

- At 7 m, continuous fire reduced 500 to 456 rounds and target HP to 9846. Release later retained 312 rounds at 9342 HP; another observation after changing target motion still showed exactly 312 rounds and the HUD showed 63%. Reuse consumed the partial magazine.
- Moving target with fixed aim showed a thin gold miss tracer while rounds fell to 267 and target HP remained 9342. Enabling tracking visibly rotated the shooter toward the target; the last 30 rounds reduced HP from 8950.2 to 8845.2. Exhaustion displayed EMPTY with Wrench intact. This demonstrates targeting consequences without establishing competitive steering difficulty.
- Refilling restored 500 rounds/100%. At 14 m with tracking enabled, firing then release left 242 rounds while target HP stayed exactly 8845.2: 258 actual rounds spent without distant damage.
- Moving the fixture to 3 m and resuming reduced 242 to 197 rounds and HP from 8845.2 to 8687.7, exactly 45 × 3.5 HP. At this centre separation the large vehicle meshes can overlap during scripted motion; this is not collision/close-driving evidence.
- Native [host firing](ts-167/host-firing.png), [exhaustion](ts-167/exhausted.png) and HUD captures were viewed. The name/percentage, selected slot and empty transition were legible. Tracers communicate shot direction with a small visual footprint; the UI communicates the resource independently.
- Audio execution/routing/deduplication is verified, but no subjective listening claim is made. Authoritative central impulse magnitude/falloff is verified by the Core transaction assertions (12 N s maximum per hit); the interactive pose-controlled fixture cannot establish free-body knockback sensation.

The interactive Finish button exposed a fixture-only teardown race (transport disposed before the next polling step). Fixed by marking completion and queuing arena teardown before disposing transports. A focused `check-fast.ps1 -Area Client` build and the full `check.ps1` were rerun successfully after this correction; the final full-check log above is that later run.

The final interactive repeat ran **after** the last comprehensive check and affected impaired-native rerun. Continuous stationary 7 m fire spent all 500 rounds, reduced target HP from 10000 to **8250**, displayed EMPTY, and retained Wrench. [Observed final state](ts-167/interactive-exhausted.png). The corrected Finish button exited with code **0**, with no exception/warning in [the final runtime log](ts-167/interactive-final.log).

## Acceptance coverage

| TS-167 criterion | Evidence |
| --- | --- |
| Obtain/equip/use/exhaust through existing flow | Both real application peers acquire MachineGun by driving into production pickups; driver-native firing and observed empty transition |
| Complete continuous 100%→0% lifecycle, release/reuse | Exact 1200-tick Core budget, normal/impaired native depletion, interactive full magazine and retained 312-round partial magazine |
| Correct source/attacker attribution | Core actual damage attribution and lethal generic kill-credit assertions; no nonlethal Circus damage points |
| No damage beyond maximum range | 10 m ray endpoint/zero-edge Core checks; native beyond-range/cover scenarios and interactive 258-round distant burst with unchanged HP |
| Multiplayer/repeated use/recovery | Host/remote UDP, impaired release convergence, repeated native grants/pickups, two-slot tests, reconnect and 2/3-peer migration |
| Existing developer tuning | 141-key catalog native apply/replication/persistence run plus live machine-gun configuration/no-refill Core assertions |
| TS-155 sequencing | Production source inspection and zero nonlethal score assertions; no damage-score implementation added |

## Astra Runtime Critique — Story Round 1

**Overall Score: 8.1 / 10**  
**Quality Assessment: PASS (>=8.0)**

This is an experiential judgment of the exercised scope, not a static engineering score or a rounded test-count average.

| Relevant category | Score | Observed basis |
| --- | --- | --- |
| Runtime functionality | 8.8 | Repeated acquisition/use, retained partial resources, complete depletion, bounded range and successful restore across native lifecycle transitions |
| Close-range usefulness / damage pacing | 8.0 | Sustained tracking produces gradual meaningful HP pressure; missed/distant rounds cost ammunition; one perfectly landed magazine produced 1750 HP rather than a missile-like single burst |
| Controls / responsiveness | 8.0 | Hold/release and reuse produce predictable resource transitions; remote behavior converges under the exercised impairment, with expected delivery delay |
| VFX / feedback | 7.8 | Thin short gold traces communicate direction without obscuring vehicles; small impact/muzzle treatment is restrained; clarity also relies on the percentage/empty state |
| UI / UX | 8.2 | Registry icon, selection and independent Wrench slot remain readable; rounded partial percentage and exhaustion transition are clear in viewed captures |
| Multiplayer / runtime integration | 8.6 | Host/remote native use, repeated pickup, exact checkpoint resources and sequential authority replacement remained consistent in the exercised local runs |
| Runtime stability | 8.5 | Final native suites and the corrected interactive teardown completed cleanly; no crashes/exceptions or sustained-resource failure observed on the final result |

**What was exercised:** VERIFIED — actual interactive firing/release/refill, moving fixed aim versus tracking, 3/7/14 m fixture positions, full magazine and rendered HUD/tracers; native normal/impaired host/remote fire, range/cover/falloff, independent inventory and recovery. VERIFIED numerically — low 12 N s impulse/falloff and discrete cadence. INFERRED — usefulness in a competitive driving duel from controlled moving-target behavior. UNVERIFIED — subjective audio quality, free-body knockback sensation and human competitive tracking. Those are not silently scored as observed successes; no Audio or Vehicle Feel category score is assigned.

## Assumptions, limitations and risks

- Controlled native poses isolate weapon hits from steering/collision noise. They deliberately overwrite motion, so neither screenshots nor the range fixture establish free-body impulse feel. At 3 m centre spacing vehicles can overlap; that observation is only hit/resource evidence.
- Interactive controls toggle production held input and scripted tracking. They do not replace a keyboard/controller driving duel, independent-device latency assessment or competitive balance session. A perfectly aligned large target at 7 m centre separation has its near surface inside the full-damage region.
- Sound assets load, voices execute and cues deduplicate correctly, but no listening/volume/spatialization quality claim is made.
- Local UDP impairment is 30 ms outbound latency, 5 ms jitter and 2% loss. Real authenticated EOS, NAT traversal, Internet play, exported builds and independent physical machines were not exercised in this Story completion run. Recovery identity/lease seams are described by the native harnesses.
- Reliable complete item publications during sustained fire increase traffic. Two firing peers and existing eight-peer item regressions do not prove eight simultaneous machine guns over a constrained Internet link. No long soak or formal frame-time/bandwidth benchmark was performed.
- Future shared TS-155 damage scoring remains intentionally absent. Existing ordinary kill awards remain intact. The 1750 HP theoretical total is an initial balance value, not a promise about human accuracy.
- Ten pre-existing untracked rock texture PNGs were left untouched and excluded from the Story commit.
- No unresolved production defect was found in the exercised scope. Human critique acceptance and the existing independent PR review process remain separate from this evidence; the Story PR is not merged.
