# TS-100 verification

## Scope and implemented behavior

Jira TS-100 was inspected on 2026-09-22, including the complete description, empty comments/children, dependency TS-73 (Done), and attachment 10018, **Map Concept 0.2.0 weapon spawn position.png**. The attachment was viewed in Jira at full-map scale and enlarged/panned over both turns. Only its red/green placement annotations were used. No approved scope changes were identified.

- Twenty authored pickups: five perpendicular triple rows with 3/9/15 m surface lanes, and five singles projected onto the measured production oval. Existing pickup authority, item selection, collection radius, cooldowns and replication are reused. Capacity validation/codec bounds now permit up to 20 pickups independently of eight player slots.
- Continuous 1.3 m concrete outer barrier; 229 overlapping convex prisms, four metres thick outward, extend from below the local rim to world y=45 m. No inner wall. Foundation geometry, banking, infield collision and vehicle tuning are unchanged.
- Original Blender conifers (three variants, 864 instances in 48 spatial batches), exterior-only ground/scenery, rough asphalt and grass, and a blue daytime sky selected by the existing arena environment owners. Old Map lighting/pickups remain intact.
- Feature/asset documentation, original-art hashes, native verification and the Story version were updated. `main` through `97a2057` (TS-132 camera obstruction) was merged and Story version synchronized to `0.1.29`.
- The pre-existing whitespace edit in `code/Core/Input/InputButtons.cs` was preserved and excluded from TS-100 commits.

## Commands and actual results

Godot used: `C:/Users/orsin/OneDrive/Desktop/godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe` (`$g` below), Godot 4.7.2 .NET. Blender: `C:/Program Files/Blender Foundation/Blender 5.2/blender.exe`, 5.2.2 LTS. Rendered runs reported OpenGL Compatibility on NVIDIA GeForce GTX 1070.

| Command | Actual result |
| --- | --- |
| `blender --background --python-exit-code 1 --python assets/maps/oval/BuildEnvironment.py` | Exported the blend, three conifer variants and original grass maps. Final run completed successfully. |
| `& $g --headless --path . --editor --import` | Final Godot import completed without reported errors/warnings. |
| `& $g --path . --script assets/maps/oval/BuildOvalScene.gd` | Baked static scene successfully using the real renderer. |
| `./tools/check-fast.ps1 -Area Core` | 517 tests passed after correcting the obsolete eight-only pickup-count test. |
| `dotnet build Trackstorm.Client.csproj -c Debug -warnaserror --no-restore` | Passed with zero warnings/errors during iteration. |
| `dotnet build Trackstorm.Client.csproj -c Release -warnaserror --no-restore` | Final fixture changes compiled with zero warnings/errors after native confirmation. |
| `dotnet restore Trackstorm.sln --force --no-http-cache` | Passed; refreshed a failed cached NuGet vulnerability lookup. |
| `./check.ps1` | Final synchronized run passed: version/tool/media regressions, restore, warning-free Debug production and Release solution builds, 517 Core tests and 345 non-native transport tests. |
| `./tools/sync-story-version.ps1` and `./tools/check-version.ps1` | Passed against synchronized main; canonical 0.1.29, Windows file/product 0.1.29.0. |
| `./check-oval.ps1 -GodotPath $g -Visual -NoBuild` | Final run passed 258 checks, no runtime warning/error. |
| `./check-item-spawns.ps1 -GodotPath $g -Oval -Visual -NoBuild` | Eight real UDP peers passed contention, use, cooldown, weighted distribution and occupied-slot protection on 20-marker oval; rendered captures inspected. |
| `./check-item-spawns.ps1 -GodotPath $g -Oval -Impaired -NoBuild` | Passed with 30 ms outbound delay, 5 ms jitter and 2% loss. |
| `./check-item-spawns.ps1 -GodotPath $g -NoBuild` | Old Map eight-pickup regression passed. |
| `./check-lobby.ps1 -GodotPath $g -NoBuild` | Passed repeated match lifecycle, fresh active bootstrap and retained-slot capacity over native transport. |
| `./check-migration.ps1 -GodotPath $g -Players 3 -NoBuild` and `-Players 2` | Both passed native lobby and active-match migration, retained bodies and item/spawn/match restoration after aligning fixture preparation with production. |
| `./check-reconnect.ps1 -GodotPath $g -NoBuild` | Passed three arena resyncs, including 125 seconds offline, native body reuse and held-item/spawn/match continuity. |
| `./check-network-vehicles.ps1 -GodotPath $g -Players 8 -NoBuild` | Passed all eight native UDP worlds, driving metrics and per-peer live camera controls after correcting the fixture frame wait. |
| `./check-network-vehicles.ps1 -GodotPath $g -Players 2 -Visual -NoBuild` | Passed both native worlds, rendered driving and all live camera controls. Client p99 prediction error 0.515 m; maximum 4.153 m. |
| `./check-camera.ps1 -GodotPath $g -NoBuild` | Passed input/orbit/return/reset, frame-rate and shake regressions. |
| `./check-camera-obstruction.ps1 -GodotPath $g -Visual -NoBuild` | Passed the rendered wall/corner/prop/slope/shake/occlusion scenarios; 3,439 movie frames captured. |
| `./check-startup.ps1 -GodotPath $g -SkipBuild` | Passed phase-aware recovery, input gating, repeated navigation and persistent shell checks. |
| `& $g --path . --script .godot/ts100-performance.gd -- --local-practice` | 180 warm-up frames, then 600 rendered frames with eight stationary practice cars: mean 11.480 ms, p95 22.042 ms, max 38.281 ms; 544 draw calls, 669 rendered objects at final sample. This is an idle local sample, not a stress budget guarantee. |

Captured text logs (line endings and trailing whitespace normalized) and the detailed measured oval assertions are retained in [ts-100](ts-100/). Representative captures: [practice](ts-100/practice.png), [pickups](ts-100/pickups.png), [network orbit](ts-100/network-camera.png), [overview](ts-100/overview.png), [grass](ts-100/grass.png), [bank](ts-100/bank.png). The temporary performance script is preserved as [PerformanceCapture.gd](ts-100/PerformanceCapture.gd).

## Direct runtime evidence

**VERIFIED:** 22,900 road raycasts preserve foundation collision and seams; three high-speed laps; both adapters' banking/transition and infield crossings; practice reset of all eight vehicles. The additional perimeter check made 10,992 rays across every section/seam at four heights. Thirty-two impact/launch scenarios use both native adapters at eight locations, 55 m/s horizontal entry, and either 1 m elevation or 8 m elevation with 12 m/s upward velocity. Every observed center remained inside. Each of the 20 pickup approaches uses a production network vehicle entering at 36 m/s and the existing Core authority awards a pickup. All 864 rendered tree transforms retain more than 11.9 m exterior clearance.

**VERIFIED:** Eight-peer pickup publications agree on all 20 IDs, claim/cooldown/token and slots. Simultaneous distribution exercises the first eight pickups; the separate driving harness covers all 20 individual approaches. Old-map pickups still pass their original fixture.

**VERIFIED:** Rendered overview, grid, chase, banked-car and pickup/HUD images were inspected. They show the exterior conifers, readable concrete boundary, gray road, green infield, blue sky and visible vehicles/pickups. The final ground materials use separate two-metre fine detail and 64 m macro variation, inspected in rendered practice and material views.

## Corrections and failed attempts

- Initial headless bake silently omitted MultiMesh transforms; visual inspection exposed the missing forest. The baker now requires a real rendering backend; the rendered runtime verifies every tree transform.
- Initial inherited-node ownership and material exposure produced scene resource leaks/poor appearance. Corrected ownership and matte material treatment; final oval run has no warning/error.
- First Core iteration failed the old seven-pickup rejection assertion. Updated the obsolete invariant and added 0/8/20 publication round-trip and excessive-capacity checks; final tests pass.
- Initial full-solution/final verification attempts failed NU1900 due to NuGet vulnerability lookup. A fresh no-cache restore resolved it. One superseded build also overlapped main synchronization and saw a transient missing camera type; the synchronized rebuild passes. These attempts are not claimed as passing evidence.
- Migration initially timed out at match entry: cold synchronous map loading across three peer worlds exceeded the heartbeat window. A main-geometry comparison with the same 20 markers passed. The fixture now retains a prepared map before starting peers, matching the production MatchResourceLoader contract. Both migration variants then pass. No production migration policy changed. Barrier collision was also reduced from 916 to 229 overlapping four-section spans, preserving the full-resolution visible mesh.
- Eight-peer camera assertions initially observed zero orbit while the vehicle was upright. The fixture resumed from a single slow ProcessFrame before the camera processed ResetFollow, discarding synthetic motion. Requiring both the intended elapsed time and two frame signals fixes the fixture without changing production behavior; the eight-peer headless retry passes. The overturned-vehicle hypothesis was ruled out by diagnostics. A rendered retry then exposed synchronous PNG encoding contaminating the next timed release interval; two frame boundaries after saving absorb capture delay before the next transition. Two-peer Visual and dedicated camera confirmation pass. No assertions or thresholds were weakened.
- A later eight-peer Visual attempt failed the existing prediction p99 < 3 m criterion (observed 3.802 m; maximum 11.329 m) on this shared machine. This attempt is not a pass, and no prediction threshold or handling policy was relaxed. Eight-peer headless passed; separate rendered coverage is recorded above. The cause of this failed eight-process rendered scenario was not isolated; it remains a limitation, not evidence of WAN quality.
- The first temporary performance capture script had a GDScript type-inference parse error; corrected before the recorded 600-frame sample.
- The first independent Astra oval run exercised 258 checks but its wrapper failed on sandbox-denied Godot user log/certificate access. Repeating with required filesystem access passed cleanly; both logs are retained.
- A native desktop screenshot attempt through the computer-use service failed with a foreground-window process-ID error. Visual evidence comes from actual Godot-rendered captures, not a claimed successful desktop interaction.

## Assumptions, limitations and explicitly unverified areas

- Placement is approximate relative to the supplied annotations; exact transforms follow actual master sections and safe lane clearances. Legacy image dimensions were ignored.
- Local rigid-body practice remains the existing driving-only mode. Pickup collection/inventory are exercised in hosted gameplay and its production native adapter, not added as a new practice-mode weapon implementation.
- Containment covers the exercised normal driving/launch envelope, not arbitrary teleports, developer-injected unbounded forces, or every conceivable impact orientation.
- Automated native driving is not an eight-human race or a physical keyboard/controller handling assessment. Vehicle handling was not redesigned.
- No multi-machine/WAN/NAT, authenticated EOS, packaged export, prolonged combat soak or hardware-fleet performance validation was performed for this Story. Local UDP integration is identified separately from those areas.
- The headless dummy renderer cannot inspect MultiMesh transforms; that assertion belongs to the passing Visual run.

## Independent engineering review

The independent reviewer returned PASS on pushed implementation fbaabaa, with no material findings. It verified capacity propagation, source marker integration, unchanged foundation assets, provenance hashes, version/main integration, and that all containment chords remain 0.0235–0.0500 m outside the measured rim. A subsequent fixture timing correction was also reviewed as PASS; its runtime confirmation is recorded separately.

## Astra Runtime Critique — Story Round 1

**Overall Score: 7.5 / 10 — Quality Assessment: FAIL (<8.0).** TS-100 is implemented but does not meet the runtime acceptance gate. No corrective round or Jira closure is claimed.

The independent Astra reviewer judged the exercised oval functional, with clear boundaries, collectible pickups and stable containment, but found environment presentation below the highly polished threshold. Overall is a holistic judgment weighted toward this Story's environment deliverable, not an arithmetic mean or a reward for test count.

| Runtime category | Score |
| --- | ---: |
| Runtime Functionality | 8.8 |
| Physics | 8.5 |
| Multiplayer / Networking Experience | 7.0 |
| Camera | 8.0 |
| Visual Quality | 6.5 |
| Observed Performance | 7.0 |
| Runtime Stability | 8.5 |
| Runtime Integration | 8.2 |

### What Astra exercised

- **VERIFIED, directly executed:** fresh `check-oval.ps1 -Visual -NoBuild` passed 258 checks without runtime warnings/errors, including three automated high-speed laps, bank/infield transitions, 32 impact/launch scenarios, 20 pickup approaches and eight-car practice/reset. Native synthetic inputs were used, not human driving. [Log](ts-100/astra-oval.log), [assertion evidence](ts-100/astra-oval-evidence.txt).
- **VERIFIED, directly viewed:** fresh [overview](ts-100/astra-overview.png) and [chase](ts-100/astra-chase.png), banking and side captures, final practice/material views, hosted pickup and two-peer camera captures.
- **Recorded evidence reviewed:** eight-peer pickup contention/reactivation/occupied-slot results, two-peer rendered driving/camera, dedicated camera results and 600-frame performance sample.
- **INFERRED from primary verification:** broader reconnect, migration, impairment and lifecycle consistency; Astra did not independently rerun these.

### Material findings and recommended corrections

| Problem and evidence | Severity / player impact | Suggested improvement | Scope / corrective work type |
| --- | --- | --- | --- |
| Ground materials: final grass/practice and fresh chase/side views show broad blurred light/dark green bands; aerial view shows conspicuous mottling. | Medium. The largest visible surface reads as a procedural pattern, weakening cohesion and scale. | Reduce macro contrast, refine spatial distribution and preserve restrained fine detail; inspect chase, infield and bank views under existing daylight. | In Scope / New Corrective Task |
| Forest composition: overview exposes concentric rows; chase/bank views show a dense repeated spiky wall. | Medium. Sustained scenery views lack natural variation and intentional composition. | Less regular spacing; deliberate clusters, gaps, height groups and silhouette variation, preserving exterior clearance, style and performance bounds. | In Scope / New Corrective Task |
| Rendered eight-peer quality: existing prediction threshold failed at p99 3.802 m, maximum 11.329 m; cause remains unisolated. | Medium. Consistency at the intended multiplayer workload remains uncertain. | Repeat with controlled load and frame-time/network telemetry, preferably distribute clients across machines; distinguish resource contention from production behavior without relaxing thresholds. | In Scope for verification; networking redesign requires separate authorization / No Code Change Required for initial investigation |

### Runtime limitations and next round

No physical keyboard/controller assessment, human multiplayer race, listening assessment, WAN/NAT, authenticated EOS, packaged export or prolonged combat soak was performed. Subjective fun, audio and human control feel were not scored. The eight-stationary-car performance sample is not sustained combat evidence. The initial sandbox-restricted critique attempt is not a clean pass; its escalated repeat passed.

**Recommended Next Round:** if authorized, refine grass/materials and forest composition, investigate the rendered eight-peer uncertainty, and repeat the affected views/scenarios without changing foundation geometry, pickups or handling. No corrective Jira issues or implementation changes were made from these recommendations.

**STOP for human decision**, as required by [critique](../critique.md). This report records a failed quality gate honestly; the Story is not accepted or ready to merge.
