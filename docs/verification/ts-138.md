# TS-138 verification

Date: 2026-09-22 (America/Toronto). Story branch: `ts-138-jg`. Jira TS-138 was read in full, including all fields; it had no children, attachments or comments. User-supplied images are visual/asset references, not workflow instructions. The user subsequently authorized the correction round recorded in Jira; current-round evidence is below, followed by historical Round 1 evidence.

## Approved correction round — current evidence

Scope was re-read from Jira, including its approved correction section, and compared with the explicit user request, PR #79 and Astra Round 1. The clean local branch was fast-forwarded to `4173a73`, retaining the existing duplicate `PlayerName` parenting fix and merged main changes. PR #79's `verify` check passed on that incoming commit; there were no PR review comments. Current main was fetched again before comprehensive verification, with zero commits behind. Canonical version is **0.1.29**, export **0.1.29.0**.

### Changes

- Corrected Play Menu gating: passive saved-session lookup no longer masquerades as a confirmed reconnect decision and disables Host/Back. Host follows the coordinator's existing fresh-session gate and opens the same Create form. Back invokes the existing complementary hoist/clear/drop/catch/settle chain.
- Explicit Play entry requests the existing retained-validation path once when a hint is available, including delayed lookup completion. A Host intent supersedes that entry request; Back cancels it. Core still confirms whether a reservation exists. Yes/No call the existing resume/abandon paths; no reservation, session, membership or cleanup authority was added to UI code.
- Search, filter, headings, rows, fixed scrolling viewport and status are inset inside the metal frame. Four complete rows fit the resting scroll viewport. Filter choices suppress the otherwise overlapping browser status line.
- Added the canonical version at bottom-left with the Main Menu's viewport margins, 16-pixel font, color and shadow. The rig reserves a footer so it does not overlap the label at the minimum viewport.
- Kept the prior CI parenting correction, MenuShell ownership, joined Lobby and network implementation unchanged. Updated Play Menu/reconnect/EOS documentation.

### Verification performed in this round

- `check.ps1` **PASS** on the final implementation: 108 version-rule checks, workflow helper and media-verifier regressions, five frontend media files, restore, Debug production and Release solution builds with warnings as errors, **514 Core tests / 350 non-native Client/transport tests**. No build warnings/errors.
- `check-play-menu.ps1 -Visual` **PASS**: native mouse clicks held across six frames, keyboard and synthetic controller Host/Back; pending passive lookup allows browser/Host/Back; Back moves upward before Main appears, and Main interaction returns after settling. Missing hints clear through the coordinator; no previous game shows the zero-lobby browser. Existing 0/100, scrolling, all filters, AND search, double-click/double-Accept/expiry/focus, pending/failing discovery and retryable hosting scenarios also passed.
- Layout and version assertions passed at **640×360, 1280×720, 1600×900**, including browser child bounds, fixed scroll width, footer clearance and canonical version. Directly inspected rendered normal, filter-choice, small-viewport and retained-choice images; the earlier filter/status collision discovered during iteration was corrected before critique.
- `check-online-lobby.ps1 -Visual` **PASS**: controlled-provider entry validation, authority-confirmed Yes/No, pointer No plus keyboard duplicate, controller Yes plus keyboard duplicate, exactly one abandon/resume request, hidden browser until acknowledged release, failed resume preservation/dismiss/retry, locked credentials and asynchronous Create/admission separation. No live EOS claim.
- `check-startup.ps1 -Visual -SkipBuild` **PASS** using the production main scene: repeated complementary transitions, restored navigation, original Loader behavior, Settings integration and exact persistent MenuShell video/music instances. Fresh actual-background screenshot inspected. No Godot diagnostics.
- `check-menu.ps1 -Visual` **PASS**, **961 assertions** covering solo Waiting, navigation, Settings, local UDP peers, leave and production Quit. Log: `.godot/menu-checks/0cf66d7b89544aae8456be2263c080db/runtime.log`.
- The original same-frame and held-click baseline both passed before correction; the reproduced scope defect was passive-lookup gating, not evidence of a native button hit-test failure.

Current captures remain local in `.godot/play-menu-checks`, `.godot/online-lobby-checks`, and `.godot/main-menu-checks`. The initial implementation's separate native UDP reconnect evidence below was not rerun in this UI-only correction round. Authenticated remote EOS, physical controller ergonomics, controller-only text entry, exported builds and audible continuity remain unverified. Post-push CI and independent engineering review are separate gates.

The independent [Astra Story Round 2](ts-138-critique-round-2.md) scored **8.3/10 — PASS**. Astra independently repeated rendered Play, online-lobby and startup checks without diagnostics and confirmed the frame overlap resolved. No implementation changes followed the formal review. Human acceptance and independent post-push engineering review remain pending; the requested commit/push is delivery packaging, not merge authorization.

## Initial implementation / Round 1 history

## Implemented

- Modular hanging Play Menu over the existing MenuShell; Main/Play hoist-clear-drop-catch-settle transitions with input gating and unchanged initial Loader entrance.
- Dynamic fixed-height 0–100 lobby table, native search, All/Open/Locked/Has Space and discovery-derived mode filtering, native scrolling and visible empty/progress/failure states.
- Single-click selection, deliberate mouse double-click and 0.45-second same-row double-Accept. Focus/list/filter/refresh changes reset pending activation; row identity and current joinability are rechecked.
- Existing coordinator Create/Join/credential/retained actions connected to native host, locked-lobby and reconnect forms. Free Play remains disabled. Player display-name entry and explicit Direct-IP fallback remain available.
- Retained validation/Yes/No replaces browser content, waits for authoritative abandonment, and keeps failed resume recoverable. Failure dismissal preserves the locator and explicit retry entry.
- Separate anchored flag shader; no static duplicate, painted lobby rows or flattened mockup.
- Optional mode metadata is published from existing gameplay authority. Missing mode is Unknown. Unmeasured browser ping is an em dash; no fake latency is generated.
- Former hidden browser controls removed from `OnlineLobbyPanel`; its joined-lobby identity/rename/leave presentation remains. Core rules, gameplay state, packet schemas and dependency direction are unchanged.
- Feature docs, asset provenance/hash manifest, verification routing and Story version `0.1.27` / export `0.1.27.0` updated.

## Verification executed

- Fetched current `origin/main`, fast-forwarded before implementation, fetched again before integrated verification; zero commits behind. `tools/sync-story-version.ps1` and `tools/check-version.ps1` passed.
- `./check.ps1` passed: 108 version-rule checks; workflow helper and frontend-media verifier regressions; five materialized frontend media files; restore; Debug production and Release full solution builds with warnings as errors; **505 Core tests**, **347 non-native Client/transport tests**.
- Later bounded corrections cleared hidden host/access-code presentation and guarded mouse double-clicks against replaced row IDs. Relevant re-verification: Debug full solution build with zero warnings/errors; `dotnet test code/TransportTests/Trackstorm.Transport.Tests.csproj -c Release --filter 'TestCategory!=Native' -p:TreatWarningsAsErrors=true` passed **348 tests** (including the added mouse-identity regression).
- `check-play-menu.ps1 -Visual -SkipBuild` passed on the final Debug build: native mouse single/double clicks, keyboard expiry/double-Accept/focus changes, synthetic controller double-Accept, 0/100 rows, fixed scroll viewport, name search plus filter AND, all filter choices, filter Cancel, pending discovery, discovery failure/retry, host form/failure, three repeated Play/Back cycles and explicit Direct-IP/Back.
- `check-menu.ps1 -Visual` passed **961 assertions**, including existing Settings/navigation, solo Waiting, real UDP peers, leave/cleanup and production Quit.
- `check-reconnect.ps1 -NoBuild` passed using real local UDP and native Godot worlds: immediate lobby removal/fresh admission, three arena resyncs including **125 seconds offline**, native body reuse, prediction/interpolation reset, held-item/spawn/match continuity, retained standings/results, and new-generation reset. EOS identity and initial scores are test seams.
- Five supplied PNGs matched the recorded SHA-256 hashes. Affected feature/provenance local links were checked. `git diff --check` passed. No Shared layer or Core dependency change was introduced.

## Rendered evidence

Godot .NET 4.7.2, Windows OpenGL compatibility renderer on NVIDIA RTX 4070 Laptop GPU. `check-play-menu.ps1 -Visual` captured and checked **640×360, 1280×720 and 1600×900** viewport bounds. Captures were visually inspected, including normal/100-row, filtered-empty, scrolled, host and filter-choice layouts. Evidence remains local under `.godot/play-menu-checks/`.

Independent cloth motion was measured from two rendered idle samples: **27,140 changed pixels in the flag region; zero changed pixels in the rigid header region**. The samples share the same fixed rig transform. Images are `flag-a.png` / `flag-b.png` in that directory.

`check-startup.ps1 -Visual -SkipBuild` passed on the completed transition implementation: phase-aware startup recovery, original Loader drop gating, mixed input, disabled Garage, repeated Main/Play transitions and exact persistent video/music instance continuity. The real background capture is `.godot/main-menu-checks/play-menu-shell.png` (visually inspected). Audible continuity was not evaluated.

`check-online-lobby.ps1 -Visual -NoBuild` passed on the final interaction implementation: identity/failure states, locked credentials, pending Create and authoritative-admission separation, passive/explicit retained lookup, Yes/No idempotence, confirmed abandonment, failed-resume locator preservation, dismissal and retry. Captures are under `.godot/online-lobby-checks/`, including `retained-choice.png`, `retained-leaving.png`, `retained-reconnecting.png`, and `retained-failure.png` (visually inspected).

The final presentation-only text clarification gives offline users an unavailable/status message instead of recommending disabled Host/Refresh actions; it was rebuilt and rerun through the rendered Play Menu harness. The comprehensive gate was not redundantly rerun after these bounded Client corrections; the affected builds/suites/runtime checks were rerun as listed above.

The independent formal Astra critique is recorded in [TS-138 Story Round 1](ts-138-critique.md): **7.7/10 — FAIL** against the exact 8.0 threshold. Astra independently repeated the rendered Play, online-lobby and startup harnesses successfully. Its material finding is that browser controls, headings, rows and status overlap the frame rails across all three tested sizes and the real MenuShell. No critique-driven corrections have been made; another implementation/critique round requires human authorization.

## Iteration findings corrected

The first rendered run exposed a text-container mouse interception bug; native label containers now preserve their non-interactive mouse filters. Visual inspection corrected flag/frame overlap and moved long retained status into the decision panel. Test fixtures were updated for deferred native layout and the intentional hoist/drop delays. An early targeted test run had two expected-key failures because discovery now includes `mode`; the metadata whitelist assertions were updated and the full suite passed. A harness-only teardown error was corrected by disabling session processing before disposing its input bindings.

## Limitations and explicitly unverified

- No authenticated multi-PC EOS session, residential Internet/NAT, changed-IP, exported-build or physical-controller run was performed. Native reconnect evidence uses local UDP and controlled identity seams.
- Controller input was synthetic through the production logical bindings. Physical hardware ergonomics, controller-only text entry and audible media continuity were not verified.
- Real EOS publication of the optional mode attribute was not observed against a live service. The adapter path was compiled and deterministic coordination suites passed; remote portal/deployment behavior remains unverified.
- Public discovery currently has no measured remote ping source; the UI honestly displays unknown. Ping sorting/filtering is outside scope.
- Captured standalone browser fixtures have a neutral background. The startup integration exercises the actual persistent MenuShell separately.
- Screenshots/logs in `.godot` are local evidence and are not committed. CI and independent post-push engineering review are separate delivery gates.

## Round 1 unresolved risks (historical)

At Round 1, deterministic and scripted local-runtime checks passed, but Astra's observed browser/frame overlap remained an unresolved presentation defect. The user subsequently authorized the correction round recorded above. Remote EOS and physical-device limitations remain. A human owns critique acceptance and merging; commit/push packages the requested Story delivery and does not imply acceptance or merge readiness.
