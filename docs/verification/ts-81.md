# TS-81 — Production map environment dressing

## Scope and inspection

TS-81 was read completely in Jira on 2026-09-24: description, acceptance criteria,
fields, dependency links, empty comments/attachments and no subtasks. TS-76 and
TS-80 were Done; TS-82 remains separate. No scope changes or Jira mutations were
made. Read TS-88's reference hierarchy and TS-74's reference description. Opened
and visually inspected **Map Concept 0.2.0 Scale.png**, Jira attachment 10017.
Rock-covered islands, low scrub, clear dirt routes and restrained perimeter
infrastructure informed placement; numeric labels and exact geometry did not.

Read root/Client routing, workflow, standards, critique, feature index, relevant
oval/environment docs, terrain/structure/oval/library authoring docs and the
third-party registry. Inspected merged TS-80 source/manifest and relevant prior
captures, the actual production scene/baker, collision and driving harnesses.
No library mesh/material, terrain, surface field, route, jump, bridge, tunnel,
spawn, pickup, vehicle or gameplay production code changed.

## Implemented

- Separate scriptless dressing scene, retained by the oval baker. The existing
  production map diff adds only one resource and one scene instance.
- 147 reused rocks/loose stones across ten named island zones, with three larger
  landmarks. Natural rubble supplies debris without dynamic objects.
- 2,447 cover instances share four source meshes in 212 spatial MultiMesh batches.
  Batch origins sit at their 32 m cell centers for local distance culling.
- 17 geometry-only light poles and 51 unit-scale guardrails outside the existing
  outer wall. Existing forest, materials and lighting remain unchanged.
- Fixed-seed placement protects routes/recovery shoulders, tabletop slopes/collars,
  tunnel approaches, jumps, basin centers and curved asphalt edges. Ground sampling
  uses actual terrain collision. No runtime generation or per-prop scripts.
- Native clearance/reuse harness and six production-car approaches into landmark
  groups, recording actual collision bodies. Fast-check routing/regression assertion,
  feature/authoring documentation and canonical version 0.1.47 / 0.1.47.0.

## Verification actually completed

Windows, Godot **4.7.2 .NET**, Compatibility/OpenGL, **NVIDIA GTX 1070**.
Godot executable:
`C:/Users/orsin/OneDrive/Desktop/godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.
Commands ran from the repository root. [Logs and captures](ts-81/).

| Command / exercise | Actual result |
| --- | --- |
| `git fetch origin main`; `git rev-list --left-right --count HEAD...origin/main` | Start and final-verification synchronization: 0/0 before the Story commit. |
| `tools/sync-story-version.ps1`; `tools/check-version.ps1` | PASS: 0.1.47 / 0.1.47.0. |
| Godot `--headless --path . --editor --import --quit` | Exit 0; native import completed. |
| Godot `--path . --script assets/maps/infield/BuildDressing.gd` | Final bake exit 0: 215 hard props, 2,447 foliage, 212 batches. [Log](ts-81/ts81-bake.log). |
| `tools/check-fast.ps1 -Area Client` | Targeted iteration builds passed, 0 warnings/errors. [Latest log](ts-81/ts81-fast.log). |
| `check.ps1` | PASS, exit 0: workflow/version/media regressions, Debug production and Release solution builds, **610 Core + 355 transport tests**, no failures/skips, 0 build warnings/errors. [Log](ts-81/ts81-check-final.log). |
| `check-environment.ps1 -GodotPath <above>` | PASS: 284 native assertions, 28,550 source triangles, 27 LOD surfaces, eight proxy impacts. Headless library verification, not production driving. [Log](ts-81/ts81-library-final.log). |
| `check-dressing.ps1 -GodotPath <above> -Visual` | PASS: **5,970 assertions**, **873 native vehicle-envelope probes**, 215 props, 2,447 foliage. Ground rays exclude dressing so obstacles cannot hide by raising probe height. [Log](ts-81/ts81-dressing-final.log). |
| `check-oval.ps1 -GodotPath <above> -NoBuild` | PASS: **3,087 checks**, native high-speed laps, bank transitions, containment, pickups and spawn/practice integration. [Log](ts-81/ts81-oval-final.log), [measurements](ts-81/oval-evidence.txt). |
| `check-infield.ps1 -GodotPath <above> -NoBuild -Visual -Case Dressing` | Targeted iteration PASS: six 10 m/s car approaches, confirmed placed-rock contacts, finite state, bounded speed and damage response. [Log](ts-81/ts81-impacts-iteration2.log). |
| `check-infield.ps1 -GodotPath <above> -NoBuild -Visual` | Final PASS: 2,598 support probes, **47 complete input-driven scenarios**, four pier impacts and six new dressing approaches. All ten routes, six jump speed/direction cases, repeated tunnel/deck crossings, slow/tabletop side traversals, three non-Water basin recoveries, bank transitions, connected tour and damage-free two-car tunnel traversal. [Log](ts-81/ts81-infield-final.log), [measurements](ts-81/infield-evidence.txt). |
| Godot `--path . --quit-after 180` | Rendered main-scene startup exit 0; no logged warnings/errors. [Log](ts-81/ts81-startup-final.log). |

`git diff --check` passed after correcting the scaffold whitespace. Relative links
in changed authoring/system/report documents were checked and resolved.

Native final dressing approaches recorded contacts with the intended landmarks
and neighboring rock groups: closest center distances 4.96–7.56 m, peak speeds
8.53–11.00 m/s and final HP 965.85–999.83/1000. These are deliberately damaging
obstacle contacts; damage-free route traversal is a separate assertion. Actual
contact paths are retained in the infield measurements.

## Iteration findings

- Fixed initial GDScript parse errors with explicit float/Vector3 annotations.
  Subsequent native runs passed.
- The first sandboxed Godot invocation could not write its user log and reported
  certificate-store access failure. Approved native runs used normal runtime
  access without those errors; the first invocation is not claimed as clean.
- Corrected foliage batches to use local cell origins; increased low-cover density.
  Final saved transforms survive scene reload.
- An initial collision fixture failed a guessed seven-metre proximity threshold:
  a nearby rock cluster stopped the north approach at 7.56 m. Replaced that proxy
  with actual `GetCollidingBodies()` evidence while retaining nonpenetration,
  speed, damage and finite-state checks. [Original failure](ts-81/ts81-impacts-iteration.log)
  and [successful correction](ts-81/ts81-impacts-iteration2.log) are retained.
- Diff inspection confirmed existing mesh/terrain/collision source assets are
  unchanged. Corrected a scaffold trailing blank line. Core/Client dependency
  direction is unchanged; no Shared layer was introduced.

## Assumptions, limitations and explicitly unverified areas

- **VERIFIED:** production-map loading, native clearance envelopes, controlled
  native vehicle scenarios and rendered captures. Scripted logical input/native
  physics is active automated playtesting, not manual driving.
- **UNVERIFIED:** GUI-controlled driving and human handling feel. Windows Computer
  Use capture failed twice, including refreshed window selection, with
  `SetIsBorderRequired failed: No such interface supported (0x80004002)`.
  No successful GUI driving or live screen observation is claimed.
- No new Blender geometry. Approved library meshes/materials and known visual
  limitations were inputs, not silently refined. No TS-82 shaders, surface polish,
  particles, lighting effects or new asset acquisitions were added.
- Collision scenarios reset cars at initial poses. They do not prove recovery
  from every off-route crevice, high-speed angle, upside-down pose or pileup.
  Exhaustive collision/recovery coverage remains unverified.
- No exported-build test, remote multiplayer/EOS test, eight-human race, prolonged
  soak or controlled frame-time benchmark. Two native cars in one scene do not
  establish remote replication. No performance score is inferred from asset counts
  or automated checks. Other user applications were running during verification.
- Library hard distance cutoffs remain, applied per cover batch. Moving-camera
  fade quality is unverified. The four cached cover meshes require rebaking after
  library changes.
- Pre-existing `code/Core/Input/InputButtons.cs` edits and ten extracted rock PNGs
  were preserved and excluded from the Story change set. Tests ran with that
  existing input-file edit present.
- No independent engineering review. Delivery is a Story-branch handoff for
  human direction, not merge approval.

## Astra Runtime Critique — Story Round 1

**Overall Score: 7.5 / 10**

**Quality Assessment: FAIL (<8.0)**

This formal implementation-agent experiential assessment occurred after the final
comprehensive, library, oval, dressing and full infield checks passed. A further
native rendered `check-infield.ps1 -GodotPath <above> -NoBuild -Visual -Case WestLoop`
run passed for the critique, followed by direct inspection of its capture and the
final production-map views. [Critique run](ts-81/ts81-critique-drive.log),
[measurements](ts-81/critique-drive-evidence.txt),
[west-loop driving view](ts-81/critique-WestLoop-drive.png).
No implementation corrections were made after this assessment.

| Material category | Score | Observed basis |
| --- | --- | --- |
| Runtime Functionality | 8.5 | Actual production composition loads and repeated input-driven scenarios complete without runtime errors. |
| Route Readability / Gameplay Integration | 8.3 | Wide routes, jump approaches, tabletop and tunnel remain clear; the two-car crossing completes without damage. |
| Physics | 8.1 | Native route traversal and six placed-rock contacts are stable in the tested cases; arbitrary off-route recovery is unverified. |
| Visual Quality / Composition | 7.0 | Meaningfully denser shoulders and recognizable landmarks, but basin islands still read as individually scattered props rather than integrated rocky formations. |
| Runtime Integration | 7.8 | Approved assets fit the map scale and remain outside the driving lanes; distant cover loss leaves a much sparser overall composition. |

The overall assessment prioritizes this Story's environmental composition, not an
arithmetic average of technical checks. Passing tests do not raise the art score.

### What was exercised

**VERIFIED:** 47 native driving scenarios, actual placed-rock contacts, sampled
lane/shoulder envelopes, repeated jumps/deck/tunnel traversal and two cars sharing
the tunnel. Directly inspected final native images of the
[south island](ts-81/south-island.png), [north ridge](ts-81/north-ridge.png),
[west islands](ts-81/west-islands.png), [overview](ts-81/overview.png),
[jump approach](ts-81/jump-approach.png), [tabletop crossing](ts-81/TabletopAcross-1_7-drive.png),
[airborne jump](ts-81/EastJump16-air.png), [perimeter](ts-81/perimeter.png) and
[two-car tunnel approach](ts-81/TwoCarTunnel-drive.png). The additional west-loop
drive after verification also completed without damage.

**INFERRED:** Conservative exclusions and tested paths reduce ordinary blocker
risk; this is not proof of every possible off-route collision/recovery case.

**UNVERIFIED:** Manual handling feel, continuous moving-camera culling quality,
remote multiplayer, export behavior, long-duration performance and arbitrary
off-route trapping. The GUI capture failure described above prevented manual
control/observation. No category score substitutes for these missing observations.

### Finding and recommended next round

- **Problem:** Rock islands remain visually fragmented. Small isolated stones and
  repeated separate boulders dominate much of the basin/loop surroundings; broad
  green gaps persist compared with the concept's integrated rocky islands.
- **Evidence:** Final `west-islands.png`, `overview.png` and the post-verification
  `critique-WestLoop-drive.png`. Close shoulder density is improved, but the larger
  environmental masses are still weak, especially at medium and overview distances.
- **Severity:** Medium.
- **Impact:** The map remains easy to drive, but the primary environmental-art
  objective falls short of the highly polished 8.0 gate. Distinct areas have less
  geological identity than the concept direction calls for.
- **Suggested Improvement:** With human authorization, regroup a portion of the
  existing ledges, clusters and boulders into fewer contiguous outcrops inside the
  protected islands, with asymmetric secondary rubble and vegetation around their
  bases. Check silhouette density at driving and medium distances while retaining
  all route/shoulder exclusions. Re-run collision and driving checks on changes.
  This does not require new meshes, effects or material polish.
- **Scope:** In Scope (TS-81 placement/composition).
- **Corrective Work Type:** Existing Task (the current TS-81 Story).

**Recommended Next Round:** Only if the human authorizes it, refine those island
compositions using the approved kit, then repeat applicable verification and the
formal critique. No corrective round or Jira issue is started automatically.

## Delivery boundary

The user explicitly requested a commit and push of the existing `ts-81-pg` branch
after critique, with no PR and a stop for human direction. This instruction applies
despite the failed score; it is not acceptance of the visual result. The handoff
commit uses `[skip ci]` to suppress the repository's push-triggered automatic PR
creation workflow. Consequently no new remote CI result is claimed; the complete
local verification above is the available evidence. No PR is created or merged,
and no critique recommendation is implemented in this round.
