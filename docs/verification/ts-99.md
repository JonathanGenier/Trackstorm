# TS-99 — Categorized DevTools Logs

Implemented on `ts-99-jg`, 2026-09-19. Read Jira TS-99 and TS-59 descriptions, acceptance criteria, comments and issue links, TS-96 dependency requirements, and the approved TS-97 shell action placement. TS-99 has no children, comments or remote links; its TS-96 dependency is Done. Inspected root/nested instructions, workflow, standards, critique, feature index, `features.md` compatibility pointer, DevTools/Event Log/menu documentation, runtime owners and existing verification. No requirement changes were introduced.

Synchronized with `origin/main` at `e8640bc` before implementation and fetched again before final verification; no newer main changes were present. Required Story version is `0.1.16`, with Windows preset versions `0.1.16.0`.

After the initial push, main advanced to `1e180a9` with CI-only Node 24 action updates and a maintenance-PR version-check condition. Merged that compatible maintenance change during delivery and reran version synchronization/validation successfully; canonical versions remained unchanged. The build/test/native evidence below predates this CI-only merge; no production or test source changed during that synchronization. GitHub CI completion is not claimed.

## Implementation evidence

- `EventLogFormatter` retains existing wording, elapsed millisecond timestamps, category, Host/Local marker, sequence, exact numeric formatting and actor/target positions. Its literal text callbacks expose semantic styling boundaries without matching names inside arbitrary message strings. A player named Missile is red in identity positions; the Missile cause keeps normal message styling.
- `EventLogPanel` uses native color spans and literal `AddText`, with BBCode disabled. Timestamp color is `#929ca8`, category `#69b7ff`, player name `#ff7777`, message `#eeeeee`. Line separation improves scanning. Existing full-area shell layout and F3 routing are reused.
- Follow latest off freezes the last presented bounded snapshot without modifying collection. Category changes filter that snapshot, even after source eviction. Unfreezing displays the currently retained journal in observed order and follows its newest entry. New/absent sources replace/clear stale presentation. Categories and selectable text remain available.
- No Core, event schema, producer, protocol, deduplication, authority, timestamp, privacy allowlist or gameplay control changes. No continuous telemetry was added. The journal's existing retention and watermark remain authoritative. Logs content contains only presentation controls.
- Preserved the explicitly approved TS-97 host-only Force Start action in the shared shell header, outside Logs content. As in TS-98, the read-only tab-content requirement is interpreted alongside this approved shared-shell placement; this is not a claim that the entire shell has no mutation action. TS-96's repeated-F3 select behavior supersedes TS-59's older independent-window toggle requirement.
- Updated current feature documents and verification index. Added seven deterministic formatter cases and expanded the native Event Log fixture.

## Actual checks and results

Windows, .NET 10, `DOTNET_PROCESSOR_COUNT=4`. Native executable: `C:/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`. Rendered checks used OpenGL compatibility on NVIDIA RTX 4070 Ti.

| Command | Actual result |
| --- | --- |
| `./check.ps1` (final run) | PASS. Version/media regression checks, media integrity, restore, Debug/Release warnings-as-errors builds (zero warnings/errors), 461 Core tests and 332 non-native Client/transport tests in each configuration. |
| `./check-event-log.ps1 -GodotPath <above>` | PASS. Production bootstrap and two local native UDP worlds; F3/repeated F3/Escape/Close, one shell, read-only Logs content, live replicated item outcome equality, ongoing simulation, category filtering, frozen snapshot/retention and resumed rendering. |
| `./check-event-log.ps1 -GodotPath <above> -NoBuild -Visual -Resolution 640x360` | PASS. Rendered small-viewport shell checks and Event Log/activity-feed integration. Screenshot directly inspected for muted timestamps, blue categories, red names, readable text and available Close/filter controls. |
| `./check-event-log.ps1 -GodotPath <above> -Visual -Resolution 1280x720` | PASS. Final expanded fixture also sets a nonzero inspection scroll, appends 1,030 events during freeze, verifies unchanged view/scroll, filters an evicted event from the frozen snapshot, switches F2/F3, resumes the exact 1,024 retained entries and auto-scroll, rejects an evicted duplicate and resumes live rendering. Screenshot directly inspected. |
| `./check-menu.ps1 -GodotPath <above>` | PASS: 756 assertions, including shared tabs, configuration navigation, remapped keyboard/controller/axis behavior, focus and input suppression. |
| `./check-statistics.ps1 -GodotPath <above> -NoBuild` | PASS: 161 assertions for the neighboring Stats tab and live runtime projection. |
| Godot `--headless --editor --path . --import --quit` | PASS: exit 0; no ERROR/WARNING lines in captured log. |
| `./tools/sync-story-version.ps1` / invoked `check-version.ps1` | PASS after final fetch; canonical and Windows preset versions match current main's normal Story increment. |
| `git diff --check` | PASS. |

The Core suite directly exercises equal-time ordering, bounded eviction, replay watermark persistence, codec validation, exact damage/healing, collision duplicate suppression and authenticated-subject exclusion. Existing transport tests exercise trusted-host replication and its rejection boundaries. These existing tests ran again; no old result is being substituted for current evidence.

Reviewed the complete diff against current main and Jira TS-99 for scope, wording, structured data ownership, safe rendering, freeze boundaries, tests, documentation and accidental artifacts. No dependencies or Shared layer were introduced.

Ignored local evidence: `.godot/ts-99-check-final.log`, `ts-99-event.log`, `ts-99-visual.log`, `ts-99-visual-1280.log`, `ts-99-menu.log`, `ts-99-stats.log`, `ts-99-import.log`, `ts-99-logs-640.png` and `ts-99-logs-1280.png`. Final rendered Event Log artifact directory: `.godot/event-log-checks/8d5a7fc2ff074575867ad7d8fab1c897/`.

## Failed attempts and corrections

- Initial sandbox Git/NuGet access was denied; authorized execution outside the sandbox enabled synchronization and builds.
- A new formatter assertion incorrectly expected the cause text in the Setting changed branch; corrected its expectation. A first harness accessor referenced the wrong match owner; compiler rejected it and the accessor was corrected.
- The existing native fixture attempted item use before Active, which latest main's participation rules correctly reject. The fixture now waits for the actual Active phase.
- Headless Godot did not supply a usable window size from the CLI resolution argument. The harness now explicitly establishes a 1280x720 root viewport before exercising layout; rendered checks subsequently apply their requested resolution.
- All corrected checks passed in subsequent runs. GitHub CLI is unavailable; PR inspection uses the connected GitHub tools.

## Assumptions, limitations and unresolved risks

- **VERIFIED:** literal markup rendering, semantic name boundaries, frozen collection/retention/filtering, nonzero scroll stability, resumed auto-follow, live local UDP outcomes, category preservation, shared navigation, deterministic secret-exclusion tests, and rendered readability at 640x360 and 1280x720.
- **INFERRED:** unchanged producer allowlists and protocol/authority implementations preserve their other security and network behavior. The UI does not implement a general-purpose secret scrubber; it continues relying on safe structured publication. No arbitrary provider error strings were introduced.
- **UNVERIFIED:** a newly exported Release executable, authenticated multi-PC EOS, new host-migration/reconnect runtime scenarios, physical keyboard/controller ergonomics, manual long play sessions and long-duration performance profiling. Release compilation/tests and synthetic native input were verified.
- Rendering still rebuilds the bounded visible history when the source revision changes. The expanded runtime fixture exercised a full 1,024-event projection, but does not establish a sustained high-rate performance benchmark.
- Shared-shell Force Start remains visible as approved by TS-97; removing or relocating it is outside this implementation's interpretation of TS-99 scope. No other unresolved functional defect was identified by the checks performed.
