# TS-183 verification

The initial delivery and Round 1 evidence below are historical. The [approved Import Configs follow-up](#approved-import-configs-follow-up) records the current additional scope, integrated version and fresh verification.

## Scope and implementation

Verified against Jira TS-183, including its complete description and empty children/comments/links, on 2026-09-26. No additional approved requirement changes were supplied. Work stays on `ts-183-jg`.

- Admitted hosts and clients edit the same 170-key gameplay catalog. Clients send bounded deltas to the host; Core validates and applies atomic transactions. Gameplay never consumes a speculative client configuration.
- The reliable session configuration channel checks actual transport identity, admission, session, connection generation, authority epoch, match generation, request identity, payload size and catalog values. Complete publications converge lobby replicas; arena configuration still precedes the matching world revision and native/prediction consumers.
- Immediate category/global resets use the same authority path. Pending client requests have explicit confirmation/error feedback. Other peers' changes refresh untouched controls while preserving visible local drafts.
- Late join, existing resume checkpoints and migration restore current shared configuration. Accepted tuning no longer writes participants' permanent defaults files. Existing host seed files remain readable when creating a fresh session.
- Export Changes captures confirmed session values, excludes drafts/local graphics/network simulation, compares against canonical HostedDefaults, groups stable keys by Configs category and includes the actual version plus UTC timestamp. The FileDialog requests native Windows Save As with `configs.txt`; save/cancel/failure callbacks are implemented.
- Force Start, map selection, item grants and local transport simulation retain their existing authority restrictions. No import, admin-permission scheme or defaults promotion was added.
- Canonical version is `0.1.70`; tracked Windows metadata is `0.1.70.0`, derived from current main `0.1.69`. The Story's release target/example is not authorization to change the canonical release prefix.

## Environment and iteration checks

Windows, .NET 10, Godot `4.7.2.stable.mono.official.ed1daf0bf`; native GameNetworkingSockets localhost UDP. Commands below use `$Godot` for `C:/Users/j_gen/Documents/Godot/Godot_v4.7.2-stable_mono_win64/Godot_v4.7.2-stable_mono_win64_console.exe`.

- `git fetch origin main`, `git merge --ff-only origin/main`: current main already included, repeated before final checks.
- `./tools/sync-story-version.ps1` and `./tools/check-version.ps1`: passed, canonical/preset values above.
- `dotnet test code/Tests/Trackstorm.Core.Tests.csproj -c Debug --filter 'FullyQualifiedName~Development' --no-restore`: 186 passed.
- Transport Debug filter `FullyQualifiedName~SharedConfiguration|FullyQualifiedName~DeveloperOptionsTests`: 16 passed.
- Transport Debug filter for `DeveloperAuthorityAndConfigurationSurviveSuccessiveMigrations`: 1 passed, initial tuning now originates from an admitted client.
- Debug warning-as-error builds passed during iteration.
- `./tools/check-fast.ps1` reported no committed changes against main because implementation was still uncommitted. This was a no-op, not claimed verification coverage; explicit targeted checks above were used.
- Iteration exposed and corrected source encoding, obsolete persistence expectations, and an idempotent-reset revision expectation. The first integrated gate caught an accidental map-authority expansion and one obsolete water-authority expectation; map selection was restored to host-only and the water test now exercises admitted-client tuning through the host. These failures were corrected before the passing results below. The menu harness also had obsolete solo-Waiting assertions and uncapped frame waits shorter than the production five-second countdown; its fixture now asserts the existing synchronized Countdown and uses the same 60 FPS pacing as other native harnesses. No production match-entry behavior was changed. One rendered rerun mistakenly reused its prior preferences directory and failed the expected initial speed-unit fixture; the final rendered run used a fresh directory and passed. Small-viewport observation also led to shorter guidance text, followed by repeat native DevTools/rendered menu checks.

## Comprehensive final checks

- `./check.ps1`: PASS. Debug production build and Release solution/analyzers: zero warnings/errors. Release Core: **867 passed**. Release non-native transport/client: **379 passed**. Version regressions, workflow routing and materialized frontend-media checks passed. Log: `.godot/ts183-final-check.log` (initial failed gate retained separately as `.godot/ts183-final-check-first.log`).
- `./check-developer-options.ps1 -GodotPath $Godot`: PASS, **1,235 write-phase assertions and 13 read-phase assertions**, separate fresh processes. Artifacts: `.godot/developer-options-checks/d4d244a87e5f445096e1195958a40837`; wrapper log `.godot/ts183-final-devtools.log`.

- `./check-reconnect.ps1 -GodotPath $Godot -NoBuild`: PASS, three native UDP arena resyncs including 125 seconds offline. Each restores client-originated `items.wrench_heal=123` and exact current revision before prediction, native body reuse and held-item/spawn/match continuity. Log: `.godot/ts183-final-reconnect.log`.
- `./check-migration.ps1 -GodotPath $Godot -Players 3 -NoBuild`: PASS, intentional lobby leave, former-host rejoin, native active-match authority loss and sequential epochs, restored tuning/items/spawns/match. Identity is a trusted seam. Log: `.godot/ts183-final-migration.log`.
- `./check-lobby.ps1 -GodotPath $Godot -NoBuild`: PASS, native eight-participant lobby/arena lifecycle, fresh admission into active gameplay, retained-slot rejection and host-loss cleanup. Artifacts: `.godot/lobby-checks/ed5c1876e5c04a5a9ce661557be6627c`; log `.godot/ts183-final-lobby.log`.

- `./check-transport.ps1 -GodotPath $Godot`: PASS, **390 Debug tests**, including native capacity/reconnect, impairment/reliable ordering and vehicle loops; Godot transport completed three creation/poll/message/cleanup cycles. Log: `.godot/ts183-final-transport.log`.
- `./check-menu.ps1 -GodotPath $Godot`: PASS, **2,387 assertions** for production navigation, logical keyboard/controller input, Configs draft/close handling, responsive layout (640x360 through ultrawide), session leave and Quit. Artifacts: `.godot/menu-checks/48d7ae63e1e1495d8984c6297585095a`; log `.godot/ts183-final-menu.log`.

- `./check-network-vehicles.ps1 -GodotPath $Godot -Players 3 -NoBuild`: PASS, three separate native processes. Artifacts: `.godot/network-vehicle-checks/1c1292a47b734afd8e52293047ba7ead`; log `.godot/ts183-final-network.log`.
- Same command with `-Latency 50 -Jitter 10 -Loss 2`: PASS. Artifacts: `.godot/network-vehicle-checks/517a9293c79a4693aeeae8fe9e6709e2`; log `.godot/ts183-final-network-impaired.log`. These existing driving/camera integration checks exercise network regressions, not additional configuration edits.
- `$Godot --headless --path . --quit-after 120`: PASS, main-scene startup exited cleanly with no ERROR/WARNING. Log: `.godot/ts183-final-smoke.log`.
- Final diff whitespace and changed-document local file-link checks: PASS. The existing incoming `host-local-persistence` link remains valid; legacy seed/default documentation is retained.

- Rendered menu scene: `$Godot --path <repo> res://scenes/verification/menu_checks.tscn --quit-after 2400 -- --menu-output=<repo>/.godot/ts183-rendered-final`: PASS, **2,436 assertions**, empty stderr. Captured PNGs were directly inspected at 640x360, 1280x720 and 2560x1080; final guidance fits one line at the small viewport, with search/export and footer actions unobscured. Logs: `.godot/ts183-rendered-final.log` and `.godot/ts183-rendered-final.errors.log`.
- `dotnet build Trackstorm.sln -c Release --no-restore -warnaserror`: PASS after final menu-fixture/text maintenance, zero warnings/errors; `.godot/ts183-final-release-build.log`. The repeat DevTools run above built Debug and passed all 1,235 + 13 assertions on that final text.

## Direct runtime and operational evidence

**VERIFIED** by production Godot UI/native UDP harness:

- Host plus two connected clients, including late arena admission with existing nondefault tuning.
- Client Apply remains pending without speculative gameplay changes; acceptance converges all peers. Invalid multi-field values are rejected without a partial change.
- Alternating client transactions repeatedly change max HP and missile speed. Client native vehicle HP owners reflect synchronized max HP; the existing full-catalog harness also checks native observation/prediction and gameplay effects.
- An external edit refreshes untouched editor fields while an unrelated local draft stays visibly staged. Category reset preserves other categories/drafts; global reset converges all peers on defaults.
- Host and both clients produce identical exports at a common timestamp. Real save callback writes accepted mass 1100 rather than unapplied draft 1700. Cancel preserves the file; a directory destination produces retryable error feedback.
- Accepted session changes do not create/overwrite the host defaults file; a new process/session starts from the original seed/defaults.
- Deterministic transport exercises concurrent disjoint deltas, twenty repeated peer edits, malformed/forged/stale traffic, request deduplication, lobby late join and export equality. These are controlled transport seams, not real EOS deployment evidence.

**VERIFIED** by rendered desktop observation through the Computer Use skill:

- Configs at 1280x720 has readable shared-authority guidance, category list, Export Changes beside search and unobscured persistent footer actions.
- Clicking Export Changes opens the native Windows Save As dialog titled Export Changes with selected `configs.txt` and text-file filter.

**UNVERIFIED** OS Save completion: the automation API reactivates the parent game window when targeting the modal, preventing reliable typing/Save interaction. FileDialog save/cancel callbacks and actual output writing were exercised separately as described above. No completed native OS Save click is claimed.

## Assumptions, limits and risks

- Canonical defaults means `GameplayConfiguration.HostedDefaults`, not a participant's host seed or current session starting values. Exports use stable catalog keys and owning numeric precision; timestamps may differ across separate exports.
- Same-epoch changes are serialized in host receive order; a later edit to the same field wins. Explicit unsaved text is intentionally preserved and remains a draft until submitted/cancelled.
- Existing Core active-match validation remains authoritative for resets. A reset that would change a prohibited match mode or invalid kill target is rejected atomically with feedback.
- A confirmation interrupted by disconnect, timeout or authority change cannot guarantee whether the host applied it. The UI reports uncertainty and instructs checking current values. Migration can roll back to its selected complete checkpoint, consistent with the existing migration contract.
- Local tire graphics/preferences and process-wide transport impairment remain outside shared gameplay state and export.
- The separate-process movement checks passed their existing thresholds but recorded startup/transient corrections: client error p99 was 0.678/0.679 m normally and 0.820/0.638 m under impairment; maxima reached 5.105 m normally and 4.462 m impaired. Impaired client frame maxima were 269/330 ms. This is not evidence of hitch-free rendering or a TS-183-specific performance improvement/regression; those runs were headless.
- No new dependencies/assets or redistribution obligations were introduced. Core remains engine-independent with Client -> Core dependency direction; no Shared layer was added.
- The pre-existing user change to `assets/maps/oval/grass_variation.png.import` is excluded from this Story's commit.

## Explicitly unverified

- Native Windows Save button completion through an actual end-user click, as distinct from verified dialog presentation and callback/file writing.
- Authenticated EOS between separate PCs/accounts, real NAT/relay/WAN conditions and deployment credentials.
- Exported Windows package execution, physical-controller ergonomics, multi-hour/high-frequency tuning soak and subjective balance across the entire 170-value catalog.
- Independent engineering review and remote CI results are separate post-push gates; local verification does not imply either passed.

## Delivery observation

Implementation commit `d389b2a0afe3a8420691aa43c6065feb3b64e28a` was pushed to `origin/ts-183-jg`. The repository automatically created [PR #123](https://github.com/JonathanGenier/Trackstorm/pull/123), and its title/body were updated with the scoped implementation and actual evidence. The unrelated grass import change remains local and uncommitted.

The first [post-push CI run](https://github.com/JonathanGenier/Trackstorm/actions/runs/36251920862) returned `action_required` with no jobs executed. The run metadata did not expose a more specific reason. Remote CI has therefore not passed; this is distinct from the successful local checks above. Independent engineering review and human acceptance remain pending.

## Astra Runtime Critique — Story Round 1

**Overall Score: 8.1 / 10**

**Quality Assessment: PASS (>=8.0)**

| Material runtime category | Score | Observed basis |
| --- | --- | --- |
| Runtime functionality | 8.3 | Host/client edits, rejection, repeated changes and authoritative resets completed through the actual session and UI paths. |
| Multiplayer experience | 8.3 | Three-peer configuration convergence, late join, client-originated values through three reconnects, and successive authority replacement retained a coherent shared boundary. |
| UI / UX | 8.0 | Clear pending/accepted/rejected feedback and visible draft preservation; native dialog appearance and readable responsive layout directly observed. Full OS Save-click completion remains unverified. |
| Runtime integration | 8.2 | Synchronized values reached native gameplay/HP and prediction owners; confirmed exports agreed across peers; fresh sessions did not inherit transient tuning. |
| Runtime stability | 8.2 | Final scoped native lifecycle, menu, transport and recovery runs completed without runtime errors/warnings. Network driving logs still include the startup corrections/stalls recorded above. |

**What Was Exercised:** VERIFIED production Godot UI/native UDP host plus clients, client requests, validation failures, resets, late admission, repeated changes, reconnect/migration, gameplay consumption, callback file output and rendered navigation/layout. VERIFIED native Save As presentation/default filename. File contents were produced by the real callback, not a separately reimplemented writer.

**Runtime / Operational Limitations:** UNVERIFIED actual OS Save-button completion, real authenticated multi-PC EOS/WAN, packaged Windows execution, physical controller use and extended soak. Logical controller input was injected by the existing native harness. No new subjective gameplay-balance, audio or movement-feel score is inferred from configuration convergence. Early network corrections and measured frame stalls prevent a claim of hitch-free performance. The overall score is a direct judgment of the exercised shared tuning workflow, not a rounded test-count average or an engineering-review score.

The exercised result is coherent and dependable across the required shared-session operations, with clear editing feedback and an explicit export boundary. The incomplete end-to-end OS Save observation and external-network limits constrain confidence beyond this local result.

Stop for explicit human direction after presenting this critique. This report and a pushed Story branch do not constitute human acceptance or merge authorization.


## Approved Import Configs follow-up

On 2026-09-26 the user authorized reading the exported configuration format and explicitly chose: **"Change only the settings listed in the file."** Jira TS-183 was re-read (no children/comments) and its description updated with this approved scope before delivery. This supersedes the initial delivery's exclusion of import.

- Added **Import Configs** beside Export Changes for synchronized hosts and clients. A bounded UTF-8 read passes the existing exported header/category/key format through Core validation, then stages only listed keys for review and Apply Settings. Unlisted current settings and unrelated drafts remain intact. No session or permanent defaults change merely by opening/importing a file.
- Apply uses the existing host-authoritative atomic transaction and replication path. Explicit imported intent survives intervening peer edits, including a value initially equal to its baseline. Untouched unlisted fields continue to converge.
- BOM/CRLF and invariant float/double round trips are supported. Invalid metadata, repeated/unknown/misgrouped keys, nonfinite/fractional/out-of-range/cross-field-invalid values, oversized files and unreadable/non-UTF-8 files reject without partial draft/session changes. Valid older-version files with supported stable keys are accepted with source-version feedback; no renamed-key migration is promised.
- Import blocks during pending confirmation or an export picker, and rejects a selection after its session owner/authority changes. The host repeats live validation on Apply, including active-match restrictions.
- Integrated current main `a46f29c` in merge `d401cc6`, retaining TS-174's fence/out-of-bounds feature and 171-key/TC24 configuration format. Documentation conflicts retained both Stories' contracts/evidence. Canonical version is now **0.1.71**, Windows metadata **0.1.71.0**, derived from main **0.1.70**. Final fetch found no further main commits; sync/version checks passed.

### Follow-up verification evidence

Same Windows/.NET/Godot/native UDP environment as Round 1; `$Godot` denotes the console executable recorded above.

- Targeted iteration: Core import filter initially passed **18 tests**; a fractional-integer case was then added and all **19 import cases** passed within the final Core gate. Client draft filter `FullyQualifiedName~DeveloperOptionsTests` passed **16 tests**. Logs: `.godot/ts183-import-core.log`, `.godot/ts183-import-draft.log`. Nullable fixture warnings found during iteration were corrected before warning-as-error final builds.
- `./check.ps1`: **PASS**, zero build warnings/errors, **889 Release Core tests** and **380 Release non-native client/transport tests**, plus version/workflow/media regressions and checksum verification. Log: `.godot/ts183-import-final-check.log`. The first final attempt found TS-174's merged test still expecting an admitted client configuration request to fail. Its fixture now checks unknown-peer rejection and admitted-client host validation, preserving the out-of-bounds behavior assertions. Initial failure retained in `.godot/ts183-import-final-check-first.log`; no production out-of-bounds behavior was changed.
- `./check-developer-options.ps1 -GodotPath $Godot`: **PASS**, **1,283 write + 13 read assertions**, warning-free Debug build and separate fresh write/read processes. Artifacts: `.godot/developer-options-checks/71bed642f9eb437e9ecf6d06ecfc951d`; log `.godot/ts183-import-runtime-final.log`.
- **VERIFIED native UDP / actual Configs controls:** host plus two clients; client and host import/Apply; stage-before-Apply; preservation of unlisted nondefault values; another peer changing an unlisted field while an import is staged; all-peer convergence; native HP owners consuming imported max HP; host/client re-export equality; active-match mode rejection without partial application; retained rejected draft; cancel, malformed file, missing file, invalid UTF-8, oversized file and stale-session-selection feedback. Existing shared edits, repeated transactions, category/global resets, late admission and non-persistence checks ran in the same harness. File selection/cancel are actual FileDialog signal callbacks with real files, not completed OS picker clicks.
- `./check-reconnect.ps1 -GodotPath $Godot -NoBuild`: **PASS**, three native arena resyncs including **125 seconds offline**, restoring client-originated configuration/revision before prediction and native body reuse. Log: `.godot/ts183-import-reconnect.log`. This exercises the shared configuration recovery boundary, not a separate reconnect immediately following the import button.
- Rendered menu scene with fresh output directory, `$Godot --path <repo> res://scenes/verification/menu_checks.tscn --quit-after 2400 -- --menu-output=<fresh-directory>`: **PASS**, **2,451 assertions**, exit 0 and empty stderr. Images directly inspected at **640x360, 1280x720 and 2560x1080**: search, Import/Export, guidance and footer remain readable/unobscured. Artifacts: `.godot/ts183-import-rendered-83b1ed2f46a94dee8bf0dd2d1225a9ea`; logs `.godot/ts183-import-rendered.log` and `.errors.log`.
- **VERIFIED desktop observation via Computer Use:** clicked Import Configs in an isolated rendered local host; native Windows **Open a File** dialog displayed selected `configs.txt`, the Trackstorm text-export filter and Open/Cancel actions. The automation API then activated the parent instead of focusing the modal field; no successful native OS Open click is claimed. Inspection log: `.godot/ts183-import-inspect.log`, empty stderr; task-owned inspection processes were stopped.
- `./check-network-vehicles.ps1 -GodotPath $Godot -Players 3 -NoBuild`: **PASS**, three separate native processes, driving/camera and network regression assertions. Artifacts: `.godot/network-vehicle-checks/7dfd3d086deb4953884b8d796e52b57b`; log `.godot/ts183-import-network.log`. Client correction p99 **0.502 / 0.451 m**, maxima **0.825 / 2.143 m**. One headless client recorded mean frame time **58.28 ms**, p99 **207.45 ms**, maximum **221.32 ms**; the other maximum was **39.88 ms**. This was not an isolated performance benchmark and is not evidence of hitch-free operation or an import-caused regression. No impaired-network rerun was performed in this follow-up; Round 1 impairment evidence remains historical.
- `$Godot --headless --path . --quit-after 120`: **PASS**, exit 0, no ERROR/WARNING. Log: `.godot/ts183-import-smoke.log`.

- `./check-transport.ps1 -GodotPath $Godot`: **PASS**, **391 Debug tests** including native capacity/reconnect and impairment/reliable ordering, plus **3 Godot transport lifecycle cycles**. Log: `.godot/ts183-import-transport.log`.
- `./check-migration.ps1 -GodotPath $Godot -Players 3 -NoBuild`: **PASS**, intentional host leave/former-host rejoin, active-match authority loss, successive epochs and gameplay/configuration recovery. Identity is a trusted seam. Log: `.godot/ts183-import-migration.log`.
- Final Story diff inspection, `git diff --check`, and changed-document relative file-link existence check: **PASS**. Production Core remains engine independent; no Shared layer or new dependency was introduced.

### Follow-up assumptions, limitations and unverified areas

- Import is a merge of explicitly listed supported settings, **not a full snapshot restore**. A default-only export is a no-op and cannot reset unrelated current values. Apply can include unrelated existing drafts because it retains the normal editor workflow.
- Core import validation uses current authoritative configuration; Apply revalidates against the host's latest state. Same-field concurrent transactions retain existing host receive-order semantics.
- The UI caps actual bytes at 64 KiB and requires UTF-8; Core independently caps text length. No file-format migration for removed/renamed keys, external preset manager or defaults promotion was added.
- **UNVERIFIED:** actual OS Open/Save completion, authenticated EOS/WAN/multiple physical PCs, packaged Windows execution, physical-controller ergonomics, extended soak, subjective balance and isolated performance. The UI harness injects logical input; native callback/file/UDP checks do not prove OS dialog completion or external-network behavior.
- The original Story evidence remains historical where not explicitly rerun here. No new assets/dependencies; the user's pre-existing grass import change remains excluded. Independent engineering review, human acceptance and remote CI are separate gates.


### Follow-up delivery observation

Before this push, PR #123's [CI run 36251984499](https://github.com/JonathanGenier/Trackstorm/actions/runs/36251984499) had failed its current-main ancestry/version gate because main advanced. This follow-up includes that main commit and passes the local version/ancestry check at 0.1.71. A new post-push CI result is not inferred from the local pass. The existing PR is reused; independent engineering review and human acceptance remain pending.

## Astra Runtime Critique — Story Round 2

**Overall Score: 8.1 / 10**

**Quality Assessment: PASS (>=8.0)**

| Material runtime category | Score | Observed basis |
| --- | --- | --- |
| Runtime functionality | 8.3 | Valid exported files stage and apply correctly; malformed/read/cancel/stale-owner failures preserve state; active-match restrictions reject atomically. |
| Multiplayer experience | 8.3 | Host and client imports converge across three peers, preserve another peer's unlisted edit, reach native gameplay owners and produce equal re-exports. Shared late-admission, reset, reconnect and migration checks also passed. |
| UI / UX | 8.0 | Discoverable adjacent Import/Export actions, readable small-to-ultrawide layouts, explicit staged/Apply and source-version feedback. Native Open dialog presentation observed; complete OS selection remains unverified. |
| Runtime integration | 8.3 | Imported numeric values survive the actual controls and existing Apply authority path; accepted state stays session-owned and reload does not promote it into defaults. |
| Runtime stability | 8.1 | Scoped native import, file-error, lifecycle and recovery scenarios completed without runtime errors. Separate-process driving passed but recorded the frame stalls above; no smooth-performance claim is inferred. |

**What Was Exercised:** VERIFIED production Configs controls and FileDialog callbacks with real files, actual native UDP host plus two clients, imported max-HP gameplay consumption, repeated shared changes/resets, late join, reconnect, migration, equal re-exports, rendered menu navigation/layout and native Open-dialog presentation. The import-specific checks cover both request origins and whole-transaction rejection. INFERRED: an imported accepted configuration recovers through the same fully exercised shared reconnect boundary; an import-immediately-followed-by-reconnect scenario was not separately run.

**Runtime / Operational Limitations:** UNVERIFIED end-to-end OS Open/Save-button completion, authenticated EOS/WAN/multiple physical devices, exported package execution, physical controller ergonomics, long soak and isolated performance. Automation activated the parent instead of the modal, so callback success is reported separately from picker completion. Headless frame stalls remain visible in evidence and are not attributed to import without a controlled comparison. Gameplay balance, audio and movement feel were not rescored from serialization tests.

The observed workflow is reliable and clear: selecting a valid export stages precisely its listed settings, while host validation governs the eventual shared result. Error paths preserve the operator's work. The remaining OS-dialog and external-network observation gaps limit confidence beyond the tested local workflow. The **8.1** score is a direct assessment of that result, not a rounded category average or a test-count score.

Stop for explicit human direction after presenting this formal critique. No acceptance or merge authorization is implied.
